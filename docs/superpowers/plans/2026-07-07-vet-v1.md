# vet v1 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** spec 없는 일상 지대에서 세션이 조용히 갭을 찍지 않게 만드는 3층위 규율 플러그인 `vet`을 구현한다 — 항상 켜짐 훅 규율 + 작업 계약 + 옵트인 `/vet` 답변 게이트(렌즈 3검증자 합의) + 벤치 세트.

**Architecture:** 층위 1·2는 SessionStart 훅 하나의 `additionalContext` 문안(게이트 리더 3라운드 3/3 수렴안). 층위 3은 `/vet` 스킬(세션이 C-n 초안 작성·수정·재검증 트리거)과 Workflow scriptPath JS 엔진(검증자 3명 스폰 + 순수 코드 집계, 무상태·최대 2회 호출)의 분업. 벤치는 문서형 산출물(문항+정답 키, 채점 프로토콜).

**Tech Stack:** Claude Code 플러그인(hooks.json 자동 발견, SKILL.md), Workflow 엔진 스크립트(JS, `agent()`/`parallel()` 전역), Node 순정 테스트(`node:assert`).

**Spec:** `plugins/vet/specs/001-vet-v1/spec.md` (Gate: passed). 게이트 밖 사용자 결정 2건은 `gate-report.md` 말미 "구현 단계 에스컬레이션 기록" 참조.

## Global Constraints

(spec에서 그대로 — 모든 태스크의 요구사항에 암묵 포함)

- 플러그인 이름 `vet`, 커맨드 `/vet` (I-4, FR-001·002·010).
- 검증자 **정확히 3명**, 렌즈 고정: A 전제·사실 / B 논리·반례 / C 의도 부합 (FR-012).
- 엔진의 **모든 `agent()` 호출에 `model: 'opus'` 명시** — 세션 모델 상속 금지 (D-12·D-13).
- 확정 반박 = **같은 `C-n` 앵커 AND 같은 카테고리 enum에 서로 다른 검증자 2표 이상**, 순수 코드 집계, LLM 심판 금지. 1인 단독·앵커/카테고리 불일치는 정보성 (FR-013·014).
- 카테고리 enum은 **세 렌즈 공유**(교차-렌즈 합의 표현 가능) (D-11).
- 확정 반박 0건 → 재검증 호출 0회(초안 즉시 최종). 수정 발생 → **전면 재검증 정확히 1회**. 재검증 잔존 확정 반박 → 반영 후 "미해소" 표시하고 종료, 3회차 없음 (FR-016, SC-003).
- 검증자 실패 시 **실패한 검증자만 1회 재시도** → 그래도 실패면 부분 집계 없이 "⚠ 검증 미완료" 배너 + 초안 전달 (FR-017, SC-004).
- 검증자 도구 = **저장소 읽기(Read/Grep/Glob)만, 웹·쓰기·실행 금지** — 프롬프트로 강제 (FR-013 + 2026-07-07 사용자 결정).
- 초안은 검증자 프롬프트에 **인라인 전달**(파일 경로 금지) (FR-013).
- 작업 계약 = 정확히 4필드(의도 재진술 / 조사로 확인한 것 / 남은 가정 / 완료 기준), 선언 후 대기 없이 진행. 생략은 자명할 때만: 대상 AND 할 일 AND 완료 기준 전부 명시, 또는 순수 질문·대화 턴 (FR-006~008).
- 층위 1 네 규율: 실측 조사 / 가정 선언 / 전제 검증 / 추천 커밋 — 관찰 가능한 문장으로 (FR-004).
- 벤치 = **10문항**, 도메인 혼합(chenjing-plugins 레포 사실 + 공개 기술 사실), 문항별 **정답 키**(핵심 사실 + 실측 근거 파일:라인 또는 출처), 채점 = 일치/모순/무언급 3분류·**모순만 확정 오류**·저자 수동 대조, arm = vet(opus 초안+opus 검증 3) vs 기준선(claude-fable-5 단독 ×1) (FR-018, I-10·I-11, 2026-07-07 결정: 구현 세션 출제 + 저자 검수).
- **best-of-N 구현 금지** (FR-019).
- `/vet` 산출물은 일회성(파일 영속화 없음). 모든 사용자 대면 문안은 한국어.

---

### Task 1: 패키징 골격 (plugin.json + 마켓플레이스 등록)

**Files:**
- Create: `plugins/vet/.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json` (plugins 배열 말미에 항목 추가)

**Interfaces:**
- Produces: 플러그인 루트 `plugins/vet/` — 이후 태스크의 hooks/skills/bench가 이 아래 들어감. `specs/001-vet-v1/`은 이미 존재하므로 건드리지 않는다.

- [ ] **Step 1: plugin.json 작성**

`plugins/vet/.claude-plugin/plugin.json`:

```json
{
  "name": "vet",
  "description": "spec 없는 일상 지대의 규율 세트 — 항상 켜짐 규율 훅 + 작업 계약 + 옵트인 답변 게이트 /vet (렌즈 3검증자 2표 합의)",
  "version": "0.1.0"
}
```

- [ ] **Step 2: marketplace.json에 vet 항목 추가**

`.claude-plugin/marketplace.json`의 `plugins` 배열에서 `tiers` 항목(마지막) 뒤에 추가:

```json
    {
      "name": "vet",
      "description": "spec 없는 일상 지대의 규율 세트 — 항상 켜짐 규율 훅 + 작업 계약 + 옵트인 답변 게이트 /vet (렌즈 3검증자 2표 합의)",
      "source": "./plugins/vet",
      "skills": [
        "./skills/vet"
      ]
    }
```

(기존 4개 항목은 수정하지 않는다 — FR-001 "별개 항목".)

- [ ] **Step 3: JSON 유효성 검증**

Run: `jq -e '.name=="vet" and .version=="0.1.0"' plugins/vet/.claude-plugin/plugin.json && jq -e '.plugins | length == 5 and (.[4].name=="vet") and (.[4].skills==["./skills/vet"])' .claude-plugin/marketplace.json`
Expected: `true` 두 번, exit 0

- [ ] **Step 4: Commit**

```bash
git add plugins/vet/.claude-plugin/plugin.json .claude-plugin/marketplace.json
git commit -m "feat(vet): register vet plugin skeleton in marketplace"
```

---

### Task 2: 층위 1+2 — SessionStart 훅 규율 문안

**Files:**
- Create: `plugins/vet/hooks/hooks.json`

**Interfaces:**
- Consumes: Task 1의 `plugins/vet/` 루트.
- Produces: 매 세션 주입되는 규율 문안. 층위 1(네 규율)과 층위 2(작업 계약 프로토콜)를 **한 `additionalContext`에 합쳐** 주입한다(게이트 리더 3라운드 3/3 수렴, FR-003·D-9). hooks.json은 자동 발견되므로 plugin.json 참조 불필요.

- [ ] **Step 1: hooks.json 작성**

`plugins/vet/hooks/hooks.json` (fableus `hooks/hooks.json`과 동일 패턴 — echo-JSON command 훅, 비차단. 문안에 작은따옴표를 쓰지 말 것 — 셸 인용이 깨진다):

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"[vet 규율] 1. 실측 우선: 사실 주장을 하기 전에 파일·코드·문서를 직접 확인하라 — 짐작으로 단정하지 마라. 2. 가정 선언: 침묵 가정 금지 — 조사로 닫지 못한 가정은 산출물에 명시하라. 3. 전제 검증: 요청에 깔린 전제(파일 존재·상태·원인 추정)를 행동 전에 확인하라. 4. 추천 커밋: 선택지 나열로 끝내지 말고 추천 1개 + 근거로 커밋하라. [작업 계약] 자명하지 않은 작업 요청은 실행 전에 5줄 내외의 작업 계약을 선언하고, 확인을 기다리지 말고 바로 진행하라. 계약 필드 4개: 의도 재진술 / 조사로 확인한 것 / 남은 가정 / 완료 기준. 생략은 요청이 자명할 때만: 대상·할 일·완료 기준이 전부 명시돼 짐작할 것이 0개이거나, 순수 질문·대화 턴일 때. 계약은 파괴적 작업의 권한 확인을 대체하지 않는다.\"}}'"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 훅 커맨드 실행 검증 (JSON 유효 + 필수 문구 포함)**

Run:
```bash
jq -e '.hooks.SessionStart[0].hooks[0].type=="command"' plugins/vet/hooks/hooks.json
bash -c "$(jq -r '.hooks.SessionStart[0].hooks[0].command' plugins/vet/hooks/hooks.json)" | jq -e '.hookSpecificOutput.hookEventName=="SessionStart" and (.hookSpecificOutput.additionalContext | contains("실측 우선") and contains("가정 선언") and contains("전제 검증") and contains("추천") and contains("작업 계약") and contains("의도 재진술") and contains("완료 기준") and contains("자명"))'
```
Expected: `true` 두 번, exit 0 (네 규율 FR-004 + 4필드·자명 술어 FR-006·007이 문안에 실재함을 기계 확인)

- [ ] **Step 3: Commit**

```bash
git add plugins/vet/hooks/hooks.json
git commit -m "feat(vet): layer 1+2 discipline via SessionStart additionalContext hook"
```

---

### Task 3: 층위 3 엔진 — engine.js (검증자 3 스폰 + 순수 코드 집계) — TDD

**Files:**
- Create: `plugins/vet/tests/engine.test.mjs` (테스트 먼저)
- Create: `plugins/vet/skills/vet/engine.js`

**Interfaces:**
- Consumes: 없음 (무상태 Workflow 스크립트 — `agent`/`parallel`/`log`/`phase`/`args` 전역은 하네스가 주입).
- Produces: `/vet` 스킬(Task 4)이 호출하는 엔진. **호출 계약**: `Workflow({scriptPath: '<plugin-root>/skills/vet/engine.js', args: {question: string, draft: string, round: 1|2}})`. **반환 계약**(Task 4·테스트가 이 필드명에 의존):
  - 성공: `{round, unverified: false, confirmed: [{anchor, category, votes, voters, evidence: [{lens, evidence}]}], informational: [동일 구조], invalidAnchors: [...], anchorsInDraft: number, stats: {validatorCalls, model}}`
  - 검증 실패(재시도 후에도): `{round, unverified: true, failedLenses: [...], confirmed: [], informational: [], stats}` — 부분 집계 금지
  - 입력 오류: `{error: string}`

- [ ] **Step 1: 실패하는 테스트 작성**

Workflow 스크립트는 `export const meta` + 최상위 `return`이라 단독 실행이 불가능하다(하네스가 async 함수로 감싼다). 테스트는 하네스와 동일하게 소스를 `AsyncFunction`으로 감싸고 전역을 스텁한다.

`plugins/vet/tests/engine.test.mjs`:

```js
// vet-verify 엔진 스모크 테스트 — 하네스 밖에서 전역(agent/parallel/…)을 스텁해 실행한다.
// 실행: node plugins/vet/tests/engine.test.mjs
import { readFile } from 'node:fs/promises'
import assert from 'node:assert/strict'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'

const here = dirname(fileURLToPath(import.meta.url))
const src = await readFile(join(here, '..', 'skills', 'vet', 'engine.js'), 'utf8')
const body = src.replace(/^export const meta =/m, 'const meta =')
const AsyncFunction = Object.getPrototypeOf(async function () {}).constructor

async function run(argsValue, agentImpl) {
  const calls = []
  const agent = async (prompt, opts) => { calls.push({ prompt, opts }); return agentImpl(prompt, opts, calls.length) }
  const parallel = (thunks) => Promise.all(thunks.map(t => Promise.resolve().then(t).catch(() => null)))
  const noop = () => {}
  const fn = new AsyncFunction('args', 'agent', 'parallel', 'log', 'phase', 'budget', 'workflow', body)
  const result = await fn(argsValue, agent, parallel, noop, noop, { total: null, spent: () => 0, remaining: () => Infinity }, noop)
  return { result, calls }
}

const DRAFT = '- C-1 주장 하나\n- C-2 주장 둘\n- C-3 주장 셋'
const rb = (anchor, category, evidence = '근거') => ({ anchor, category, evidence })

// 1) FR-014: 같은 앵커+카테고리 2표 = 확정, 나머지는 정보성. 같은 앵커·다른 카테고리는 별개.
{
  const perLens = [
    { rebuttals: [rb('C-1', 'fact-error'), rb('C-2', 'logic-flaw')] },
    { rebuttals: [rb('C-1', 'fact-error')] },
    { rebuttals: [rb('C-1', 'logic-flaw')] },
  ]
  let i = 0
  const { result, calls } = await run({ question: 'q', draft: DRAFT }, () => perLens[i++])
  assert.equal(result.unverified, false)
  assert.equal(calls.length, 3)
  assert.equal(result.confirmed.length, 1)
  assert.equal(result.confirmed[0].anchor, 'C-1')
  assert.equal(result.confirmed[0].category, 'fact-error')
  assert.equal(result.confirmed[0].votes, 2)
  assert.equal(result.informational.length, 2)
  // D-12/D-13: 모든 호출이 opus 고정
  for (const c of calls) assert.equal(c.opts.model, 'opus')
  // FR-013: 초안 인라인 전달
  for (const c of calls) assert.ok(c.prompt.includes('C-2 주장 둘'))
}

// 2) FR-017 전반: 검증자 1명 첫 시도 실패 → 그 검증자만 1회 재시도 → 성공
{
  let failedOnce = false
  const { result, calls } = await run({ question: 'q', draft: DRAFT }, (p, opts) => {
    if (opts.label.startsWith('verify:logic') && !failedOnce) { failedOnce = true; return null }
    return { rebuttals: [] }
  })
  assert.equal(result.unverified, false)
  assert.equal(calls.length, 4) // 3 + 실패분 재시도 1 (전체 재실행 아님)
  assert.equal(result.stats.validatorCalls, 4)
}

// 3) FR-017 후반 / I-7: 재시도 후에도 실패 → unverified, 부분 집계 금지
{
  const { result } = await run({ question: 'q', draft: DRAFT }, (p, opts) =>
    opts.label.startsWith('verify:intent') ? null : { rebuttals: [rb('C-1', 'fact-error')] })
  assert.equal(result.unverified, true)
  assert.deepEqual(result.failedLenses, ['intent'])
  assert.equal(result.confirmed.length, 0)
  assert.equal(result.informational.length, 0)
}

// 4) 방어: 초안에 없는 앵커는 격리, 같은 검증자의 같은 (앵커,카테고리) 중복 제기는 1표
{
  const perLens = [
    { rebuttals: [rb('C-9', 'fact-error')] },
    { rebuttals: [rb('C-1', 'fact-error'), rb('C-1', 'fact-error')] },
    { rebuttals: [rb('C-1', 'fact-error')] },
  ]
  let i = 0
  const { result } = await run({ question: 'q', draft: DRAFT }, () => perLens[i++])
  assert.equal(result.confirmed.length, 1)
  assert.equal(result.confirmed[0].votes, 2)
  assert.equal(result.invalidAnchors.length, 1)
}

// 5) 입력 계약: 앵커 없는 초안·빈 입력은 error 반환
{
  const { result: r1 } = await run({ question: 'q', draft: '앵커가 하나도 없는 초안' }, () => ({ rebuttals: [] }))
  assert.ok(r1.error)
  const { result: r2 } = await run({ question: '', draft: DRAFT }, () => ({ rebuttals: [] }))
  assert.ok(r2.error)
}

console.log('engine.test.mjs: 5/5 통과')
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node plugins/vet/tests/engine.test.mjs`
Expected: FAIL — `ENOENT ... engine.js` (엔진 미존재)

- [ ] **Step 3: engine.js 구현**

`plugins/vet/skills/vet/engine.js`:

```js
export const meta = {
  name: 'vet-verify',
  description: 'vet /vet 답변 게이트 엔진 — C-n 앵커 초안을 렌즈 분산 검증자 3명(opus 고정)이 병렬 검증하고, 순수 코드가 (앵커+카테고리) 2표 합의만 확정 반박으로 집계한다. Args: {question, draft, round}',
  whenToUse: '/vet 스킬이 검증(round 1)·재검증(round 2)에 호출한다. 초안의 각 원자 주장에 C-n 앵커가 있어야 한다.',
  phases: [{ title: 'Verify', detail: '렌즈 3검증자 병렬 (opus/high 고정, 실패분만 재시도 1회)' }],
}

// vet-verify: C-n 초안 → 렌즈 3검증자 병렬(schema 강제) → 순수 코드 2표 집계.
// 계약 (specs/001-vet-v1/spec.md):
//   FR-012 검증자 정확히 3명, 렌즈 A 전제·사실 / B 논리·반례 / C 의도 부합
//   FR-013 세 렌즈 공유 카테고리 enum + C-n 앵커 필수 · 초안 인라인 전달 ·
//          저장소 읽기 전용, 웹 배제 (2026-07-07 사용자 결정 — gate-report 에스컬레이션 기록)
//   FR-014 확정 = 같은 앵커 AND 같은 카테고리에 서로 다른 검증자 2표 이상, LLM 심판 금지
//   FR-017 실패 검증자만 1회 재시도 → 그래도 실패면 부분 집계 없이 unverified 반환
//   D-12/D-13 모든 agent() 호출에 model 명시(opus) — 세션 모델 상속 금지

let A = args || {}
if (typeof A === 'string') {
  const s = A.trim()
  let parsed = null
  if (s.startsWith('{')) { try { parsed = JSON.parse(s) } catch {} }
  A = (parsed && typeof parsed === 'object') ? parsed : {}
}
const QUESTION = (A.question || '').trim()
const DRAFT = (A.draft || '').trim()
const ROUND = A.round === 2 ? 2 : 1

if (!QUESTION || !DRAFT) {
  return { error: 'question과 draft가 모두 필요하다 — /vet 스킬이 {question, draft, round}로 호출한다.' }
}
if (DRAFT.length > 40000) {
  return { error: '초안이 너무 크다(40k자 초과). 질문을 좁혀 초안을 축소한 뒤 다시 호출하라.' }
}
const ANCHORS = [...new Set(DRAFT.match(/\bC-\d+\b/g) || [])]
if (ANCHORS.length === 0) {
  return { error: '초안에 C-n 앵커가 없다. 각 원자 주장에 C-1, C-2… 앵커를 붙여 다시 호출하라.' }
}

const CATEGORIES = ['fact-error', 'premise-error', 'logic-flaw', 'intent-mismatch', 'unsupported']
const VERDICT_SCHEMA = {
  type: 'object', required: ['rebuttals'],
  properties: {
    rebuttals: { type: 'array', maxItems: 10, items: {
      type: 'object', required: ['anchor', 'category', 'evidence'],
      properties: {
        anchor: { type: 'string', pattern: '^C-[0-9]+$' },
        category: { enum: CATEGORIES },
        evidence: { type: 'string' },
      },
    }},
  },
}

const LENSES = [
  { key: 'fact', title: '렌즈 A — 전제·사실', focus: '초안의 사실 주장과 전제가 실측과 일치하는가. 저장소 파일을 Read/Grep/Glob으로 직접 열어 대조하라. 저장소로 확인 불가능한 외부 사실의 무근거 단정은 unsupported로 표시하라.' },
  { key: 'logic', title: '렌즈 B — 논리·반례', focus: '주장 사이의 추론이 타당한가, 반례가 존재하는가. 숨은 전제 오류(premise-error)와 비약·과잉 일반화(logic-flaw)를 찾아라.' },
  { key: 'intent', title: '렌즈 C — 의도 부합', focus: '초안이 질문이 실제로 묻는 것에 답하는가. 질문 재해석 오류·초점 이탈·묻지 않은 것에 대한 답변(intent-mismatch)을 찾아라.' },
]

const PROMPT = (lens) =>
  '## vet 검증자 — ' + lens.title + ' (라운드 ' + ROUND + ')\n\n' +
  '아래는 사용자 질문과, 세션이 작성한 답변 초안이다. 너의 렌즈로 초안을 검증하라.\n' +
  '렌즈: ' + lens.focus + '\n\n' +
  '## 질문\n' + QUESTION + '\n\n' +
  '## 초안 (원자 주장마다 C-n 앵커)\n' + DRAFT + '\n\n' +
  '## 규칙\n' +
  '- 반박 카테고리(세 렌즈 공용): fact-error(실측과 모순) / premise-error(숨은 전제가 틀림) / logic-flaw(추론 결함·반례) / intent-mismatch(질문이 묻는 것과 불일치) / unsupported(근거 없는 단정). 렌즈는 주의 초점일 뿐, 발견한 반박은 어느 카테고리로든 낼 수 있다.\n' +
  '- 모든 반박은 초안에 실재하는 C-n 앵커 하나를 지목해야 한다. 앵커 없는 일반론은 내지 마라.\n' +
  '- 도구는 저장소 읽기(Read/Grep/Glob)만 사용하라. 웹 검색·페이지 가져오기·파일 쓰기·명령 실행은 금지다.\n' +
  '- evidence는 구체적으로: 실측 반박이면 파일:라인, 논리 반박이면 반례를 명시하라.\n' +
  '- 확실한 반박만 내라 — 문체·취향·사소한 표현 차이는 반박이 아니다. 반박이 없으면 빈 배열을 반환하라.\n\n' +
  'Structured output only.'

phase('Verify')
log('vet r' + ROUND + ': 앵커 ' + ANCHORS.length + '개, 검증자 3명(opus) 스폰')

const spawn = (lens) => agent(PROMPT(lens), {
  label: 'verify:' + lens.key + ':r' + ROUND,
  phase: 'Verify',
  schema: VERDICT_SCHEMA,
  model: 'opus',
  effort: 'high',
})

let reports = await parallel(LENSES.map(l => () => spawn(l)))
let validatorCalls = 3

// FR-017: 실패한 검증자만 1회 재시도 (전체 재실행 아님)
const failedIdx = reports.map((r, i) => (r ? -1 : i)).filter(i => i >= 0)
if (failedIdx.length > 0) {
  log('검증자 재시도: ' + failedIdx.map(i => LENSES[i].key).join(', '))
  const retries = await parallel(failedIdx.map(i => () => spawn(LENSES[i])))
  failedIdx.forEach((idx, j) => { reports[idx] = retries[j] })
  validatorCalls += failedIdx.length
}

const failedLenses = LENSES.filter((l, i) => !reports[i]).map(l => l.key)
if (failedLenses.length > 0) {
  // I-7: 부분 집계 금지 — 합의 기준이 라운드마다 변동하지 않도록 전체를 미검증 처리
  log('검증 미완료: ' + failedLenses.join(', ') + ' — 부분 집계 없이 반환')
  return {
    round: ROUND, unverified: true, failedLenses,
    confirmed: [], informational: [],
    stats: { validatorCalls, model: 'opus/high' },
  }
}

// ─── 순수 코드 집계 (FR-014 — LLM 심판 금지) ───
const invalidAnchors = []
const votes = new Map() // 'C-n|category' → {anchor, category, voters: [], evidence: []}
reports.forEach((rep, i) => {
  const lens = LENSES[i].key
  const seenKeys = new Set() // 같은 검증자의 같은 (앵커,카테고리) 중복 제기는 1표
  for (const r of rep.rebuttals) {
    if (!ANCHORS.includes(r.anchor)) { invalidAnchors.push({ lens, ...r }); continue }
    const key = r.anchor + '|' + r.category
    if (seenKeys.has(key)) continue
    seenKeys.add(key)
    if (!votes.has(key)) votes.set(key, { anchor: r.anchor, category: r.category, voters: [], evidence: [] })
    const v = votes.get(key)
    v.voters.push(lens)
    v.evidence.push({ lens, evidence: r.evidence })
  }
})

const anchorNum = a => parseInt(a.slice(2), 10)
const all = [...votes.values()]
  .map(v => ({ anchor: v.anchor, category: v.category, votes: v.voters.length, voters: v.voters, evidence: v.evidence }))
  .sort((a, b) => anchorNum(a.anchor) - anchorNum(b.anchor))
const confirmed = all.filter(v => v.votes >= 2)
const informational = all.filter(v => v.votes < 2)

log('집계 r' + ROUND + ': 반박 ' + all.length + '건 → 확정 ' + confirmed.length + ' · 정보성 ' + informational.length +
  (invalidAnchors.length ? ' · 무효 앵커 ' + invalidAnchors.length : ''))

return {
  round: ROUND, unverified: false,
  confirmed, informational, invalidAnchors,
  anchorsInDraft: ANCHORS.length,
  stats: { validatorCalls, model: 'opus/high' },
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node plugins/vet/tests/engine.test.mjs`
Expected: PASS — `engine.test.mjs: 5/5 통과`

- [ ] **Step 5: Commit**

```bash
git add plugins/vet/skills/vet/engine.js plugins/vet/tests/engine.test.mjs
git commit -m "feat(vet): /vet verification engine — 3 lens validators + pure-code 2-vote aggregation (TDD)"
```

---

### Task 4: /vet 스킬 — SKILL.md (초안·수정·재검증 오케스트레이션)

**Files:**
- Create: `plugins/vet/skills/vet/SKILL.md`

**Interfaces:**
- Consumes: Task 3 엔진의 호출·반환 계약 (`{question, draft, round}` → `{unverified, confirmed, informational, failedLenses, stats, error}`).
- Produces: `/vet` 커맨드. 세션 몫 = 초안 작성 / 확정 반박 반영 수정 / 재검증 트리거 / 수정 내역 표시 (엔진은 무상태 검증+집계만 — 게이트 리더 3/3 수렴 역할 경계).

- [ ] **Step 1: SKILL.md 작성**

`plugins/vet/skills/vet/SKILL.md` (frontmatter 관례는 tiers 커맨드 스킬과 동일 — `disable-model-invocation: true` + "Use when the user runs /…"):

````markdown
---
name: vet
description: "Use when the user runs /vet — 중요한 질문의 답변을 C-n 앵커 초안 → 렌즈 3검증자(opus 고정) 병렬 검증 → 2표 확정 반박만 반영하는 옵트인 답변 게이트."
argument-hint: "<질문> — 검증해서 답할 중요한 질문"
disable-model-invocation: true
---

# /vet — 옵트인 답변 게이트

중요한 질문 하나에 대해: 세션이 전제를 실측한 C-n 초안을 만들고, 번들 엔진
(`${CLAUDE_PLUGIN_ROOT}/skills/vet/engine.js`)이 렌즈가 다른 검증자 3명(opus
고정)을 병렬 스폰하며, 순수 코드가 (앵커+카테고리) 2표 합의만 확정 반박으로
집계한다. 세션은 확정 반박만 반영해 수정하고 수정 내역을 투명 표시한다.
이 스킬은 수동 옵트인 전용이다 — 층위 1·2 규율이 자동으로 이걸 트리거하지
않는다 (FR-010).

## 절차

### 1. 입력
- 질문 = $ARGUMENTS. 비어 있으면 직전 대화에서 명백할 때만 그것을 쓰고,
  아니면 한 줄로 물어라.
- 질문이 한 턴에 담기 힘들 만큼 크면 거부하고 축소를 권고하라(분할 없음).

### 2. 초안 (세션이 직접 작성)
- 질문을 하위 질문으로 분해하고, **전제를 실측**하라(Read/Grep/Glob으로 파일
  확인 — 검증자에게 미루지 말 것).
- 답변을 원자 주장 단위로 쪼개고 각 주장 앞에 안정적 앵커를 붙여라:

  ```
  - C-1: <원자 주장 1> (근거: <파일:라인 또는 추론>)
  - C-2: <원자 주장 2> …
  ```
- 초안 모델 = 지금 이 세션의 모델 그대로(FR-011). 벤치에서만 저자가 opus
  세션에서 실행해 초안을 opus로 고정한다 — 스킬에 별도 벤치 분기는 없다.

### 3. 검증 (엔진 호출, round 1)
`Workflow({scriptPath: '${CLAUDE_PLUGIN_ROOT}/skills/vet/engine.js', args: {question: <질문>, draft: <초안 전문>, round: 1}})`

- `args`는 **실제 JSON 객체**로 넘겨라 — JSON 문자열로 감싸면 안 된다.
- 경로에 `${CLAUDE_PLUGIN_ROOT}`가 문자 그대로 남아 있으면 이 스킬의 base
  directory + `/engine.js`를 쓰라.
- 반환에 `error`가 있으면(앵커 누락 등) 초안을 고쳐 다시 호출하라.

### 4. 결과 처리
- **`unverified: true`** → 수정 없이 초안을 그대로 전달하되 맨 위에 배너:
  `⚠ 검증 미완료 — 검증자 실패(<failedLenses>), 재시도 1회 후에도 실패. 아래 답변은 미검증 초안이다.` 종료 (FR-017 — 답을 삼키지 않는다).
- **확정 반박 0건** → 초안이 즉시 최종. 재검증을 돌리지 마라(추가 호출 0,
  SC-003). 5로 가서 "변경 없음"으로 마감.
- **확정 반박 ≥1건** → 모든 확정 반박을 반영해 초안을 수정하라. 수정 시
  앵커를 유지하라(같은 주장은 같은 C-n, 주장 삭제 시 번호 재사용 금지).
  정보성 반박은 수정 근거로 쓰지 마라(FR-015 — 표시만 한다). 그리고:
  **재검증 정확히 1회** — 수정된 초안 전문으로 엔진을 `round: 2`로 재호출
  (FR-016, 전면 재검증). round 2 결과의 확정 반박이 남으면 반영하되 "미해소"
  로 표시하고 종료한다 — 3회차 호출은 없다.

### 5. 최종 출력 (한 메시지에)
최종 답변 본문 + 수정 내역 섹션:

```
## 수정 내역 (/vet)
- C-3 [fact-error · 2표(fact,logic)]: "<수정 전 요지>" → "<수정 후 요지>" — 근거: <evidence 요약>
- (r2 미해소) C-7 [logic-flaw · 2표]: <반영 내용> — 재검증에서 잔존, 반영 후 종료
- (정보성 · 수정 강제 없음) C-5 [unsupported · 1표(intent)]: <요지>
- 검증 비용: 검증자 호출 <stats.validatorCalls 합계>회 (r1: N, r2: M)
```

확정 반박 0건이면 첫 줄을 `- 변경 없음 (확정 반박 0건)`으로.
`/vet` 산출물은 이 대화 턴에만 존재한다 — 파일로 저장하지 마라(일회성).

## 하지 말 것
- 정보성(1표) 반박으로 수정을 강제하는 것 (FR-015).
- 3회차 검증·수렴 루프 (FR-016 — 재검증은 수정 시 정확히 1회).
- 부분 집계로 진행하거나 답변을 보류하는 것 (FR-017).
- 복수 초안 경쟁(best-of-N) — v1 범위 밖 (FR-019).
- 검증자에게 파일 경로만 넘기는 것 — 엔진이 초안을 인라인로 전달한다 (FR-013).
````

- [ ] **Step 2: 형식 검증**

Run:
```bash
head -6 plugins/vet/skills/vet/SKILL.md | grep -c 'name: vet\|disable-model-invocation: true\|argument-hint'
grep -c 'round: 2\|unverified\|정보성\|미해소\|일회성' plugins/vet/skills/vet/SKILL.md
```
Expected: 첫 명령 `3`, 둘째 명령 `5` 이상 (필수 요소 실재)

- [ ] **Step 3: Commit**

```bash
git add plugins/vet/skills/vet/SKILL.md
git commit -m "feat(vet): /vet skill — draft/revise/single-reverify orchestration over the engine"
```

---

### Task 5: 벤치 문항 10 + 정답 키 (초안 — 저자 검수 대기)

**Files:**
- Create: `plugins/vet/bench/items.md`

**Interfaces:**
- Consumes: 레포 실측(문항 근거를 직접 Read/Grep으로 확인해야 함).
- Produces: 10문항 + 문항별 정답 키. Task 6 프로토콜이 이 파일을 채점 기준으로 참조. **상태 필드가 "초안"이면 벤치 실행 금지** — 저자(사용자) 검수 후 "확정"으로 바뀐다.

- [ ] **Step 1: 문항 파일 작성**

`plugins/vet/bench/items.md` — 아래 헤더·형식·예시 2문항을 그대로 쓰고, 나머지 8문항(repo 4 + public 4)을 **같은 형식으로 직접 실측해** 작성하라. 문항 규칙 (FR-018·I-8):

- 도메인 분포: `repo`(chenjing-plugins 사실) 6문항, `public`(공개 기술 사실 — Claude Code/플러그인/Workflow 등) 4문항.
- 각 문항의 정답 키 = 핵심 사실 3~6개, **항목마다 실측 근거 필수** — repo면 파일:라인(작성 전 반드시 그 파일을 Read로 열어 라인 확인), public이면 공식 문서·공식 출처 URL.
- 정답이 다툼거리인 문항 금지 — 실측으로 확정 안 되는 항목은 넣지 마라.
- 질문은 `/vet <질문>`에 그대로 넣을 수 있는 자연어 한 문단.

```markdown
# vet 벤치 — 10문항 + 정답 키

**상태**: 초안 — 저자 검수 대기  <!-- 초안 | 확정 (저자 검수 완료 후에만 벤치 실행) -->
**근거**: FR-018 (10문항·정답 키·실측 근거), I-11 (채점 = 일치/모순/무언급, 모순만 확정 오류), 2026-07-07 결정 (도메인 혼합, 구현 세션 출제 + 저자 검수)

## 형식
- **질문**: /vet에 그대로 넣을 한 문단.
- **도메인**: repo (chenjing-plugins 사실) | public (공개 기술 사실).
- **정답 키**: 핵심 사실 3~6항목, 각 항목에 실측 근거(파일:라인 또는 공식 출처).
- 채점 절차는 `protocol.md` 참조.

---

## Q1 (repo)
**질문**: fableus 플러그인의 /spec-gate는 콜드 리더를 어떻게 격리하고, 통과 판정 기준과 라운드 상한은 어떻게 되어 있어?

**정답 키**:
| # | 핵심 사실 | 근거 |
|---|---|---|
| 1 | 리더에게 도구·저장소 접근을 주지 않고, spec 본문을 프롬프트에 직접 포함한다(파일 경로 금지) | plugins/fableus/skills/spec-gate/SKILL.md:41-47 |
| 2 | 통과 = 확정 blocking 0건, 확정 = 같은 지점(앵커 일치 AND 논지 일치)을 2인 이상이 제기 | plugins/fableus/skills/spec-gate/SKILL.md:56-69 |
| 3 | 라운드 상한은 없다 — 중단은 사용자 몫 | plugins/fableus/skills/spec-gate/SKILL.md:23-26 |
| 4 | 집계에 별도 LLM 심판을 두지 않는다 | plugins/fableus/skills/spec-gate/SKILL.md:60-61 |

## Q2 (repo)
**질문**: tiers 플러그인의 deep-research 엔진은 검증(Verify) 단계에서 주장 하나를 몇 표로 판정하고, 판정 결과는 몇 가지로 나뉘어?

**정답 키**:
| # | 핵심 사실 | 근거 |
|---|---|---|
| 1 | 주장당 3표(VOTES_PER_CLAIM=3), 반박 2표(REFUTATIONS_REQUIRED=2) 이상이면 기각 | plugins/tiers/skills/deep-research/engine.js:40-41 |
| 2 | 판정은 3가지: survives / isRefuted / unverified(유효표 부족) | plugins/tiers/skills/deep-research/engine.js:290-303 |
| 3 | 인프라 실패(검증자 에러)는 "반박됨"으로 세지 않고 unverified로 분리한다 | plugins/tiers/skills/deep-research/engine.js:292,316-328 |

## Q3 (repo)
…(같은 형식으로 실측 작성)…

## Q7 (public)
…(같은 형식, 근거는 공식 문서 URL)…
```

- [ ] **Step 2: 형식 기계 검증**

Run: `grep -c '^## Q' plugins/vet/bench/items.md && grep -c '(repo)' plugins/vet/bench/items.md && grep -c '(public)' plugins/vet/bench/items.md && grep -c '상태.*초안' plugins/vet/bench/items.md`
Expected: `10` / `6` / `4` / `1`

- [ ] **Step 3: 근거 실측 재확인 (자가 검증)**

작성한 repo 근거의 파일:라인을 하나씩 다시 열어 실재하는지 확인하라(정답 키가 틀리면 벤치 전체가 무효 — I-11 "채점은 판단이 아니라 확인"의 전제). public 근거는 URL이 공식 출처(anthropic.com/docs 등)인지 확인.

- [ ] **Step 4: Commit**

```bash
git add plugins/vet/bench/items.md
git commit -m "feat(vet): bench 10 items + answer keys (draft, pending author review)"
```

---

### Task 6: 벤치 프로토콜 (실행·채점·기록)

**Files:**
- Create: `plugins/vet/bench/protocol.md`

**Interfaces:**
- Consumes: Task 5의 items.md (문항·정답 키), Task 4의 /vet 절차.
- Produces: 저자가 수동 1회 실행할 벤치의 정본 절차 + 기록 템플릿 (FR-018·SC-001).

- [ ] **Step 1: protocol.md 작성**

```markdown
# vet 벤치 프로토콜 — 실행·채점·기록 (FR-018 · SC-001 · I-10 · I-11)

## 전제
- `items.md` 상태가 **확정**(저자 검수 완료)일 것. 초안 상태로 실행 금지.
- 실행 창: Fable 접근 소멸 전, 저자 수동 1회 (자동 CI 아님).
- 두 arm은 같은 저장소, 같은 작업 디렉터리에서 실행한다 — 차이는 모델과 게이트 유무뿐.

## Arm 정의 (I-10)
- **vet arm**: 세션 모델 opus인 세션에서 `/vet <질문>` — 초안 opus + 검증자 opus 3 (+수정 시 재검증 3).
  env `CLAUDE_CODE_SUBAGENT_MODEL=opus`는 그대로 두어도 된다(검증자도 opus라 무해).
- **기준선**: 세션 모델 `claude-fable-5`인 세션에서 같은 질문을 **그대로**(/vet 없이, 게이트 없음) 1회 질문.
  도구 사용은 세션 재량에 맡긴다(실사용 Fable의 자연스러운 동작이 기준선).

## 실행 절차 (문항마다)
1. vet arm: 새 opus 세션 → `/vet <질문 원문>` → 최종 답변·수정 내역·검증 비용(stats)을 기록.
   부수 지표용으로 **초안**(수정 전)도 함께 저장해 둔다.
2. 기준선: 새 claude-fable-5 세션 → 질문 원문 그대로 → 답변 기록.
3. 세션을 문항마다 새로 연다(문항 간 오염 방지).

## 채점 (I-11 — 판단이 아니라 확인)
- 답변을 정답 키 항목별로 **일치 / 모순 / 무언급** 3분류한다.
- **모순 항목 수 = 그 답변의 확정 오류 수.** 무언급(누락)은 오류가 아니며 별도 기록한다.
- 채점 주체 = 저자가 정답 키와 수동 대조. vet 자신의 검증자나 제3 LLM으로 채점 금지(순환·주관성).
- 두 arm에 같은 정답 키를 동일하게 적용한다(분해 주관성을 정답 키가 흡수).

## 기록 템플릿 (문항마다)
| 문항 | arm | 확정 오류(모순) | 무언급 | 초안→최종 오류 (vet만) | 비용(검증자 호출) |
|---|---|---|---|---|---|
| Q1 | vet | | | → | |
| Q1 | 기준선 | | | — | 0 |

## 판정 (SC-001)
- 총합: vet arm 확정 오류 합계 ≤ 기준선 확정 오류 합계 → SC-001 충족.
- 문항별 표와 총합을 둘 다 보고한다.
- 부수 지표: vet 내부 초안 오류 수 → 최종 오류 수 감소량(게이트 자체의 기여).
- 비용 실측(호출 수·소요)을 함께 기록해 "예상 Opus ×2~4"를 사후 보정한다 (I-9 — "같은 가격" 슬로건 폐기, 실측이 정본).
```

- [ ] **Step 2: 형식 검증**

Run: `grep -c '## Arm 정의\|## 채점\|## 기록 템플릿\|## 판정' plugins/vet/bench/protocol.md`
Expected: `4`

- [ ] **Step 3: Commit**

```bash
git add plugins/vet/bench/protocol.md
git commit -m "feat(vet): bench protocol — arms, manual scoring vs answer keys, record template"
```

---

### Task 7: 사용자 검수 게이트 (컨트롤러 수행 — 서브에이전트 아님)

- [ ] items.md의 10문항·정답 키를 사용자에게 제시하고 검수를 받는다(2026-07-07 결정: 구현 세션 출제 + 저자 검수). 수정 요청 반영 후 상태를 `확정`으로 갱신·커밋한다. 사용자가 자리에 없으면 "초안" 상태로 두고 벤치 실행만 보류한다(다른 태스크 진행에는 영향 없음).
- [ ] fableus 스모크 테스트와 동일하게, 새 세션에서 `/vet` 노출·훅 주입을 확인하는 스모크 테스트를 사용자에게 안내한다(새 세션 필요 — 세션 시작 후 생성된 플러그인은 현 세션 레지스트리에 없음. spec-gate Round 1 스폰 노트에서 실측된 제약).

## SC ↔ Task 매핑 (자가 점검)

- SC-001 (벤치) → Task 5·6 산출물 + 저자 실행 (구현 범위는 산출물까지)
- SC-002 (체감) → Task 2 훅 (판정은 설치 2주 후 — 구현 범위 밖)
- SC-003 (확정 0 → 재검증 0) → Task 4 절차 4 + Task 3 테스트 1
- SC-004 (배너 전달) → Task 3 테스트 3 + Task 4 절차 4
- SC-005 (수정 내역 1:1) → Task 4 절차 5
- FR-019 (best-of-N 금지) → Task 4 "하지 말 것"

## 실행 방식 메모

- 커밋은 main 직행(레포 관례 — fableus 구현 16커밋 동일). 태스크당 1커밋.
- Task 1→2→3→4는 순차(경로·계약 의존), Task 5·6은 Task 1 이후 언제든 가능.
- 산출물 언어 = 한국어 (레포 관례).
