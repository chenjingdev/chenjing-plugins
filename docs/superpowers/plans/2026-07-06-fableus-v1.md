# fableus 하네스 v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fable 없이 Opus만으로 "알잘딱" 품질의 spec을 만드는 Claude Code 전용 플러그인 — `/spec`(인터뷰+작성) + `/spec-gate`(콜드 리드 실측 게이트).

**Architecture:** chenjing-plugins에 `fableus` 플러그인 신설. spec-kit의 specify/clarify 흐름과 템플릿을 흡수·한국어화하고(설치 없이 내장), 신규 `/spec-gate`는 도구 없는 커스텀 서브에이전트(cold-reader)에 spec 본문만 넘겨 blocking/discretionary 이슈를 실측한다. 게이트는 읽기 전용이며 gate-report.md에만 기록한다.

**Tech Stack:** Claude Code 플러그인 체계 (skills, agents, hooks.json), 마크다운 프롬프트. 실행 코드 없음(셸 원라이너 제외).

## Global Constraints

- Claude Code 전용 — 타 에이전트 호환 고려 금지 (설계 4.6)
- 특정 모델 의존 금지 — 리더 모델은 파라미터, 기본 `opus` (G-9)
- 게이트는 읽기 전용: `gate-report.md`에만 쓴다. spec frontmatter `gate:` 도장은 작성 세션 몫 (G-5)
- 콜드 리더는 완전 텍스트 격리: spec 본문만 프롬프트로 받고 도구 사용 금지 (G-2)
- 통과 기준: blocking 0건, 최대 3라운드 (G-4, G-8)
- 산출물 경로 관례: `specs/NNN-slug/spec.md` + `gate-report.md`
- spec-kit 코드 흡수 시 README에 MIT 출처 표기
- 모든 사용자 대면 텍스트는 한국어
- 설계 원본: `docs/superpowers/specs/2026-07-06-fableus-harness-design.md` (G-1~G-9 표가 계약)

**소스 재료 위치 (실행자가 읽어야 할 파일):**
- spec-kit 시운전 산출물: `~/dev/tmp-speckit-trial/.specify/templates/*.md`, `~/dev/tmp-speckit-trial/.claude/skills/speckit-specify/SKILL.md`, `speckit-clarify/SKILL.md`
- 게이트 spec(도그푸딩 대상): `~/dev/tmp-speckit-trial/specs/001-dryrun-gate/spec.md`
- story-spec L6 스키마(greenfield 템플릿 재료): `plugins/story-spec/skills/story-spec/schemas/spec.md`
- 콜드 리더 프롬프트의 검증된 원형: 설계 문서 §7의 실험 기록 참조 (2026-07-06 세션에서 Opus 콜드 리드로 blocking 4건 발견 실증)

---

### Task 1: 플러그인 뼈대

**Files:**
- Create: `plugins/fableus/.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json` (fableus 항목 추가)

**Interfaces:**
- Produces: 플러그인 ID `fableus` — 이후 모든 태스크가 이 디렉터리 아래에 파일을 추가

- [ ] **Step 1: plugin.json 작성**

```json
{
  "name": "fableus",
  "description": "Fable 없이 Opus만으로 알잘딱 spec을 만드는 하네스 — 인터뷰형 spec 작성(/spec) + 콜드 리드 실측 게이트(/spec-gate)",
  "version": "0.1.0"
}
```

- [ ] **Step 2: marketplace.json에 등록**

`.claude-plugin/marketplace.json`을 읽고, 기존 plugins 배열(resume, story-spec 항목이 있음)에 아래 항목을 같은 형식으로 추가:

```json
{ "name": "fableus", "source": "./plugins/fableus", "description": "Opus 전용 spec 하네스: /spec + /spec-gate" }
```

(기존 항목의 키 구성이 다르면 기존 형식을 따른다 — 기존 파일이 정본.)

- [ ] **Step 3: 검증**

Run: `cat plugins/fableus/.claude-plugin/plugin.json | python3 -m json.tool && python3 -m json.tool < .claude-plugin/marketplace.json`
Expected: 두 JSON 모두 파싱 성공

- [ ] **Step 4: Commit**

```bash
git add plugins/fableus .claude-plugin/marketplace.json
git commit -m "feat(fableus): 플러그인 뼈대"
```

---

### Task 2: spec 템플릿 4종

**Files:**
- Create: `plugins/fableus/skills/spec/references/spec-template.md` (기능 단위)
- Create: `plugins/fableus/skills/spec/references/spec-template-system.md` (greenfield 시스템 단위)
- Create: `plugins/fableus/skills/spec/references/plan-template.md`, `tasks-template.md` (참고용 복사)

**Interfaces:**
- Consumes: `~/dev/tmp-speckit-trial/.specify/templates/` 원본들, `plugins/story-spec/skills/story-spec/schemas/spec.md`의 4섹션 정의
- Produces: Task 5의 `/spec` 스킬이 이 템플릿 경로들을 참조

- [ ] **Step 1: 기능 단위 템플릿 — 원본 복사 후 보강**

```bash
cp ~/dev/tmp-speckit-trial/.specify/templates/spec-template.md plugins/fableus/skills/spec/references/spec-template.md
cp ~/dev/tmp-speckit-trial/.specify/templates/plan-template.md plugins/fableus/skills/spec/references/plan-template.md
cp ~/dev/tmp-speckit-trial/.specify/templates/tasks-template.md plugins/fableus/skills/spec/references/tasks-template.md
```

- [ ] **Step 2: spec-template.md에 두 섹션 추가**

`## Success Criteria` 섹션 뒤, `## Assumptions` 앞에 삽입:

```markdown
## Decision Ledger *(mandatory)*

<!-- 인터뷰 중 내린 모든 방향 결정을 기록한다. "근거"가 있어야 나중 세션이
     전제 신선도(그 근거가 아직 유효한가)를 검증할 수 있다. -->

| # | 결정 | 근거 (당시 사실) | 기각한 대안 |
|---|---|---|---|
| D-1 | [결정 내용] | [이 결정을 강제한 당시 조건/사실] | [고려했으나 버린 안 + 이유] |

## Deferred to Implementer *(waive 기록)*

<!-- 게이트가 제기했으나 작성자가 "구현자 재량"으로 명시 위임한 항목.
     여기 적힌 항목은 콜드 리더가 재제기하지 않는다 (G-7). -->

- [항목]: 구현자 재량. 사유: [왜 spec이 정하지 않아도 되는가]
```

그리고 frontmatter(제목 아래 메타 블록)에 한 줄 추가:

```markdown
**Gate**: not-run  <!-- not-run | round-N-blocked | passed | passed-with-waivers — 작성 세션만 갱신 -->
```

- [ ] **Step 3: greenfield 시스템 템플릿 작성**

`plugins/fableus/skills/spec/references/spec-template-system.md` 전체 내용:

```markdown
# System Specification: [SYSTEM NAME]

**Created**: [DATE]
**Status**: Draft
**Gate**: not-run

<!-- Symphony SPEC.md 스타일의 시스템 단위 구현 계약.
     원칙: "축"(상태·경계·계약·에러 분류)은 전부 박고, "값"(알고리즘 세부·
     필드명·포맷)은 implementation-defined로 위임한다. 의사코드 금지. -->

## 1. Purpose & Scope
[이 시스템이 존재하는 이유 한 문단. Out of scope 목록 필수.]

## 2. Domain Model
[핵심 엔티티와 관계. 엔티티별: 필드 목록 + 타입 + 제약 + 영속/휘발 여부.
 story-spec L6 Data model 작성 룰 준용: "todo는 텍스트와 시간을 갖는다"(нет) →
 "text: string, trim 후 1-200자"(да)]

## 3. State Model
[유한 상태 목록과 전이 조건. 모든 상태가 어떻게 진입하고 어떻게 떠나는지 빠짐없이.]

## 4. Event Flow
[이벤트 → 유효 상태 → 효과(순서 있는 목록) → 실패 시 동작. 번호 매김.]

## 5. Error Taxonomy
[타입화된 에러 목록 (예: missing_config, parse_error)과 각각의 처리 방침.]

## 6. Invariants (MUST) / Defaults (SHOULD) / Choices (MAY)
[RFC 2119 스타일. MAY 항목은 "implementation-defined:" 접두로 위임을 명시.]

## 7. Acceptance Criteria
[Given/When/Then, 외부 관찰 가능, 자동화 테스트로 옮길 수 있는 정밀도. 3개 이상.]

## Decision Ledger *(mandatory)*

| # | 결정 | 근거 (당시 사실) | 기각한 대안 |
|---|---|---|---|

## Deferred to Implementer *(waive 기록)*

## Assumptions
[명시되지 않아 합리적 기본값으로 채운 것들. 무단 가정 금지 — 전부 여기 선언.]
```

- [ ] **Step 4: 검증**

Run: `grep -l "Decision Ledger" plugins/fableus/skills/spec/references/*.md | wc -l`
Expected: `2` (spec-template.md, spec-template-system.md)

- [ ] **Step 5: Commit**

```bash
git add plugins/fableus/skills/spec/references
git commit -m "feat(fableus): spec 템플릿 4종 — Decision Ledger·waive 섹션·greenfield 변형"
```

---

### Task 3: cold-reader 에이전트 + 리더 프롬프트 + 리포트 템플릿

**Files:**
- Create: `plugins/fableus/agents/cold-reader.md`
- Create: `plugins/fableus/skills/spec-gate/references/reader-prompt.md`
- Create: `plugins/fableus/skills/spec-gate/references/gate-report-template.md`

**Interfaces:**
- Produces: 에이전트 타입 `fableus:cold-reader`(도구 없음), 리더 프롬프트의 출력 계약(아래 4분류×2축 마크다운) — Task 4의 스킬이 이 출력을 파싱해 리포트를 쓴다

- [ ] **Step 1: 에이전트 정의 작성** — `plugins/fableus/agents/cold-reader.md`:

```markdown
---
name: cold-reader
description: spec 문서를 대화 맥락·도구 없이 콜드 리드해 실행자 관점의 이슈를 열거하는 검사 전용 에이전트. /spec-gate가 스폰한다.
tools: []
model: opus
---

너는 spec 문서 하나만 받아 그것만으로 구현 가능성을 판정하는 실행 엔지니어다.
프롬프트에 포함된 문서 외의 어떤 맥락도 없으며, 도구를 사용할 수 없다.
받은 지시(reader-prompt)의 출력 형식을 정확히 따르라. 최종 텍스트가 그대로
기계 처리되므로 인사말·후기 없이 본문만 출력한다.
```

- [ ] **Step 2: 리더 프롬프트 작성** — `plugins/fableus/skills/spec-gate/references/reader-prompt.md`:

```markdown
<!-- /spec-gate가 이 프롬프트 + spec 본문을 합쳐 cold-reader에 전달한다.
     {{SPEC_BODY}}가 치환 지점. 2026-07-06 실측에서 blocking 4건을 발견한
     원형 프롬프트를 2축 분류로 정식화한 것. -->

너는 아래 spec 문서를 받아 혼자서 구현을 맡게 될 실행 엔지니어다. 이 문서
외에는 어떤 맥락도 없다. 문서만으로 판단하라.

각 이슈에 두 개의 라벨을 독립적으로 붙인다:
- **카테고리** (고치는 방법 안내): question(작성자에게 물어야 답이 나옴) /
  decision(문서가 안 정한 갈림길) / term(정의 없는 용어) / criteria(검증
  불가능한 수락 기준)
- **심각도** (통과 판정): blocking(구현 방향·범위가 갈려서 내 재량으로 정하면
  작성자 의도와 어긋날 위험이 큼) / discretionary(내가 재량으로 정해도 되는
  세부 — 네이밍, 내부 구조, 포맷, 마이너 UX)

spec 범위 밖 주제(배포 인프라 등)는 심각도 대신 `out-of-scope` 라벨.

## 출력 형식 (정확히 이 구조로)

### BLOCKING
각 항목:
- **[카테고리] 한 줄 제목**
  - 무엇이 모호한가: ...
  - 왜 재량으로 못 정하나: ...
  - 제안 (실행자 추측임):
    - A. ...
    - B. ...
    - C. ... (있으면)

### DISCRETIONARY
- **[카테고리] 한 줄 제목** — 나라면 이렇게 정한다: ...

### OUT-OF-SCOPE
- 항목과 한 줄 설명 (없으면 "없음")

### VERDICT
`구현 가능` 또는 `blocking N건 해소 필요` 한 줄 + 이 spec의 상세도가
과한 부분/부족한 부분 2-3줄.

주의: 문서의 "Deferred to Implementer" 섹션에 명시된 항목은 이미 재량으로
위임된 것이다 — 이슈로 제기하지 말라.

---

{{SPEC_BODY}}
```

- [ ] **Step 3: 리포트 템플릿 작성** — `plugins/fableus/skills/spec-gate/references/gate-report-template.md`:

```markdown
# Gate Report: [spec 제목]

> spec: [spec.md 상대 경로]
> 판정 기준: blocking 0건 = passed (G-4) · 최대 3라운드 (G-8)

## Round [N] — [ISO 시각] — reader: [모델명]

**판정: [passed | blocked (N blocking) | passed-with-waivers]**

| # | 심각도 | 카테고리 | 제목 | 상태 |
|---|---|---|---|---|
| R[N]-1 | blocking | decision | ... | open |

### 상세
[리더 출력의 BLOCKING/DISCRETIONARY/OUT-OF-SCOPE/VERDICT 원문 붙여넣기]
```

- [ ] **Step 4: 검증**

Run: `grep -c "{{SPEC_BODY}}" plugins/fableus/skills/spec-gate/references/reader-prompt.md`
Expected: `1`

- [ ] **Step 5: Commit**

```bash
git add plugins/fableus/agents plugins/fableus/skills/spec-gate/references
git commit -m "feat(fableus): cold-reader 에이전트 + 리더 프롬프트 + 리포트 템플릿"
```

---

### Task 4: /spec-gate 스킬

**Files:**
- Create: `plugins/fableus/skills/spec-gate/SKILL.md`

**Interfaces:**
- Consumes: Task 3의 `fableus:cold-reader` 에이전트, `references/reader-prompt.md`, `references/gate-report-template.md`
- Produces: `specs/NNN-slug/gate-report.md` 누적 기록, 사용자 환류 질문. spec frontmatter `**Gate**:` 갱신(작성 세션 권한으로)

- [ ] **Step 1: SKILL.md 작성** — 전체 내용:

```markdown
---
name: spec-gate
description: "spec 문서를 실행 모델이 콜드 리드해 '스스로 정할 수 없는 것(blocking)'을 실측하는 게이트. blocking 0건까지 인터뷰 환류. 사용: /spec-gate <spec.md 경로> [--reader <model>]"
---

# /spec-gate — 콜드 리드 실측 게이트

문서 품질을 작성자의 자가 점검이 아니라 **실행자의 콜드 리드**로 판정한다.
게이트 자체는 읽기 전용이다: spec.md 본문은 절대 수정하지 않는다 (G-5).

## 절차

### 0. 입력 검증
- 인자에서 spec 경로와 `--reader <model>`(기본 `opus`)을 파싱한다.
- env `CLAUDE_CODE_SUBAGENT_MODEL`이 설정돼 있으면 경고한다 — 이 env는
  per-call model 지정을 덮어써서 `--reader`를 무력화한다 (모델 우선순위:
  env > per-call > frontmatter > 세션).
- 파일이 없거나 비어 있으면: 실행을 거부하고 이유를 보고한다. 여기서 종료.
- 파일이 30,000단어를 넘으면: 거부하고 문서 축소를 권고한다. 여기서 종료.
- spec과 같은 디렉터리의 `gate-report.md`를 읽어 현재 라운드 번호를 정한다
  (없으면 Round 1). 직전 라운드가 Round 3이었고 blocking이 남아 있으면:
  "3라운드 상한 도달 — waive(스펙에 재량 위임 명시) 또는 보강 후 재시작"을
  안내하고 종료 (G-8).

### 1. 콜드 리드 (완전 격리, G-2)
- `references/reader-prompt.md`를 읽어 `{{SPEC_BODY}}`를 spec 본문 전체로
  치환한다.
- Agent tool로 `fableus:cold-reader` 타입 서브에이전트를 스폰한다:
  model = --reader 값, 프롬프트 = 치환된 리더 프롬프트 전문.
  파일 경로를 넘기지 말 것 — 본문을 프롬프트에 직접 포함한다(격리 보장).
- 라운드가 5분 이상 걸릴 수 있다. 백그라운드로 돌리고 완료 알림을 기다린다.
- 스폰 실패 시: 실패 사실만 보고하고 부분 결과를 기록하지 않는다. 종료.

### 2. 리포트 기록 (게이트의 유일한 쓰기)
- 리더 출력을 파싱해 `references/gate-report-template.md` 형식으로
  `gate-report.md`에 라운드 블록을 **추가**한다(기존 라운드 보존).
- 이슈 ID는 `R{라운드}-{순번}`.

### 3. 판정과 환류
- **blocking 0건** → 판정 `passed`. 사용자에게 보고하고, spec frontmatter의
  `**Gate**:` 라인을 `passed`(waive가 있으면 `passed-with-waivers`)로
  갱신한다. 이 갱신은 게이트가 아니라 작성 세션(현재 세션)의 쓰기다 (G-5).
- **blocking N건** → frontmatter를 `round-N-blocked`로 갱신하고, 각 blocking
  이슈를 AskUserQuestion으로 환류한다:
  - 질문 = 이슈의 "무엇이 모호한가", 선택지 = 리더의 제안 A/B/C
    (라벨에 "실행자 추측" 명시) + 자동 제공되는 직접 입력.
  - 사용자가 "구현자 재량으로 남긴다"고 답하면: spec의
    `## Deferred to Implementer` 섹션에 항목+사유를 추가한다 (G-7 — waive는
    spec 본문에 명시하는 행위다. 그래야 다음 라운드 콜드 리더가 재제기하지
    않는다).
  - 그 외 답변: 해당 내용을 spec 본문(관련 섹션)에 반영한다. 반영은 작성
    세션의 편집이며, 사용자 답변을 근거로만 한다 — 리더의 추측을 무단
    채택하지 않는다 (G-6).
- 반영이 끝나면 재실행을 제안한다(다음 라운드).

## 하지 말 것
- 게이트 판정을 건너뛰고 "대충 통과"를 선언하는 것 — blocking 판정은
  리더 출력이 정본이다.
- discretionary 이슈를 해소하려고 spec을 부풀리는 것 — 정보성 표시로 둔다
  (G-4). "값"은 구현자 몫이다.
- 리더에게 파일 경로·저장소 접근을 주는 것 (G-2).
```

- [ ] **Step 2: 검증 — 규칙 커버리지 체크**

Run: `grep -o "G-[0-9]" plugins/fableus/skills/spec-gate/SKILL.md | sort -u`
Expected: `G-2 G-4 G-5 G-6 G-7 G-8` 포함 (G-1·G-3은 리더 프롬프트가, G-9는 --reader 파라미터가 구현)

- [ ] **Step 3: Commit**

```bash
git add plugins/fableus/skills/spec-gate/SKILL.md
git commit -m "feat(fableus): /spec-gate 스킬 — 콜드 리드 게이트 오케스트레이션"
```

---

### Task 5: /spec 스킬 (specify + clarify 통합판)

**Files:**
- Create: `plugins/fableus/skills/spec/SKILL.md`

**Interfaces:**
- Consumes: Task 2의 템플릿 4종 (`references/` 경로)
- Produces: `specs/NNN-slug/spec.md` — Task 4의 게이트가 소비

- [ ] **Step 1: 원본 두 스킬 정독**

`~/dev/tmp-speckit-trial/.claude/skills/speckit-specify/SKILL.md`(348줄)과 `speckit-clarify/SKILL.md`(288줄)를 읽는다. 흡수할 핵심: 짧은 이름 생성 규칙, `specs/NNN-slug/` 순번 규칙, [NEEDS CLARIFICATION] 마커 룰(최대 3개, scope > security > UX > 세부 우선), 자가 검증 루프(최대 3회), 제안 답변 표 형식. 버릴 것: extensions.yml 훅 체크, checklist 파일 생성(게이트가 대체), 영어 전용 문구, feature.json/브랜치 연동.

- [ ] **Step 2: SKILL.md 작성** — 전체 내용:

```markdown
---
name: spec
description: "인터뷰로 아이디어를 구현 위임 가능한 spec으로 만든다. 신규 시스템이면 Symphony급 시스템 spec, 기존 코드베이스 변경이면 기능 spec. 사용: /spec <아이디어 설명>. 완성 후 /spec-gate로 실측 검증."
---

# /spec — 인터뷰형 spec 작성

목표: **실행 모델(Opus)이 이 문서만 읽고 구현할 수 있는 spec.** 품질의
최종 판정은 /spec-gate가 하므로, 여기서는 과잉 상세보다 "축"을 채우는 데
집중한다. 축 = 범위·상태·경계·계약·에러 분류. 값(네이밍·포맷·알고리즘
세부)은 구현자 재량으로 남긴다.

## 절차

### 1. 모드 판별
- 현재 디렉터리가 기존 코드베이스(소스 파일 존재)이고 요청이 그 변경이면
  → **기능 모드** (`references/spec-template.md`)
- 새 시스템/앱 아이디어면 → **시스템 모드** (`references/spec-template-system.md`)
- 애매하면 AskUserQuestion으로 한 번 묻는다.

### 2. 사전 조사 (인터뷰 전에)
- **기능 모드**: 관련 코드를 직접 조사한다 — 따라야 할 기존 패턴, 터치할
  파일, 제약을 찾아 `파일:라인` 인용으로 확보한다. 조사 결과는 spec 본문에
  박는다(구현 세션이 다시 뒤지지 않게).
- **전제 신선도 규칙**: 과거 결정·문서를 상속하기 전에 그 근거가 지금도
  유효한지 확인한다. 메모리(Honcho)와 최근 커밋을 대조하라.
- **시스템 모드**: 유사 도구·선행 사례를 아는 만큼 정리해 인터뷰 재료로
  쓴다 (조사보다 사용자의 의도가 정본).

### 3. 인터뷰 (superpowers 브레인스토밍 UX)
- **한 메시지 한 질문.** AskUserQuestion 선택지 2-4개 + 직접 입력.
- 순서: 목적/성공 기준 → 범위(명시적 out-of-scope) → 상태·데이터 축 →
  실패 모드 → 수락 기준.
- 정량화 규율: "빠르게"→"몇 ms?", "여러 개"→정확한 수. 모호한 형용사를
  spec에 옮기지 않는다.
- 내린 모든 방향 결정은 **Decision Ledger**에 근거·기각 대안과 함께 기록.
- 사용자가 답을 모르면: 합리적 기본값을 제안하고 채택 시 **Assumptions**에
  선언한다. 무단 가정 금지.
- 판단이 진짜 갈리는데 답을 못 얻은 지점만 본문에
  `[NEEDS CLARIFICATION: 구체적 질문]`으로 박는다. **최대 3개** —
  우선순위: 범위 > 보안 > UX > 기술 세부. 나머지는 기본값+Assumptions.

### 4. 작성과 자가 검증
- `specs/` 아래 다음 순번으로 `specs/NNN-slug/spec.md` 생성
  (NNN = 기존 디렉터리 스캔 후 다음 3자리, slug = 2-4단어 kebab-case).
- 템플릿 구조를 유지하며 작성. frontmatter `**Gate**: not-run`.
- 자가 검증(최대 3회 수정): 모호 형용사 잔존? 수락 기준이 관찰 가능?
  모든 결정이 Ledger에? 마커 3개 이하?
- 마커가 남아 있으면 각각을 AskUserQuestion(제안 답변 A/B/C + 직접 입력)
  으로 해소하고 본문에 반영한다.

### 5. 인계
- 완성 보고 후 **/spec-gate 실행을 제안**한다. 게이트 통과 전에는 구현
  단계로 넘어가지 않는다.
```

- [ ] **Step 3: 검증**

Run: `grep -c "NEEDS CLARIFICATION" plugins/fableus/skills/spec/SKILL.md`
Expected: `2` (룰 정의 1 + 마커 형식 1)

- [ ] **Step 4: Commit**

```bash
git add plugins/fableus/skills/spec/SKILL.md
git commit -m "feat(fableus): /spec 스킬 — specify+clarify 통합 인터뷰판"
```

---

### Task 6: SessionStart 훅 — 하네스 규칙 주입

**Files:**
- Create: `plugins/fableus/hooks/hooks.json`

**Interfaces:**
- Consumes: 없음 (독립)
- Produces: 모든 세션 시작 시 하네스 규칙 2줄이 additionalContext로 주입됨

- [ ] **Step 1: hooks.json 작성** (참고 선례: `plugins/resume/hooks/hooks.json`이 같은 체계 사용)

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"[fableus 하네스 규칙] 1. 전제 신선도: 과거 결정·문서를 상속하기 전에 그 근거가 지금도 유효한지 확인하라. 2. 가정 선언: 무단 가정 금지 — 가정했으면 산출물의 Assumptions에 기록하라.\"}}'"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 검증**

Run: `python3 -m json.tool < plugins/fableus/hooks/hooks.json > /dev/null && bash -c "$(python3 -c "import json;print(json.load(open('plugins/fableus/hooks/hooks.json'))['hooks']['SessionStart'][0]['hooks'][0]['command'])")" | python3 -m json.tool`
Expected: 훅 커맨드 실행 결과가 유효한 JSON (additionalContext 포함)

- [ ] **Step 3: Commit**

```bash
git add plugins/fableus/hooks
git commit -m "feat(fableus): SessionStart 훅 — 전제 신선도·가정 선언 규칙 주입"
```

---

### Task 7: 도그푸딩 + 운영 문서(README)

**Files:**
- Create: `plugins/fableus/specs/001-spec-gate/spec.md` (시운전 spec 이관)
- Create: `plugins/fableus/specs/001-spec-gate/gate-report.md` (도그푸딩 산출)
- Create: `plugins/fableus/README.md`

**Interfaces:**
- Consumes: Task 1–6 전부 (플러그인이 로드 가능한 상태여야 함)

- [ ] **Step 1: 시운전 spec 이관**

```bash
mkdir -p plugins/fableus/specs/001-spec-gate
cp ~/dev/tmp-speckit-trial/specs/001-dryrun-gate/spec.md plugins/fableus/specs/001-spec-gate/spec.md
```

이관 후 spec.md에 Task 2의 신규 섹션 형식을 수동 반영: frontmatter에 `**Gate**: not-run` 추가, `## Decision Ledger` 섹션 추가(이미 본문에 "사용자 결정: ..." 인라인 기록이 있으니 그것을 표로 옮긴다 — Q1~Q4, B-1~B-4에 해당하는 결정 8건), `## Deferred to Implementer` 빈 섹션 추가.

- [ ] **Step 2: 플러그인 설치 확인**

플러그인이 marketplace를 통해 로드되는지 확인한다 (chenjing-plugins 사용 방식대로 — 새 세션에서 `/spec-gate` 스킬이 목록에 보이는지). 로드 실패 시 plugin.json/디렉터리 구조를 수정하고 재확인.

- [ ] **Step 3: 도그푸딩 — 게이트 라운드 2 완주**

새 세션(또는 현재 세션)에서:

```
/spec-gate plugins/fableus/specs/001-spec-gate/spec.md
```

기대 동작 체크리스트:
- cold-reader가 스폰되고 (background, 도구 0회 사용)
- gate-report.md가 생성되며 Round 표기가 붙고
- blocking 이슈가 있으면 AskUserQuestion 환류가 뜨고, waive 선택 시 spec의 Deferred to Implementer에 항목이 추가되고
- blocking 0이 되면 frontmatter가 `passed`로 갱신된다

실패하는 항목은 해당 태스크 파일을 수정하고 재실행. **이 체크리스트 전부가 그린이 될 때까지가 이 태스크의 완료 조건.**

- [ ] **Step 4: README(운영 문서) 작성** — `plugins/fableus/README.md`:

```markdown
# fableus

Fable 없이 Opus만으로 "알잘딱" spec을 만드는 Claude Code 하네스.
원리: **모델이 알아서 복원해야 했던 것을 구조가 실어 나르게 한다.**

## 파이프라인

아이디어 → `/spec` (인터뷰·작성) → `/spec-gate` (콜드 리드 실측, blocking 0까지)
→ 구현 세션에 인계 (superpowers writing-plans 권장) → 코드

규칙: **게이트 통과(`Gate: passed`) 전에는 구현을 시작하지 않는다.**

## 스킬

- `/spec <아이디어>` — 모드 자동 판별(기능/시스템), 한 질문 인터뷰,
  Decision Ledger 기록, `specs/NNN-slug/spec.md` 생성
- `/spec-gate <spec 경로> [--reader <model>]` — 도구 없는 cold-reader
  서브에이전트가 문서만 읽고 blocking/discretionary 판정,
  `gate-report.md` 기록, blocking은 선택지 질문으로 환류

## 설계 문서

`docs/superpowers/specs/2026-07-06-fableus-harness-design.md` (G-1~G-9 계약 포함)

## 출처

spec 템플릿과 specify/clarify 흐름은 [github/spec-kit](https://github.com/github/spec-kit)
(MIT)에서 흡수해 Claude Code 전용으로 개작했다.
```

- [ ] **Step 5: 최종 커밋**

```bash
git add plugins/fableus
git commit -m "feat(fableus): 도그푸딩 spec 이관 + 게이트 완주 + README"
```

---

## Self-Review 기록

- **Spec coverage**: G-1(대상)·G-3(2축)→리더 프롬프트(T3), G-2(격리)→cold-reader tools:[] + 본문 직접 포함(T3·T4), G-4·G-8→스킬 절차 0·3(T4), G-5→"게이트의 유일한 쓰기" 구획(T4), G-6→환류 규칙(T4), G-7→Deferred to Implementer 섹션(T2)+재제기 금지(리더 프롬프트 말미), G-9→--reader 파라미터(T4). 템플릿 보강(§4.4)→T2. 하네스 규칙(§4.5)→T6. Claude 전용 프리미티브(§4.6 v1 항목)→T3(에이전트)·T4(백그라운드)·T6(훅). 도그푸딩(§5)→T7. 누락 없음. v2 항목(PreToolUse 가드)은 의도적 범위 밖.
- **Placeholder scan**: 통과 — 모든 파일 내용이 본문에 포함되거나 정확한 소스 경로+변형 지시로 명세됨.
- **일관성**: 경로(`plugins/fableus/...`), 에이전트 타입(`fableus:cold-reader`), frontmatter 라벨(`not-run|round-N-blocked|passed|passed-with-waivers`)이 태스크 간 동일함을 확인.
