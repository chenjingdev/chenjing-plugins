---
phase: 03-so-what-chain
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - plugins/resume/.claude/agents/c-level.md
  - plugins/resume/skills/resume-panel/SKILL.md
autonomous: true
requirements: [IMPACT-01, IMPACT-02, IMPACT-03]
must_haves:
  truths:
    - "SKILL.md에 [resume-panel:SO-WHAT] 메시지 처리 규칙이 존재하고, C-Level을 So What 모드로 호출하는 절차가 명시되어 있다"
    - "C-Level 에이전트가 So What 체인 모드에서 3단계(직접 결과 -> 팀/조직 영향 -> 비즈니스 지표) 질문을 생성하는 프롬프트 섹션이 존재한다"
    - "매 레벨마다 '거기까지였음' 선택지가 포함되고, 선택 시 체인이 종료되어 누적 결과가 result에 저장된다"
    - "so_what_active 플래그가 meta.json에 설정/해제되는 흐름이 SKILL.md에 명시되어 있다"
    - "SO-WHAT과 프로파일러 메시지가 동시 도착 시 SO-WHAT이 우선 처리된다"
  artifacts:
    - path: "plugins/resume/.claude/agents/c-level.md"
      provides: "So What 체인 모드 섹션 (레벨별 질문 생성 규칙)"
      contains: "So What 체인 모드"
    - path: "plugins/resume/skills/resume-panel/SKILL.md"
      provides: "[resume-panel:SO-WHAT] 처리 규칙 + so_what_active 상태 관리"
      contains: "[resume-panel:SO-WHAT]"
  key_links:
    - from: "plugins/resume/skills/resume-panel/SKILL.md"
      to: "plugins/resume/.claude/agents/c-level.md"
      via: "Agent tool call with So What mode context"
      pattern: "So What 체인 모드"
    - from: "plugins/resume/skills/resume-panel/SKILL.md"
      to: ".resume-panel/meta.json"
      via: "so_what_active flag read/write"
      pattern: "so_what_active"
    - from: "plugins/resume/scripts/episode-watcher.mjs"
      to: "plugins/resume/skills/resume-panel/SKILL.md"
      via: "[resume-panel:SO-WHAT] additionalContext message"
      pattern: "resume-panel:SO-WHAT"
---

<objective>
C-Level 에이전트에 So What 체인 모드 프롬프트 섹션을 추가하고, SKILL.md에 `[resume-panel:SO-WHAT]` 메시지 처리 규칙과 체인 상태 관리 로직을 추가한다.

Purpose: Plan 01의 트리거 감지 이후, 실제 3단계 심화 질문 생성과 오케스트레이터 체인 관리를 완성하여 So What 체인 전체 기능을 동작시킨다.
Output: c-level.md에 So What 체인 모드 섹션, SKILL.md에 SO-WHAT 메시지 처리 + 체인 상태 관리 규칙.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-so-what-chain/03-CONTEXT.md
@.planning/phases/03-so-what-chain/03-RESEARCH.md
@.planning/phases/03-so-what-chain/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 output: SO-WHAT message from episode-watcher.mjs -->
From plugins/resume/scripts/episode-watcher.mjs (after Plan 01):
```javascript
// SO-WHAT message format (locked decision):
// "[resume-panel:SO-WHAT] 에피소드 \"{title}\" 임팩트 부족"
// Sent via additionalContext when:
//   1. New episode added with result lacking number+unit pattern
//   2. so_what_active.active is NOT true in meta.json
// Only one SO-WHAT message per hook call (first impact-insufficient episode)
```

From plugins/resume/.claude/agents/c-level.md (current structure):
```markdown
# Sections:
## 역할 — line 8
## 입력 — line 14 (오케스트레이터가 전달하는 컨텍스트 목록)
## 질문 생성 규칙 — line 22
  ### 반드시 지킬 것 — line 24
  ### 절대 하지 말 것 — line 35
  ### 질문 패턴 — line 39
## 산출 형식 — line 100
## 금지사항 — line 110
```

From plugins/resume/skills/resume-panel/SKILL.md (current structure):
```markdown
# Sections:
## 핵심 원칙 — line 13
## 에이전트 구성 — line 49
## 오케스트레이터 동작 — line 69
  ### 에이전트 호출 방법 — line 73
  ### AskUserQuestion 변환 규칙 — line 99
  ### AskUserQuestion 폴백 — line 139
  ### 에이전트 선택 기준 — line 158
  ### 유저 응답 처리 — line 167
## 라운드 진행 — line 176
## 자율 오케스트레이션 — Hook 메시지 처리 — line 389
  ### 메시지 처리 규칙 — line 393 (currently items 1-4: profiler, HIGH, MEDIUM, LOW)
  ### 인터뷰 흐름 보호 — line 441
## 저장 — line 447
```

meta.json current structure:
```json
{
  "profiler_score": 0,
  "last_profiler_call": null,
  "last_profiler_episode_count": 0,
  "current_company": null,
  "total_profiler_calls": 0
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add So What chain mode section to c-level.md</name>
  <files>plugins/resume/.claude/agents/c-level.md</files>
  <read_first>
    - plugins/resume/.claude/agents/c-level.md
    - .planning/phases/03-so-what-chain/03-CONTEXT.md
    - .planning/phases/03-so-what-chain/03-RESEARCH.md
    - .claude/projects/-Users-laonpeople-dev-chenjing-plugins/memory/MEMORY.md
  </read_first>
  <action>
c-level.md의 `## 금지사항` 섹션(line 110) 바로 앞에 새 섹션 `## So What 체인 모드`를 추가한다. 기존 `## 산출 형식` 뒤, `## 금지사항` 앞에 위치한다.

추가할 정확한 내용:

```markdown
## So What 체인 모드

오케스트레이터가 So What 체인 모드로 호출하면, 에피소드의 임팩트를 단계별로 심화하는 질문을 생성한다.

### 입력

오케스트레이터가 다음을 전달한다:
- So What 체인 레벨 (1, 2, 또는 3)
- 대상 에피소드 (title, situation, task, action, result)
- 이전 레벨 답변 (레벨 2/3일 때)
- 리서처 조사 결과 (해당 회사)

### 레벨별 질문 생성

**Level 1: 직접 결과**
에피소드의 action을 참조하여 "그래서 뭐가 바뀌었어?" 수준의 질문을 생성한다.
- 에피소드의 구체적 action 내용을 질문에 반드시 포함 (추상적 질문 금지)
- 선택지는 해당 도메인에서 나올 수 있는 구체적 결과 유형으로 구성
- 예: "{action} 했다고 했는데, 이거 하고 나서 바로 뭐가 달라졌어?"

**Level 2: 팀/조직 영향**
Level 1 답변을 참조하여 팀/조직 스케일로 확장하는 질문을 생성한다.
- Level 1에서 유저가 말한 결과를 질문에 반드시 포함
- 팀 규모, 프로세스 변화, 다른 팀 영향 등으로 선택지 구성
- 예: "{Level 1 답변}이라고 했는데, 팀 전체로 보면 어떤 차이가 있었어?"

**Level 3: 비즈니스 지표**
Level 2 답변을 참조하여 비즈니스 임팩트 수치를 묻는 질문을 생성한다.
- 가능한 비즈니스 지표 유형을 선택지로 제시 (매출, 비용, 전환율, 시간 절감 등)
- "측정은 안 했지만 체감은 있었다" 수준의 선택지도 포함
- 예: "결국 매출/비용/전환율 같은 비즈니스 숫자로 찍히는 거 있어?"

### 산출 형식

모든 레벨에서 동일한 형식:
```
[C-Level] {에피소드 컨텍스트를 참조한 질문}
  1) {구체적 결과/영향 선택지}
  2) {구체적 결과/영향 선택지}
  3) 거기까지였음
```

선택지는 최대 2개 구체적 선택지 + "거기까지였음"으로 구성한다 (AskUserQuestion이 "Other"를 자동 추가하므로 총 4개 = 제한 내).

### 핵심 규칙

- 각 레벨 질문에 에피소드의 구체적 내용(action, 이전 답변)을 반드시 포함한다 — 추상적 "더 자세히" 금지
- "거기까지였음"은 항상 마지막 선택지다
- 질문에 에피소드의 구체적 장면(situation, 기술명, 프로젝트명 등)을 넣어 유저의 기억을 돕는다
```

기존 `## 금지사항` 섹션에도 So What 관련 금지 항목 2개를 추가한다 (기존 3개 항목 뒤에):

```markdown
- 에피소드 action에 없는 내용을 추측하여 질문하지 않는다
- So What 체인에서 "더 자세히 말해줘" 같은 열린 질문을 쓰지 않는다 — 항상 구체적 방향을 선택지로 제시한다
```
  </action>
  <verify>
    <automated>cd /Users/laonpeople/dev/chenjing-plugins && grep -c "So What 체인 모드" plugins/resume/.claude/agents/c-level.md && grep -c "거기까지였음" plugins/resume/.claude/agents/c-level.md && grep -c "Level 1" plugins/resume/.claude/agents/c-level.md</automated>
  </verify>
  <acceptance_criteria>
    - c-level.md에 `## So What 체인 모드` 섹션 존재
    - c-level.md에 `### 입력` 하위 섹션 존재 (So What 모드 내)
    - c-level.md에 `### 레벨별 질문 생성` 하위 섹션 존재
    - c-level.md에 `**Level 1: 직접 결과**`, `**Level 2: 팀/조직 영향**`, `**Level 3: 비즈니스 지표**` 3개 레벨 모두 존재
    - c-level.md에 `거기까지였음` 문자열이 최소 2회 등장 (산출 형식 + 핵심 규칙)
    - c-level.md에 `### 산출 형식` 하위 섹션이 So What 모드 내에 존재 (기존 산출 형식과 별도)
    - c-level.md에 `### 핵심 규칙` 하위 섹션 존재
    - c-level.md `## 금지사항` 섹션에 "에피소드 action에 없는 내용을 추측하여 질문하지 않는다" 항목 존재
    - c-level.md `## 금지사항` 섹션에 "So What 체인에서" 포함 금지 항목 존재
    - 기존 c-level.md의 `## 역할`, `## 입력`, `## 질문 생성 규칙`, `## 산출 형식`, `## 금지사항` 섹션이 모두 유지됨
  </acceptance_criteria>
  <done>C-Level 에이전트에 So What 체인 모드 섹션이 추가됨. 3단계(직접 결과/팀 영향/비즈니스 지표) 질문 생성 규칙과 산출 형식이 명시됨. 매 레벨에 "거기까지였음" 선택지 포함.</done>
</task>

<task type="auto">
  <name>Task 2: Add SO-WHAT message handling and chain state management to SKILL.md</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md
    - plugins/resume/.claude/agents/c-level.md
    - .planning/phases/03-so-what-chain/03-CONTEXT.md
    - .planning/phases/03-so-what-chain/03-RESEARCH.md
  </read_first>
  <action>
SKILL.md의 "자율 오케스트레이션 -- Hook 메시지 처리" 섹션 > "메시지 처리 규칙" 하위에 항목 5를 추가한다. 기존 항목 4 (`LOW`) 뒤, `### 인터뷰 흐름 보호` 앞에 위치한다.

추가할 항목 5의 정확한 내용:

```markdown
5. **`[resume-panel:SO-WHAT]`** -> 현재 질문-답변 사이클 완료 후 So What 체인 시작
   - meta.json에 `so_what_active` 설정:
     ```json
     {
       "so_what_active": {
         "active": true,
         "episode_title": "{메시지에서 추출한 에피소드 제목}",
         "current_level": 1,
         "accumulated_result": "{해당 에피소드의 기존 result 텍스트}"
       }
     }
     ```
   - C-Level을 So What 체인 모드로 호출 (level=1, 대상 에피소드 데이터 전달):
     ```
     Agent(
       prompt: "So What 체인 모드. Level 1. 대상 에피소드: {title, situation, task, action, result}. 리서처 조사: {해당 회사}."
     )
     ```
   - C-Level 리턴을 AskUserQuestion으로 변환 (기존 변환 규칙 동일 적용)
   - 유저 응답 처리:
     - **"거기까지였음" 선택**: accumulated_result를 에피소드 result에 저장, so_what_active를 null로, 인터뷰 복귀
     - **실질적 답변**: accumulated_result에 추가, current_level 증가
       - level < 3: C-Level 재호출 (다음 레벨, 이전 답변 포함)
       - level = 3: C-Level에게 최종 결과 통합 요청 (accumulated_result + Level 3 답변을 하나의 coherent result로 합성), 합성된 결과를 에피소드 result에 저장, so_what_active를 null로, 인터뷰 복귀
   - **결과 저장**: resume-source.json의 해당 에피소드 result 필드를 직접 업데이트 (Bash tool로 전체 JSON 재저장)
   - **원본 보존**: accumulated_result의 초기값은 기존 result 텍스트. 심화 답변은 기존 result에 추가/통합되며, 기존 내용을 덮어쓰지 않는다
```

그 다음, `### 인터뷰 흐름 보호` 섹션에 So What 관련 규칙을 추가한다. 기존 3개 bullet 뒤에:

```markdown
- SO-WHAT 체인은 multi-turn이므로, 체인 완료까지 일반 인터뷰 플로우를 일시 중단한다. 체인 완료(거기까지였음 또는 Level 3 완료) 후 인터뷰를 재개한다
- SO-WHAT과 프로파일러 메시지가 동시 도착하면 SO-WHAT을 먼저 처리한다 (체인 완료 후 프로파일러 백그라운드 실행)
- so_what_active가 active인 동안 추가 SO-WHAT 메시지는 무시한다 (hook에서 이미 필터링하지만, 오케스트레이터에서도 이중 확인)
```

또한, `### 에이전트 선택 기준` 섹션(line 158-165)에 So What 관련 항목을 추가한다. 기존 `- 라운드 3 -> 커피챗봇` 뒤에:

```markdown
- So What 체인 모드 활성화 -> C-Level (자동, 에이전트 선택 불필요)
```
  </action>
  <verify>
    <automated>cd /Users/laonpeople/dev/chenjing-plugins && grep -c "\[resume-panel:SO-WHAT\]" plugins/resume/skills/resume-panel/SKILL.md && grep -c "so_what_active" plugins/resume/skills/resume-panel/SKILL.md && grep -c "거기까지였음" plugins/resume/skills/resume-panel/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md에 `[resume-panel:SO-WHAT]` 문자열이 최소 2회 등장 (항목 5 제목 + 흐름 보호 규칙)
    - SKILL.md에 `so_what_active` 문자열이 최소 4회 등장 (JSON 예시, 설정/해제 규칙, 흐름 보호)
    - SKILL.md에 `거기까지였음` 문자열이 최소 2회 등장 (응답 처리 규칙)
    - SKILL.md에 `accumulated_result` 문자열이 최소 3회 등장 (설정, 추가, 저장)
    - SKILL.md 메시지 처리 규칙에 항목 `5.` 존재 (SO-WHAT)
    - SKILL.md 인터뷰 흐름 보호 섹션에 "SO-WHAT 체인은 multi-turn" 규칙 존재
    - SKILL.md 인터뷰 흐름 보호 섹션에 "SO-WHAT과 프로파일러 메시지가 동시 도착" 우선순위 규칙 존재
    - SKILL.md 에이전트 선택 기준에 "So What 체인 모드 활성화" 항목 존재
    - SKILL.md에 "원본 보존" 또는 "기존 result" 관련 규칙 존재 (result 덮어쓰기 방지)
    - 기존 SKILL.md 항목 1-4 (profiler, HIGH, MEDIUM, LOW) 모두 유지됨
    - 기존 SKILL.md의 AskUserQuestion 변환 규칙, 폴백, 라운드 진행 섹션 모두 유지됨
  </acceptance_criteria>
  <done>SKILL.md에 [resume-panel:SO-WHAT] 처리 규칙이 추가됨. so_what_active 상태 관리, C-Level 체인 호출, 유저 응답 분기(거기까지였음/실질 답변/Level 3 완료), result 저장, 인터뷰 흐름 보호 규칙이 모두 명시됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook additionalContext -> orchestrator | 내부 파이프라인. SO-WHAT 메시지를 오케스트레이터가 파싱하여 처리 |
| orchestrator -> c-level agent | Agent tool 호출. 에피소드 데이터를 컨텍스트로 전달 |
| orchestrator -> resume-source.json | result 필드 업데이트. 유저 본인 데이터 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | D (Denial of Service) | SO-WHAT infinite chain | mitigate | Level 3 하드 리밋 + "거기까지였음" 매 레벨 탈출 옵션. SKILL.md에서 level > 3이면 강제 종료 |
| T-03-05 | T (Tampering) | result field corruption | mitigate | accumulated_result에 기존 result 포함하여 원본 보존. 기존 내용 덮어쓰기 금지 규칙 SKILL.md에 명시 |
| T-03-06 | D (Denial of Service) | concurrent SO-WHAT + profiler | mitigate | SKILL.md에 우선순위 명시: SO-WHAT 먼저, 프로파일러 백그라운드. hook에서도 so_what_active 확인 |
</threat_model>

<verification>
1. `grep -c "So What 체인 모드" plugins/resume/.claude/agents/c-level.md` -- 1 이상
2. `grep -c "\[resume-panel:SO-WHAT\]" plugins/resume/skills/resume-panel/SKILL.md` -- 2 이상
3. `grep -c "so_what_active" plugins/resume/skills/resume-panel/SKILL.md` -- 4 이상
4. `grep -c "거기까지였음" plugins/resume/.claude/agents/c-level.md` -- 2 이상
5. c-level.md에 Level 1/2/3 모두 존재
6. SKILL.md에 기존 항목 1-4 + 신규 항목 5 모두 존재
</verification>

<success_criteria>
- C-Level 에이전트가 So What 체인 모드에서 3단계 질문 생성 가능 (프롬프트 섹션 존재)
- SKILL.md 오케스트레이터가 SO-WHAT 메시지 수신 시 체인 플로우를 관리 가능 (처리 규칙 존재)
- 매 레벨에 "거기까지였음" 탈출 옵션 포함
- so_what_active 상태 관리로 재트리거 방지 + 체인 진행 추적
- result 필드 원본 보존 + 심화 답변 통합 규칙 명시
- 기존 SKILL.md/c-level.md 기능 영향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-so-what-chain/03-02-SUMMARY.md`
</output>
