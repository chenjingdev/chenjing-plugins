---
phase: 03-so-what-chain
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/resume/scripts/episode-watcher.mjs
  - plugins/resume/scripts/test-episode-watcher.mjs
autonomous: true
requirements: [IMPACT-01, IMPACT-03]
must_haves:
  truths:
    - "에피소드 저장 시 result 필드에 수치+단위 패턴이 없으면 SO-WHAT 메시지가 발송된다"
    - "result 필드에 '매출 30% 증가' 같은 수치+단위 패턴이 있으면 SO-WHAT 메시지가 발송되지 않는다"
    - "so_what_active.active === true일 때 SO-WHAT 메시지가 발송되지 않는다 (재트리거 방지)"
    - "SO-WHAT 메시지 형식이 [resume-panel:SO-WHAT] 에피소드 \"{title}\" 임팩트 부족이다"
  artifacts:
    - path: "plugins/resume/scripts/episode-watcher.mjs"
      provides: "hasQuantifiedImpact() function + SO-WHAT trigger logic"
      contains: "hasQuantifiedImpact"
    - path: "plugins/resume/scripts/test-episode-watcher.mjs"
      provides: "So What detection unit tests"
      contains: "hasQuantifiedImpact"
  key_links:
    - from: "plugins/resume/scripts/episode-watcher.mjs"
      to: ".resume-panel/meta.json"
      via: "so_what_active flag check"
      pattern: "so_what_active"
---

<objective>
episode-watcher.mjs에 So What 체인 트리거 감지 로직을 TDD로 구현한다. `hasQuantifiedImpact()` 함수로 에피소드 result 필드의 임팩트 충분성을 정규식으로 판단하고, 부족하면 `[resume-panel:SO-WHAT]` 메시지를 발송한다.

Purpose: IMPACT-01 (자동 트리거)와 IMPACT-03 (충분한 에피소드 스킵)의 핵심 감지 로직을 결정론적으로 구현.
Output: episode-watcher.mjs에 hasQuantifiedImpact() + SO-WHAT 트리거 로직, test-episode-watcher.mjs에 6+ 신규 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-so-what-chain/03-CONTEXT.md
@.planning/phases/03-so-what-chain/03-RESEARCH.md
@.planning/phases/02-profiler-frequency-enhancement/02-01-SUMMARY.md

<interfaces>
<!-- episode-watcher.mjs current structure (relevant sections) -->

From plugins/resume/scripts/episode-watcher.mjs:
```javascript
// Helper functions already present:
function readJSON(path) { ... }           // line 61-67
function countEpisodes(source) { ... }     // line 69-75 — iterates source.projects[]
function countStarGaps(source) { ... }     // line 91-102
function detectMinimization(source, snapshot) { ... } // line 104-122

// Key data flow:
// source.projects[].episodes[] — flat project list (NOT companies[].projects[])
// messages[] — collected and joined with "\n\n" for additionalContext output
// metaPath — .resume-panel/meta.json (profiler_score stored here)
// snapshotPath — .resume-panel/snapshot.json (episode_count, project_names, etc.)

// Episode structure (from test data):
// { star: { situation, task, action, result }, title?: string }
// NOTE: episodes in test data have `star.result`, but SKILL.md schema has top-level `result`
// The hook iterates source.projects[].episodes[] — match this pattern

// SO-WHAT trigger location: AFTER the scoring block (line 206), BEFORE snapshot update (line 209)
// At this point, `source`, `snapshot`, `metaPath`, `messages` are all in scope
```

From plugins/resume/scripts/test-episode-watcher.mjs:
```javascript
// Test infrastructure:
function setupTestDir(snapshot, resumeSource, meta) { ... } // sets up /tmp/test-resume-panel-delta
function runWithBase(input) { ... }                          // runs hook with RESUME_PANEL_BASE
function readMeta() { ... }                                  // reads meta.json from test dir

// Test data uses: source.projects[].episodes[].star.result (star-wrapped)
// All existing tests use bashResumeInput for triggering
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for hasQuantifiedImpact and SO-WHAT trigger</name>
  <files>plugins/resume/scripts/test-episode-watcher.mjs</files>
  <read_first>
    - plugins/resume/scripts/test-episode-watcher.mjs
    - plugins/resume/scripts/episode-watcher.mjs
    - .planning/phases/03-so-what-chain/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test hasQuantifiedImpact-1: hasQuantifiedImpact("매출 30% 증가") returns true (% 단위)
    - Test hasQuantifiedImpact-2: hasQuantifiedImpact("응답 시간 200ms로 개선") returns true (ms 단위)
    - Test hasQuantifiedImpact-3: hasQuantifiedImpact("팀 생산성 3배 향상") returns true (배 단위)
    - Test hasQuantifiedImpact-4: hasQuantifiedImpact("업무 효율이 좋아졌다") returns false (수치 없음)
    - Test hasQuantifiedImpact-5: hasQuantifiedImpact("") returns false (빈 문자열)
    - Test hasQuantifiedImpact-6: hasQuantifiedImpact(null) returns false (null)
    - Test SO-WHAT-trigger: 에피소드 저장 시 star.result가 "개선했다" (수치 없음)이면 additionalContext에 `[resume-panel:SO-WHAT]` 포함
    - Test SO-WHAT-skip: 에피소드 저장 시 star.result가 "매출 30% 증가"이면 SO-WHAT 메시지 미발송
    - Test SO-WHAT-suppression: meta.json에 so_what_active.active === true이면 result가 수치 없어도 SO-WHAT 미발송
    - Test SO-WHAT-title: SO-WHAT 메시지에 에피소드 title이 포함됨
  </behavior>
  <action>
test-episode-watcher.mjs 파일 끝(기존 `console.log("\nAll findings routing tests passed.");` 이후)에 So What 테스트 섹션을 추가한다.

테스트 구조:

1. **hasQuantifiedImpact 직접 테스트**: episode-watcher.mjs에서 아직 export되지 않으므로, 테스트에서 함수를 직접 정의하여 로직을 검증한다 (정규식 `/\d+(\.\d+)?\s*(명|건|%|원|만|억|배|시간|분|초|ms|개월|일|주|달|회|번|개|위|등|배|톤|km|kg|L|대|편|권|통|점|곳|팀)/` 기반). 함수 구현은 Task 2에서 episode-watcher.mjs에 넣지만, 테스트에서는 예상되는 정규식 로직을 복제하여 단위 테스트한다.

   - 주의: episode-watcher.mjs는 ESM standalone script (stdin으로 JSON 받아 stdout으로 출력)이므로 import 불가. 기존 테스트 패턴대로 child_process로 실행하여 출력을 검증하는 통합 테스트 방식을 사용한다.

2. **SO-WHAT 트리거 통합 테스트** (기존 setupTestDir/runWithBase 인프라 활용):

   a. **SO-WHAT 트리거 테스트**: snapshot episode_count=2, resume-source에 episode 3개 (3번째의 star.result가 "개선했다"), meta에 profiler_score=0, so_what_active 없음. 실행 후 additionalContext에 `[resume-panel:SO-WHAT]` 포함 확인.

   b. **SO-WHAT 스킵 테스트**: 동일 구조이나 3번째 episode의 star.result가 "매출 30% 증가". 실행 후 SO-WHAT 메시지 미포함 확인 (프로파일러 메시지만 있거나 출력 없음).

   c. **SO-WHAT 억제 테스트**: meta.json에 `{"profiler_score": 0, "so_what_active": {"active": true, "episode_title": "진행중", "current_level": 2, "accumulated_result": "..."}}` 설정. star.result가 수치 없는 episode 추가. SO-WHAT 메시지 미발송 확인.

   d. **SO-WHAT 타이틀 포함 테스트**: episode에 title 필드 추가 (`title: "검색 성능 최적화"`), star.result가 수치 없음. SO-WHAT 메시지에 "검색 성능 최적화" 포함 확인.

   e. **SO-WHAT과 프로파일러 동시 발송 테스트**: profiler_score=4인 상태에서 수치 없는 episode 추가 (score 4+1=5, threshold 도달). additionalContext에 `[resume-panel]` (프로파일러) AND `[resume-panel:SO-WHAT]` 둘 다 포함 확인.

   f. **title 없는 에피소드**: title 필드가 없는 episode에서 SO-WHAT 트리거 시 "(제목 없음)" 포함 확인.

기존 테스트 output 메시지 형식을 따른다: `console.log("PASS: {test description}");`

섹션 헤더: `// ── So What chain tests ──────────────────────────────`
섹션 끝: `console.log("\nAll So What chain tests passed.");`

모든 테스트가 FAIL하는 것을 `node plugins/resume/scripts/test-episode-watcher.mjs` 실행으로 확인한다 (기존 테스트는 PASS, 신규 테스트만 FAIL).
  </action>
  <verify>
    <automated>cd /Users/laonpeople/dev/chenjing-plugins && node plugins/resume/scripts/test-episode-watcher.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test-episode-watcher.mjs 파일에 `// ── So What chain tests` 섹션이 존재
    - `So What chain tests` 섹션에 최소 6개의 테스트 존재 (assert 호출 포함)
    - `[resume-panel:SO-WHAT]` 문자열이 테스트 파일에 최소 4회 등장
    - `hasQuantifiedImpact` 문자열이 테스트 파일에 등장
    - 기존 22개 테스트는 모두 PASS (All scoring system tests passed + All findings routing tests passed 출력)
    - 신규 So What 테스트는 FAIL (SO-WHAT 로직이 아직 없으므로)
  </acceptance_criteria>
  <done>So What 관련 신규 테스트 6+개가 test-episode-watcher.mjs에 추가됨. 기존 22개 테스트는 PASS, 신규 테스트는 FAIL (RED 상태).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement hasQuantifiedImpact() and SO-WHAT trigger in episode-watcher.mjs</name>
  <files>plugins/resume/scripts/episode-watcher.mjs</files>
  <read_first>
    - plugins/resume/scripts/episode-watcher.mjs
    - plugins/resume/scripts/test-episode-watcher.mjs
    - .planning/phases/03-so-what-chain/03-CONTEXT.md
  </read_first>
  <behavior>
    - hasQuantifiedImpact("매출 30% 증가") returns true
    - hasQuantifiedImpact("") returns false
    - hasQuantifiedImpact(null) returns false
    - Episode with weak result triggers [resume-panel:SO-WHAT] message
    - Episode with quantified result does NOT trigger SO-WHAT
    - so_what_active.active === true suppresses SO-WHAT trigger
    - SO-WHAT message includes episode title
    - SO-WHAT and profiler messages can coexist in same additionalContext
  </behavior>
  <action>
episode-watcher.mjs에 두 가지를 추가한다:

**1. hasQuantifiedImpact() 헬퍼 함수** (line 102 이후, countStarGaps 바로 뒤에 추가):

```javascript
function hasQuantifiedImpact(resultText) {
  if (!resultText || resultText.trim() === "") return false;
  const IMPACT_PATTERN = /\d+(\.\d+)?\s*(명|건|%|원|만|억|배|시간|분|초|ms|개월|일|주|달|회|번|개|위|등|톤|km|kg|L|대|편|권|통|점|곳|팀)/;
  return IMPACT_PATTERN.test(resultText);
}
```

정규식 설명: 숫자(정수 또는 소수) 뒤에 한국어 단위(명, 건, % 등) 또는 기술 단위(ms, km 등)가 오는 패턴. CONTEXT.md에서 잠금된 판단 기준.

**2. SO-WHAT 트리거 로직** (isResumeSourceChange 블록 내, 스코어링 로직의 임계값 체크와 score 리셋 이후, 스냅샷 업데이트 직전에 추가):

위치: line ~206 (score = 0 리셋) 이후, line ~209 (writeFileSync snapshotPath) 이전.

```javascript
// So What chain trigger — check for impact-shallow episodes
const metaForSoWhat = readJSON(metaPath) || {};
if (!metaForSoWhat.so_what_active?.active) {
  const prevCount = snapshot.episode_count || 0;
  let checked = 0;
  for (const project of source.projects || []) {
    for (const ep of project.episodes || []) {
      checked++;
      if (checked <= prevCount) continue;
      if (!hasQuantifiedImpact(ep.star?.result || ep.result || "")) {
        messages.push(
          `[resume-panel:SO-WHAT] 에피소드 "${ep.title || "(제목 없음)"}" 임팩트 부족`
        );
        break;
      }
    }
    if (messages.some(m => m.includes("[resume-panel:SO-WHAT]"))) break;
  }
}
```

핵심 포인트:
- `ep.star?.result || ep.result || ""` -- star-wrapped와 flat 구조 모두 지원 (기존 코드는 source.projects[] 사용하므로 동일 패턴)
- `metaForSoWhat.so_what_active?.active` 체크로 진행 중인 체인에서 재트리거 방지 (Pitfall 1 대응)
- `break` + outer break로 hook 호출당 최대 1개 SO-WHAT 메시지만 발송 (RESEARCH.md 권장사항)
- 프로파일러 score 리셋 이후에 위치하므로 SO-WHAT과 프로파일러 메시지가 동시 발송 가능 (독립적)

metaPath에서 meta를 다시 읽는 이유: 스코어링 블록에서 이미 meta.json에 score를 쓴 후이므로, 최신 상태(score 포함)를 반영한 meta를 읽어야 함. 하지만 so_what_active는 이 hook이 아닌 오케스트레이터가 쓰므로, 이전에 읽은 metaJSON을 재활용해도 됨. 다만 명확성을 위해 새로 읽는다.

주의: 스코어링 블록이 metaJSON 변수를 이미 사용하므로, 변수명 충돌 방지를 위해 `metaForSoWhat`로 별도 명명한다.

모든 테스트(기존 22 + 신규 So What)가 PASS하는 것을 확인한다.
  </action>
  <verify>
    <automated>cd /Users/laonpeople/dev/chenjing-plugins && node plugins/resume/scripts/test-episode-watcher.mjs 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - episode-watcher.mjs에 `function hasQuantifiedImpact(resultText)` 함수 존재
    - episode-watcher.mjs에 `IMPACT_PATTERN` 정규식 존재 (명|건|%|원|만|억|배 등 단위 포함)
    - episode-watcher.mjs에 `[resume-panel:SO-WHAT]` 문자열이 messages.push 호출 내에 존재
    - episode-watcher.mjs에 `so_what_active?.active` 체크 존재
    - episode-watcher.mjs에 `(제목 없음)` 폴백 문자열 존재
    - `node plugins/resume/scripts/test-episode-watcher.mjs` 실행 시 모든 테스트 PASS (exit code 0)
    - 출력에 "All So What chain tests passed." 포함
    - 출력에 "All scoring system tests passed." 포함 (기존 테스트 유지)
    - 출력에 "All findings routing tests passed." 포함 (기존 테스트 유지)
  </acceptance_criteria>
  <done>hasQuantifiedImpact() + SO-WHAT 트리거 로직이 episode-watcher.mjs에 구현됨. 기존 22개 + 신규 6+개 테스트 모두 PASS (GREEN 상태).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resume-source.json -> hook | 사용자 프로젝트 디렉토리의 로컬 JSON 파일을 hook이 읽음. 외부 입력 아님 |
| hook -> additionalContext | hook stdout -> Claude Code 오케스트레이터. 내부 파이프라인 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | D (Denial of Service) | SO-WHAT re-trigger loop | mitigate | `so_what_active.active` 플래그를 meta.json에서 확인하여 active chain 중 재트리거 차단. hook 호출당 최대 1개 SO-WHAT 메시지 (break문) |
| T-03-02 | T (Tampering) | result field overwrite | accept | 이 plan에서는 result를 읽기만 함 (쓰기는 Plan 02의 SKILL.md 오케스트레이터 담당). hook은 resume-source.json을 수정하지 않음 |
| T-03-03 | I (Information Disclosure) | episode title in SO-WHAT message | accept | 에피소드 타이틀은 유저 본인의 데이터이며, 동일 머신 내 additionalContext로만 전달. 외부 노출 없음 |
</threat_model>

<verification>
1. `node plugins/resume/scripts/test-episode-watcher.mjs` -- 모든 테스트 PASS (exit code 0)
2. "All So What chain tests passed." 출력 확인
3. "All scoring system tests passed." + "All findings routing tests passed." 출력으로 기존 테스트 회귀 없음 확인
</verification>

<success_criteria>
- hasQuantifiedImpact() 함수가 수치+단위 정규식으로 임팩트 충분성을 판단
- 수치 없는 episode 저장 시 `[resume-panel:SO-WHAT] 에피소드 "{title}" 임팩트 부족` 메시지 발송
- 수치 있는 episode 저장 시 SO-WHAT 메시지 미발송
- so_what_active chain 진행 중 재트리거 방지
- 기존 스코어링 + findings 라우팅 로직 영향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-so-what-chain/03-01-SUMMARY.md`
</output>
