---
phase: 05-perspective-shifting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/resume/.claude/agents/profiler.md
  - plugins/resume/.claude/agents/hr.md
  - plugins/resume/.claude/agents/c-level.md
autonomous: true
requirements: [PERSP-01, PERSP-02, PERSP-03]

must_haves:
  truths:
    - "profiler.md has a perspective shift detection section that identifies undervaluation signals in leadership/collaboration episodes"
    - "HR.md has a perspective shifting mode that generates junior/PM perspective questions with concrete scene descriptions"
    - "C-Level.md has a perspective shifting mode that generates boss/customer perspective questions with concrete scene descriptions"
    - "Perspective mapping table matches CLAUDE.md: leadership->junior(HR), collaboration->PM(HR), problem-solving->boss(C-Level), achievement->customer(C-Level)"
  artifacts:
    - path: "plugins/resume/.claude/agents/profiler.md"
      provides: "Perspective shift detection + finding generation"
      contains: "관점 전환 탐지"
    - path: "plugins/resume/.claude/agents/hr.md"
      provides: "Perspective shifting mode for leadership/collaboration episodes"
      contains: "관점 전환 모드"
    - path: "plugins/resume/.claude/agents/c-level.md"
      provides: "Perspective shifting mode for problem-solving/achievement episodes"
      contains: "관점 전환 모드"
  key_links:
    - from: "plugins/resume/.claude/agents/profiler.md"
      to: ".resume-panel/findings-inbox.jsonl"
      via: "perspective_shift finding JSON output"
      pattern: "perspective_shift"
    - from: "plugins/resume/.claude/agents/hr.md"
      to: "profiler perspective_shift finding context"
      via: "Input section references target_perspective, scene_hint"
      pattern: "scene_hint"
    - from: "plugins/resume/.claude/agents/c-level.md"
      to: "profiler perspective_shift finding context"
      via: "Input section references target_perspective, scene_hint"
      pattern: "scene_hint"
---

<objective>
Add perspective shift detection to profiler.md and perspective shifting mode sections to HR.md and C-Level.md, enabling third-person viewpoint questions that bypass user self-deprecation for leadership/collaboration episodes.

Purpose: When the profiler detects undervaluation signals in leadership/collaboration episodes, it generates `perspective_shift` findings that will be routed (via SKILL.md in Plan 02) to the appropriate agent. This plan creates the detection criteria and the agent mode sections that generate concrete, scene-based perspective questions.

Output: Three modified agent prompt files with perspective shifting capabilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perspective-shifting/05-CONTEXT.md
@.planning/phases/05-perspective-shifting/05-RESEARCH.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-02-SUMMARY.md

@plugins/resume/.claude/agents/profiler.md
@plugins/resume/.claude/agents/hr.md
@plugins/resume/.claude/agents/c-level.md

<interfaces>
<!-- Existing profiler.md structure (lines 79-185): -->
<!-- ## 자율 오케스트레이션 모드 -->
<!--   ### 1. 전문 에이전트 디스패치 -->
<!--   ### 2. 크로스 컴퍼니 패턴 분석 (Phase 4 addition) -->
<!--   ### 3. findings-inbox.jsonl에 결과 기록 -->
<!--   ### 4. meta.json 갱신 -->
<!-- New perspective shift detection section goes AFTER pattern analysis (### 2), renumbered as ### 3 -->
<!-- Existing ### 3 -> ### 4, existing ### 4 -> ### 5 -->

<!-- Existing hr.md structure (lines 79-130): -->
<!-- ## 갭 프로빙 모드 (Phase 4 addition, lines 79-122) -->
<!-- ## 금지사항 (lines 124-130) -->
<!-- New perspective shifting mode section goes AFTER gap probing mode, BEFORE 금지사항 -->

<!-- Existing c-level.md structure (lines 110-167): -->
<!-- ## So What 체인 모드 (Phase 3 addition, lines 110-158) -->
<!-- ## 금지사항 (lines 160-167) -->
<!-- New perspective shifting mode section goes AFTER So What chain mode, BEFORE 금지사항 -->

<!-- Existing finding format example from profiler.md pattern_detected: -->
<!-- {"id":"pt-{timestamp}","type":"pattern_detected","urgency":"MEDIUM","source":"profiler","message":"...","context":{...},"created_at":"{ISO timestamp}"} -->
<!-- perspective_shift finding follows same structure with type: "perspective_shift" -->

<!-- Episode type field in resume-source.json: "성과|문제해결|리더십|협업|학습|기타" -->
<!-- Minimization keywords from Phase 2: 도움, 참여, 지원, 보조, 서포트 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add perspective shift detection section to profiler.md</name>
  <files>plugins/resume/.claude/agents/profiler.md</files>
  <read_first>
    - plugins/resume/.claude/agents/profiler.md
    - .planning/phases/05-perspective-shifting/05-RESEARCH.md (Code Examples section, lines 240-270)
  </read_first>
  <action>
Add a new `### 3. 관점 전환 탐지` section inside `## 자율 오케스트레이션 모드`, AFTER the existing `### 2. 크로스 컴퍼니 패턴 분석` section and BEFORE the existing `### 3. findings-inbox.jsonl에 결과 기록` section. Renumber existing ### 3 to ### 4 and ### 4 to ### 5.

The new section must contain these exact subsections:

**#### 탐지 조건 (AND):**
Two conditions must BOTH be met:
1. Episode type is 리더십 or 협업 (from resume-source.json episode `type` field)
2. At least ONE undervaluation signal detected:
   - result 필드가 비어있거나 구체적 수치/규모가 없음
   - action 필드에 역할 축소 키워드 포함 (도움, 참여, 지원, 보조, 서포트)
   - 회사 규모/MAU 대비 result가 지나치게 겸손함
   - 유저의 자기 평가 경향이 과소평가형 (커뮤니케이션 스타일 분석 결과 기반)

**#### 관점 매핑 (table format):**
| 에피소드 타입 | 관점 | 담당 에이전트 |
|-------------|------|-------------|
| 리더십 | 주니어 팀원 | 인사담당자 |
| 협업 | PM 또는 상대 팀 담당자 | 인사담당자 |
| 문제해결 | 상사 또는 CTO | C-Level |
| 성과 | 고객 또는 비즈니스 오너 | C-Level |

**#### scene_hint 생성:**
Instruct profiler to extract scene context from the episode's situation + action fields. scene_hint must reference at least one concrete detail (technology name, project name, team name, or specific event). Generic scenes like "팀 미팅에서" are forbidden -- must be specific like "Kafka 마이그레이션 완료 후 팀 회고 자리에서".

**#### 산출 형식:**
Finding JSON output appended to findings-inbox.jsonl. Structure:
```json
{"id":"ps-{timestamp}","type":"perspective_shift","urgency":"MEDIUM","source":"profiler","message":"관점 전환 추천: '{에피소드 제목}' -- {target_perspective} 시점에서 추가 발굴 가능. 과소평가 신호: {signal}.","context":{"target_perspective":"{관점}","target_agent":"{에이전트명}","episode_ref":"{에피소드 제목}","company":"{회사명}","project":"{프로젝트명}","episode_type":"{리더십|협업|문제해결|성과}","scene_hint":"{구체적 장면}","undervaluation_signals":["{신호1}","{신호2}"]},"created_at":"{ISO timestamp}"}
```

**#### 금지:**
- 세션당 2개 초과 관점 전환 finding 생성 금지 (meta.json의 perspective_shifts_count 확인)
- 유저가 재구성할 수 없는 관점 사용 금지 (인턴 에피소드에 CEO 관점 등)
- 이미 관점 전환한 에피소드 재탐지 금지 (meta.json의 perspective_shifted_episodes 확인)

IMPORTANT: Keep the section concise -- under 40 lines total. Use bullet-point criteria, not prose. This prevents profiler prompt length bloat (STATE.md blocker concern).
  </action>
  <verify>
    <automated>grep -n "관점 전환 탐지" plugins/resume/.claude/agents/profiler.md && grep -n "perspective_shift" plugins/resume/.claude/agents/profiler.md && grep -n "### 4\. findings-inbox" plugins/resume/.claude/agents/profiler.md && grep -n "### 5\. meta.json" plugins/resume/.claude/agents/profiler.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "관점 전환 탐지" profiler.md returns a match (section exists)
    - grep "perspective_shift" profiler.md returns a match (finding type present)
    - grep "scene_hint" profiler.md returns a match (scene hint generation included)
    - grep "perspective_shifts_count" profiler.md returns a match (session limit referenced)
    - grep "perspective_shifted_episodes" profiler.md returns a match (duplicate prevention)
    - grep "### 4" profiler.md shows "findings-inbox" (renumbered correctly from 3)
    - grep "### 5" profiler.md shows "meta.json" (renumbered correctly from 4)
    - The mapping table includes all 4 episode types: 리더십, 협업, 문제해결, 성과
    - Undervaluation signal list includes: 도움, 참여, 지원, 보조, 서포트
  </acceptance_criteria>
  <done>profiler.md contains a perspective shift detection section with AND-condition trigger (type + undervaluation signal), CLAUDE.md mapping table, scene_hint generation instructions, perspective_shift finding JSON format, and prohibition rules. Existing subsections renumbered correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add perspective shifting mode to HR.md and C-Level.md</name>
  <files>plugins/resume/.claude/agents/hr.md, plugins/resume/.claude/agents/c-level.md</files>
  <read_first>
    - plugins/resume/.claude/agents/hr.md
    - plugins/resume/.claude/agents/c-level.md
    - .planning/phases/05-perspective-shifting/05-RESEARCH.md (Code Examples section, lines 274-340)
  </read_first>
  <action>
**HR.md -- Add `## 관점 전환 모드` section:**

Insert AFTER `## 갭 프로빙 모드` section (which ends at line 122) and BEFORE `## 금지사항` (line 124). Follow the same mode section structure as gap probing mode.

Section contents:

```
## 관점 전환 모드

오케스트레이터가 관점 전환 컨텍스트를 전달하면 관점 전환 모드로 동작한다.

### 입력

오케스트레이터가 다음을 전달한다:
- 대상 에피소드 (title, situation, task, action, result)
- 관점 (주니어 팀원 또는 PM/상대 팀 담당자)
- 장면 힌트 (scene_hint)
- 유저 프로파일
- 해당 회사 리서처 조사 결과

### 질문 생성 규칙

1. **장면 묘사 필수** -- scene_hint를 활용하여 구체적 장면을 설정한다. "{scene_hint}에서 {관점 인물}이 너에 대해 뭐라고 할 것 같아?"
2. **선택지에 업그레이드된 역할 포함** -- 유저가 직접 말한 것보다 더 큰 역할을 선택지에 반드시 포함
3. **호기심 + 인정 톤** -- "주니어 입장에서 보면, 네가 한 게 더 대단해 보일 수 있거든"

### 질문 패턴

**리더십 에피소드 (주니어 관점):**
[인사담당자] {scene_hint}에서, 팀에 새로 온 주니어가 너를 보고 뭐라고 했을 것 같아?
  1) "저 사람이 이 프로젝트 방향 잡은 사람이구나"
  2) "기술적으로 막힐 때마다 저 사람한테 가면 됐다"
  3) 딱히 그런 인상은 없었을 듯

**협업 에피소드 (PM 관점):**
[인사담당자] {project}에서 같이 일한 PM이 {scene_hint} 때 너에 대해 뭐라고 했을 것 같아?
  1) "이 사람 덕분에 일정이 당겨졌다"
  2) "기획 의도를 제일 잘 이해하고 구현해줬다"
  3) 특별히 언급할 정도는 아니었을 듯

### 핵심 규칙

- 마지막 선택지는 항상 "겸손 옵션" (특별히 없었을 듯 / 딱히 그런 인상은 없었을 듯) -- 유저의 선택권 보장
- 앞선 선택지들이 유저 본인 인식보다 큰 역할을 표현해야 한다
- 장면이 추상적이면 안 된다 -- 프로젝트명, 기술명, 팀명 등 구체적 디테일 포함
- 선택지 최대 2개 + 겸손 옵션 1개 = 총 3개 (AskUserQuestion이 Other 자동 추가)
```

**C-Level.md -- Add `## 관점 전환 모드` section:**

Insert AFTER `## So What 체인 모드` section (which ends at line 158) and BEFORE `## 금지사항` (line 160). Follow the same mode section structure.

Section contents:

```
## 관점 전환 모드

오케스트레이터가 관점 전환 컨텍스트를 전달하면 관점 전환 모드로 동작한다.

### 입력

오케스트레이터가 다음을 전달한다:
- 대상 에피소드 (title, situation, task, action, result)
- 관점 (상사/CTO 또는 고객/비즈니스 오너)
- 장면 힌트 (scene_hint)
- 유저 프로파일
- 해당 회사 리서처 조사 결과

### 질문 생성 규칙

1. **장면 묘사 필수** -- scene_hint를 활용하여 구체적 장면을 설정한다. "{scene_hint}에서 {관점 인물}이 이 성과를 어떻게 설명할까?"
2. **선택지에 업그레이드된 임팩트 포함** -- 유저가 직접 말한 것보다 더 큰 비즈니스 임팩트를 선택지에 반드시 포함
3. **호기심 + 인정 톤** -- "상사 입장에서 보면, 이게 팀 전체에 미친 영향이 더 크게 보일 수 있거든"

### 질문 패턴

**문제해결 에피소드 (상사/CTO 관점):**
[C-Level] {scene_hint}에서, 네 상사가 이 문제 해결을 경영진한테 보고할 때 뭐라고 했을 것 같아?
  1) "이 사람이 핵심 기술 판단을 해서 해결됐다"
  2) "이 사람이 주도해서 팀이 빠르게 대응할 수 있었다"
  3) 특별히 보고할 정도는 아니었을 듯

**성과 에피소드 (고객/비즈니스 오너 관점):**
[C-Level] {project} 런칭 후, {scene_hint} 때 주요 고객이나 비즈니스 오너가 뭐라고 했을 것 같아?
  1) "이 기능 덕분에 비용/시간이 확 줄었다"
  2) "이거 없었으면 우리 사업 못 했다"
  3) 특별히 언급할 정도는 아니었을 듯

### 핵심 규칙

- 마지막 선택지는 항상 "겸손 옵션" (특별히 보고/언급할 정도는 아니었을 듯) -- 유저의 선택권 보장
- 앞선 선택지들이 유저 본인 인식보다 큰 비즈니스 임팩트를 표현해야 한다
- 장면이 추상적이면 안 된다 -- 프로젝트명, 기술명, 비즈니스 맥락 등 구체적 디테일 포함
- 선택지 최대 2개 + 겸손 옵션 1개 = 총 3개 (AskUserQuestion이 Other 자동 추가)
```

IMPORTANT for both files:
- Do NOT modify any existing sections -- only add the new section
- Maintain Korean language consistency with existing content
- The question patterns are EXAMPLES for the agent -- the agent generates context-specific versions using episode data
  </action>
  <verify>
    <automated>grep -n "관점 전환 모드" plugins/resume/.claude/agents/hr.md && grep -n "관점 전환 모드" plugins/resume/.claude/agents/c-level.md && grep -n "scene_hint" plugins/resume/.claude/agents/hr.md && grep -n "scene_hint" plugins/resume/.claude/agents/c-level.md && grep -c "겸손 옵션" plugins/resume/.claude/agents/hr.md && grep -c "겸손 옵션" plugins/resume/.claude/agents/c-level.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "관점 전환 모드" hr.md returns a match (section exists in HR)
    - grep "관점 전환 모드" c-level.md returns a match (section exists in C-Level)
    - grep "scene_hint" hr.md returns a match (scene hint usage in HR)
    - grep "scene_hint" c-level.md returns a match (scene hint usage in C-Level)
    - grep "겸손 옵션" hr.md returns count >= 1 (humble option present)
    - grep "겸손 옵션" c-level.md returns count >= 1 (humble option present)
    - grep "주니어" hr.md returns match in perspective shifting section (junior perspective)
    - grep "PM" hr.md returns match in perspective shifting section (PM perspective)
    - grep "상사" c-level.md returns match in perspective shifting section (boss perspective)
    - grep "고객" c-level.md returns match in perspective shifting section (customer perspective)
    - hr.md 갭 프로빙 모드 section is intact (not modified)
    - c-level.md So What 체인 모드 section is intact (not modified)
    - Both files' 금지사항 sections are intact (not modified)
  </acceptance_criteria>
  <done>HR.md has a perspective shifting mode section handling leadership (junior viewpoint) and collaboration (PM viewpoint) episodes with concrete scene-based question patterns and mandatory humble option. C-Level.md has a perspective shifting mode section handling problem-solving (boss viewpoint) and achievement (customer viewpoint) episodes with the same structure. Both use curiosity+recognition tone and enforce concrete scene descriptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- this plan modifies only markdown prompt files within the Claude Code plugin system. No user input is processed by custom code, no external APIs are called, no data is stored in new locations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | D (Denial of Service) | profiler.md prompt length | mitigate | Keep perspective detection section under 40 lines; concise bullet-point format prevents lost-in-the-middle degradation of existing analysis quality |
| T-05-02 | S (Spoofing) | perspective_shift finding | accept | Finding is generated internally by profiler, not from user input; same trust model as pattern_detected findings |
</threat_model>

<verification>
1. profiler.md: `grep "관점 전환 탐지" profiler.md` finds the detection section
2. profiler.md: `grep "perspective_shift" profiler.md` finds the finding type
3. profiler.md: Subsection numbering is sequential (### 1 through ### 5)
4. hr.md: `grep "관점 전환 모드" hr.md` finds the mode section
5. c-level.md: `grep "관점 전환 모드" c-level.md` finds the mode section
6. Both agent modes reference scene_hint and include concrete question patterns
7. Both agent modes have a mandatory humble option as last choice
8. Existing sections in all three files remain unmodified
</verification>

<success_criteria>
- profiler.md can detect undervaluation in leadership/collaboration episodes and generate perspective_shift findings with correct JSON format
- HR.md can generate junior/PM perspective questions with concrete scene descriptions when invoked in perspective shifting mode
- C-Level.md can generate boss/customer perspective questions with concrete scene descriptions when invoked in perspective shifting mode
- All prompt additions are concise and maintain existing file structure
</success_criteria>

<output>
After completion, create `.planning/phases/05-perspective-shifting/05-01-SUMMARY.md`
</output>
