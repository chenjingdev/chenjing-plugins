---
phase: 05-perspective-shifting
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - plugins/resume/skills/resume-panel/SKILL.md
autonomous: true
requirements: [PERSP-01, PERSP-03]

must_haves:
  truths:
    - "SKILL.md routes perspective_shift findings to the designated agent (HR or C-Level) in perspective shifting mode"
    - "Session limit of 2 perspective shifts is enforced via meta.json counter"
    - "Humble option ('겸손 옵션') triggers meta.json tracking and interview resumption"
    - "Upgraded role selection triggers optional follow-up question before interview resumption"
    - "perspective_shifted_episodes tracking prevents duplicate perspective shifts for same episode"
  artifacts:
    - path: "plugins/resume/skills/resume-panel/SKILL.md"
      provides: "perspective_shift handler (item 8) + agent selection criteria + flow protection"
      contains: "perspective_shift"
  key_links:
    - from: "plugins/resume/skills/resume-panel/SKILL.md"
      to: "plugins/resume/.claude/agents/hr.md"
      via: "Agent() call with perspective shifting mode context"
      pattern: "관점 전환 모드"
    - from: "plugins/resume/skills/resume-panel/SKILL.md"
      to: "plugins/resume/.claude/agents/c-level.md"
      via: "Agent() call with perspective shifting mode context"
      pattern: "관점 전환 모드"
    - from: "plugins/resume/skills/resume-panel/SKILL.md"
      to: ".resume-panel/meta.json"
      via: "perspective_shifts_this_session counter + perspective_shifted_episodes array"
      pattern: "perspective_shifts_this_session"
---

<objective>
Add perspective_shift message handling rules to SKILL.md (item 8), connecting the profiler's perspective_shift findings to HR/C-Level agent invocation in perspective shifting mode.

Purpose: Wire the complete pipeline -- profiler detection (Plan 01) -> findings-inbox.jsonl -> episode-watcher hook -> SKILL.md routing -> agent mode invocation -> user interaction -> meta.json tracking. Without this, the profiler's perspective_shift findings would be generated but never acted upon.

Output: SKILL.md with complete perspective_shift orchestration rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perspective-shifting/05-CONTEXT.md
@.planning/phases/05-perspective-shifting/05-RESEARCH.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-03-SUMMARY.md
@.planning/phases/05-perspective-shifting/05-01-SUMMARY.md

@plugins/resume/skills/resume-panel/SKILL.md

<interfaces>
<!-- SKILL.md message handling rules structure (lines 392-527): -->
<!-- ## 자율 오케스트레이션 -- Hook 메시지 처리 -->
<!--   ### 메시지 처리 규칙 -->
<!--     1. [resume-panel] 프로파일러 호출 필요 -> profiler background call -->
<!--     2. [resume-panel:HIGH] -> immediate AskUserQuestion -->
<!--     3. [resume-panel:MEDIUM] -> delayed AskUserQuestion -->
<!--     4. LOW -> stored in findings.json -->
<!--     5. [resume-panel:SO-WHAT] -> So What chain start -->
<!--     6. [resume-panel:MEDIUM] (timeline_gap_found) -> gap probing -->
<!--     7. [resume-panel:MEDIUM] (pattern_detected) -> pattern-based discovery -->
<!-- NEW item 8: [resume-panel:MEDIUM] (perspective_shift) -> perspective shifting -->

<!-- Agent selection criteria section (lines 157-168): -->
<!-- Currently lists: senior, C-Level, recruiter, HR, coffee-chat, So What chain, gap probing, pattern target_agent -->
<!-- Need to add: perspective_shift -> designated agent (HR or C-Level) -->

<!-- Interview flow protection section (lines 516-527): -->
<!-- Currently covers: HIGH, MEDIUM/LOW, SO-WHAT multi-turn, SO-WHAT+profiler priority, gap probing single-turn, gap+SO-WHAT priority, gap limit -->
<!-- Need to add: perspective shift single-turn behavior, perspective+SO-WHAT priority -->

<!-- perspective_shift finding format (from Plan 01 profiler.md): -->
<!-- {"id":"ps-{timestamp}","type":"perspective_shift","urgency":"MEDIUM",...,"context":{"target_perspective":"...","target_agent":"인사담당자|C-Level","episode_ref":"...","scene_hint":"...","undervaluation_signals":[...]}} -->

<!-- Two-signal classification pattern (established in Phase 4, item 6-7): -->
<!-- "공백"+"개월" -> timeline_gap_found -->
<!-- "패턴 발견" -> pattern_detected -->
<!-- NEW: "관점 전환" -> perspective_shift -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add perspective_shift handler, agent selection, and flow protection to SKILL.md</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md
    - .planning/phases/05-perspective-shifting/05-RESEARCH.md (Code Examples section, lines 323-340 for SKILL.md handler)
    - .planning/phases/05-perspective-shifting/05-01-SUMMARY.md (for finding format and agent mode definitions from Plan 01)
  </read_first>
  <action>
Make three additions to SKILL.md. Do NOT modify any existing content -- only add new items.

**Addition 1: Message handling rule item 8 (after existing item 7)**

Add the following as item 8 in the `### 메시지 처리 규칙` section, after item 7 (`[resume-panel:MEDIUM]` pattern_detected):

```
8. **`[resume-panel:MEDIUM]` (perspective_shift)** -> 관점 전환 질문
   - MEDIUM 메시지 내용에 "관점 전환"이 포함되어 있으면 perspective_shift finding으로 판단
   - meta.json의 `perspective_shifts_this_session` 카운터 확인 -- 2 이상이면 무시
   - meta.json의 `perspective_shifted_episodes` 배열 확인 -- 해당 episode_ref가 이미 있으면 무시
   - context에서 target_agent 확인 -- 해당 에이전트를 관점 전환 모드로 호출:
     ```
     Agent(
       prompt: "관점 전환 모드. 대상 에피소드: {episode_ref}({company} {project}). 관점: {target_perspective}. 장면 힌트: {scene_hint}. 유저 프로파일: {프로파일 요약}. 리서처 조사: {관련 회사 조사 결과}."
     )
     ```
   - 에이전트 리턴을 AskUserQuestion으로 변환 (기존 변환 규칙 동일 적용)
   - 유저 응답 처리:
     - **겸손 옵션 선택** ("특별히 없었을 듯", "딱히 그런 인상은 없었을 듯" 등): meta.json에 `perspective_shifted_episodes` 배열에 episode_ref 추가, 인터뷰 복귀
     - **업그레이드 역할 선택**: 해당 에피소드의 result 보강을 위한 후속 질문 1개 가능 (일반 모드로 전환), meta.json에 `perspective_shifted_episodes` 배열에 episode_ref 추가
   - meta.json `perspective_shifts_this_session` 카운터 증가
```

**Addition 2: Agent selection criteria (in `### 에이전트 선택 기준` section)**

Add one line after the existing "패턴에서 `target_agent` 지정됨 -> 해당 에이전트 우선" line:
```
- 관점 전환 finding에서 `target_agent` 지정됨 -> 해당 에이전트 관점 전환 모드로 호출
```

**Addition 3: Interview flow protection rules (in `### 인터뷰 흐름 보호` section)**

Add these lines at the end of the existing flow protection rules:
```
- 관점 전환은 single-turn이므로 인터뷰 흐름을 크게 방해하지 않는다. 유저가 겸손 옵션을 선택하면 즉시 복귀한다
- 관점 전환과 SO-WHAT이 동시 도착하면 SO-WHAT을 먼저 처리한다 (체인 완료 후 관점 전환)
- 관점 전환 제한(2개/세션)에 도달하면 나머지 관점 전환 finding은 조용히 무시한다
```

IMPORTANT:
- The two-signal classification follows the established Phase 4 pattern: "공백"+"개월" for gaps (item 6), "패턴 발견" for patterns (item 7), now "관점 전환" for perspective shifts (item 8)
- Session limit is 2 (not 3 like gap probing) per user decision
- meta.json fields: `perspective_shifts_this_session` (counter, integer) and `perspective_shifted_episodes` (array of episode_ref strings)
- Perspective shift is MEDIUM urgency, so it follows MEDIUM timing rules (delivered at project/company change, not immediately)
  </action>
  <verify>
    <automated>grep -n "perspective_shift" plugins/resume/skills/resume-panel/SKILL.md && grep -n "관점 전환 모드" plugins/resume/skills/resume-panel/SKILL.md && grep -n "perspective_shifts_this_session" plugins/resume/skills/resume-panel/SKILL.md && grep -n "perspective_shifted_episodes" plugins/resume/skills/resume-panel/SKILL.md && grep -c "관점 전환" plugins/resume/skills/resume-panel/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "perspective_shift" SKILL.md returns matches (finding type referenced)
    - grep "관점 전환 모드" SKILL.md returns match in item 8 Agent() prompt
    - grep "perspective_shifts_this_session" SKILL.md returns match (session counter)
    - grep "perspective_shifted_episodes" SKILL.md returns match (duplicate prevention array)
    - grep "관점 전환" SKILL.md returns count >= 4 (item 8 header + agent selection + flow protection rules)
    - Item 8 includes two-signal detection: "관점 전환" in MEDIUM message content
    - Item 8 includes session limit check (2 이상이면 무시)
    - Item 8 includes duplicate episode check (perspective_shifted_episodes)
    - Item 8 includes humble option response handling (겸손 옵션 -> meta.json update + resume)
    - Item 8 includes upgraded role response handling (후속 질문 1개 가능)
    - Agent selection criteria mentions perspective_shift target_agent routing
    - Flow protection rules mention single-turn, SO-WHAT priority, and session limit
    - Existing items 1-7 are NOT modified
    - Existing agent selection criteria are NOT modified
    - Existing flow protection rules are NOT modified
  </acceptance_criteria>
  <done>SKILL.md has item 8 handling perspective_shift findings with two-signal classification ("관점 전환"), session limiting (max 2), duplicate prevention (perspective_shifted_episodes), Agent() invocation in perspective shifting mode, AskUserQuestion conversion, humble/upgraded response handling, meta.json tracking, agent selection routing, and flow protection rules. Complete pipeline: profiler detection -> findings-inbox -> hook -> SKILL.md item 8 -> HR/C-Level perspective mode -> AskUserQuestion -> user response -> meta.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- this plan modifies only the SKILL.md markdown prompt file. The orchestrator processes findings generated internally by the profiler agent, not direct user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | D (Denial of Service) | perspective_shift over-triggering | mitigate | Session limit of 2 enforced via meta.json counter; duplicate prevention via perspective_shifted_episodes array |
| T-05-04 | T (Tampering) | MEDIUM message misclassification | mitigate | Two-signal detection ("관점 전환" keyword) follows established Phase 4 pattern; reduces false classification between gap/pattern/perspective MEDIUM messages |
</threat_model>

<verification>
1. SKILL.md item 8 exists and handles perspective_shift findings
2. Two-signal classification: "관점 전환" in MEDIUM message -> perspective_shift
3. Session limit: perspective_shifts_this_session >= 2 -> ignore
4. Duplicate prevention: episode_ref in perspective_shifted_episodes -> ignore
5. Agent invocation: target_agent from finding context -> Agent() with perspective mode
6. Response handling: humble option -> meta.json update + resume; upgraded role -> follow-up + meta.json
7. Agent selection criteria includes perspective_shift routing
8. Flow protection includes single-turn behavior, SO-WHAT priority, session limit enforcement
9. Items 1-7 unchanged
</verification>

<success_criteria>
- perspective_shift MEDIUM messages are correctly classified and routed to designated agent
- Session limit of 2 prevents user fatigue from excessive perspective questions
- Duplicate prevention stops re-asking about already perspective-shifted episodes
- Humble option selection cleanly returns to normal interview flow
- Upgraded role selection allows one follow-up question for result enrichment
- Pipeline is complete: profiler -> findings -> hook -> SKILL.md -> agent -> user -> meta.json
</success_criteria>

<output>
After completion, create `.planning/phases/05-perspective-shifting/05-02-SUMMARY.md`
</output>
