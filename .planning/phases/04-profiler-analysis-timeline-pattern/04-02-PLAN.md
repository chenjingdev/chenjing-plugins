---
phase: 04-profiler-analysis-timeline-pattern
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/resume/.claude/agents/profiler.md
  - plugins/resume/.claude/agents/hr.md
autonomous: true
requirements: [PTRN-01, PTRN-02, TIME-03]

must_haves:
  truths:
    - "profiler.md contains a structured 4-category pattern analysis framework section"
    - "profiler.md pattern output includes pattern name, evidence episodes, unexplored company estimation, and agent-designated questions"
    - "profiler.md pattern analysis is guarded by 3+ episodes and 2+ companies condition"
    - "hr.md contains a gap probing mode section with mandatory skip option"
    - "hr.md gap probing generates 2 substantive options + 건너뛰기 exit"
  artifacts:
    - path: "plugins/resume/.claude/agents/profiler.md"
      provides: "Cross-company pattern analysis framework"
      contains: "패턴 분석"
    - path: "plugins/resume/.claude/agents/hr.md"
      provides: "Gap probing mode with skip option"
      contains: "갭 프로빙 모드"
  key_links:
    - from: "profiler.md pattern analysis"
      to: "findings-inbox.jsonl"
      via: "Bash append"
      pattern: "pattern_detected"
    - from: "hr.md gap probing"
      to: "SKILL.md orchestrator"
      via: "agent return text"
      pattern: "건너뛰기"
---

<objective>
Add cross-company pattern analysis framework to profiler.md and gap probing mode to hr.md.

Purpose: Enable the profiler to discover recurring behavioral patterns across companies and the HR agent to sensitively probe career timeline gaps with a mandatory skip option. These prompt additions are consumed by SKILL.md orchestration rules (Plan 03).

Output: Enhanced profiler.md with pattern analysis section, enhanced hr.md with gap probing mode section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-CONTEXT.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-RESEARCH.md

<interfaces>
<!-- Profiler.md existing structure (134 lines): -->
<!-- Sections in order: 임무, 입력 시그널, 분석 항목 (1-5), 산출 형식, 자율 오케스트레이션 모드 (1-3), 긴급도 판단 기준, 금지사항 -->
<!-- New pattern analysis section goes inside "자율 오케스트레이션 모드" between step 1 and step 2 -->

<!-- hr.md existing structure (83 lines): -->
<!-- Sections: 역할, 입력, 질문 생성 규칙, 질문 패턴, 산출 형식, 금지사항 -->
<!-- New gap probing mode section goes between 산출 형식 and 금지사항 -->

<!-- Existing findings format from profiler.md: -->
```json
{"id":"f-001","urgency":"HIGH","source":"recruiter","type":"gap_detected","message":"...","context":{...},"created_at":"..."}
```

<!-- Pattern finding format from RESEARCH.md: -->
```json
{"id":"pt-001","type":"pattern_detected","urgency":"MEDIUM","source":"profiler","message":"패턴 발견: '{패턴이름}' -- {회사1}({에피소드1}), {회사2}({에피소드2})에서 반복. {미탐색회사}에서도 비슷한 경험이 있을 것으로 추정.","context":{"pattern_name":"...","evidence_episodes":[...],"unexplored_company":"...","suggested_question":"...","target_agent":"시니어"},"created_at":"..."}
```

<!-- HR agent output format (must maintain): -->
```
[인사담당자] {질문 텍스트}
  1) {선택지1}
  2) {선택지2}
  3) {선택지3}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-company pattern analysis section to profiler.md</name>
  <files>plugins/resume/.claude/agents/profiler.md</files>
  <read_first>
    - plugins/resume/.claude/agents/profiler.md (full 134-line file -- understand exact structure and insertion point)
    - plugins/resume/.claude/agents/c-level.md (reference for how So What chain mode section was added in Phase 3 -- similar section-addition pattern)
    - .planning/phases/04-profiler-analysis-timeline-pattern/04-RESEARCH.md (Pattern 2 section for exact framework specification)
    - .planning/phases/04-profiler-analysis-timeline-pattern/04-CONTEXT.md (locked decisions on pattern detection)
  </read_first>
  <action>
    Add a new section `### 4. 크로스 컴퍼니 패턴 분석` inside the `## 자율 오케스트레이션 모드` section, between the existing `### 1. 전문 에이전트 디스패치` subsection and `### 2. findings-inbox.jsonl에 결과 기록` subsection. Renumber existing subsections: current "2. findings-inbox.jsonl" becomes "3. findings-inbox.jsonl", current "3. meta.json 갱신" becomes "4. meta.json 갱신".

    The new section content:

    ```markdown
    ### 2. 크로스 컴퍼니 패턴 분석

    오케스트레이터 메시지에 "패턴 분석 가능"이 포함되어 있을 때만 실행한다. 에피소드가 3개 이상이고 2개 이상 회사에 걸쳐 있을 때, 다음 4가지 카테고리로 크로스 컴퍼니 패턴을 분석한다:

    1. **역할 반복**: 여러 회사에서 비슷한 역할/포지션을 반복하는 패턴 (예: "레거시 시스템 현대화 주도")
    2. **기술 선택 패턴**: 어디를 가든 특정 기술/도구를 선택하거나 도입하는 패턴 (예: "Kafka 도입 추진")
    3. **성장/전환 패턴**: 커리어 궤적에서 반복되는 성장 또는 전환 행동 (예: "6개월 내 테크리드 전환")
    4. **문제해결 스타일**: 문제에 접근하는 반복적 방식 (예: "데이터 기반 의사결정 주도")

    #### 패턴 판정 기준

    - 최소 근거: **2개 이상 에피소드**, **2개 이상 다른 회사**에서 발견되어야 함
    - 유저의 **에이전시(주도적 선택)**가 있는 패턴만 인정 — 회사가 이미 사용하던 기술은 패턴이 아님
    - 패턴이 없으면 "패턴 미발견"도 유효한 결과다 — 억지로 만들지 않는다

    #### 각 패턴의 산출 형식

    패턴별로 다음을 findings-inbox.jsonl에 기록한다:

    ```json
    {"id":"pt-{timestamp}","type":"pattern_detected","urgency":"MEDIUM","source":"profiler","message":"패턴 발견: '{패턴 이름 3-5단어}' — {회사1}({에피소드1}), {회사2}({에피소드2})에서 반복. {미탐색 회사}에서도 비슷한 경험이 있을 것으로 추정.","context":{"pattern_name":"{패턴 이름}","category":"{역할반복|기술선택|성장전환|문제해결}","evidence_episodes":[{"company":"{회사명}","project":"{프로젝트명}","episode":"{에피소드 제목}"}],"unexplored_company":"{아직 물어보지 않은 회사명 또는 null}","suggested_question":"{구체적 질문}","target_agent":"{시니어|C-Level|인사담당자|채용담당자}"},"created_at":"{ISO timestamp}"}
    ```

    - 미탐색 회사가 없으면 `unexplored_company`를 null로, `suggested_question`은 기존 에피소드 심화 질문으로
    - `target_agent`는 패턴 유형에 따라 결정:
      - 역할 반복, 기술 선택 → 시니어
      - 성장/전환 → C-Level
      - 문제해결 스타일 → 시니어 또는 C-Level

    #### 금지

    - 임베딩/벡터 유사도를 사용하지 않는다 — 시맨틱 비교로 충분
    - 3개 미만 에피소드 또는 단일 회사 데이터로 패턴 분석하지 않는다
    - 일반적 기술 트렌드를 개인 패턴으로 혼동하지 않는다 (예: "모든 회사에서 Git 사용" ≠ 패턴)
    ```

    Also update the existing meta.json 갱신 section (now renumbered to ### 4) to include pattern analysis tracking fields. Add after the existing meta.json example:

    ```markdown
    패턴 분석 실행 시 추가 필드:
    ```bash
    cat <<'EOF' > .resume-panel/meta.json
    {
      "last_profiler_call": "2026-04-08T10:00:00Z",
      "last_profiler_episode_count": 12,
      "current_company": "튜닙",
      "total_profiler_calls": 3,
      "last_pattern_analysis_episode_count": 12,
      "last_pattern_analysis_company_count": 3,
      "last_timeline_check": "2026-04-08T10:00:00Z"
    }
    EOF
    ```
    ```
  </action>
  <verify>
    <automated>grep -c "크로스 컴퍼니 패턴 분석" plugins/resume/.claude/agents/profiler.md</automated>
  </verify>
  <acceptance_criteria>
    - profiler.md contains `### 2. 크로스 컴퍼니 패턴 분석` section header
    - profiler.md contains all 4 category names: `역할 반복`, `기술 선택 패턴`, `성장/전환 패턴`, `문제해결 스타일`
    - profiler.md contains `패턴 분석 가능` as the trigger condition
    - profiler.md contains `pattern_detected` as the finding type
    - profiler.md contains `2개 이상 에피소드` and `2개 이상 다른 회사` as minimum evidence
    - profiler.md contains `패턴 미발견` as valid null result
    - profiler.md contains `임베딩/벡터` in the prohibition section
    - profiler.md subsections are renumbered: 1. 전문 에이전트 디스패치, 2. 크로스 컴퍼니 패턴 분석, 3. findings-inbox.jsonl, 4. meta.json 갱신
    - profiler.md contains `last_pattern_analysis_episode_count` in meta.json example
    - profiler.md contains `target_agent` field in pattern finding format
    - profiler.md contains `suggested_question` field in pattern finding format
  </acceptance_criteria>
  <done>profiler.md has a complete 4-category pattern analysis framework section with output format, guard conditions, and prohibitions. Subsections correctly renumbered.</done>
</task>

<task type="auto">
  <name>Task 2: Add gap probing mode section to hr.md</name>
  <files>plugins/resume/.claude/agents/hr.md</files>
  <read_first>
    - plugins/resume/.claude/agents/hr.md (full 83-line file -- understand exact structure and insertion point)
    - plugins/resume/.claude/agents/c-level.md (reference for how mode sections are structured -- So What chain mode pattern)
    - .planning/phases/04-profiler-analysis-timeline-pattern/04-RESEARCH.md (Pattern 3 -- Gap Probing via HR Agent)
    - .planning/phases/04-profiler-analysis-timeline-pattern/04-CONTEXT.md (locked decisions on gap probing)
  </read_first>
  <action>
    Add a new section `## 갭 프로빙 모드` between the existing `## 산출 형식` section and `## 금지사항` section in hr.md.

    The new section content:

    ```markdown
    ## 갭 프로빙 모드

    오케스트레이터가 타임라인 갭 정보를 전달하면 갭 프로빙 모드로 동작한다.

    ### 입력

    오케스트레이터가 다음을 전달한다:
    - 공백 기간 (시작 회사/프로젝트, 종료 회사/프로젝트, 개월 수)
    - 갭 유형 (회사간 inter_company 또는 회사내 intra_company)
    - 유저 프로파일
    - 리서처 조사 결과 (해당 회사들)

    ### 질문 생성 규칙

    1. **기회 프레이밍** — 갭을 심문하지 않는다. "왜 비었어?"가 아니라 "이 기간에 혹시 이런 거 했어?"로 접근
    2. **선택지 2개 + 건너뛰기** — 반드시 3개 선택지: 실질적 옵션 2개 + "이 기간은 건너뛰기"
    3. **갭 유형별 선택지 조정**:
       - inter_company (회사간): 이직 준비, 사이드 프로젝트, 학습/자격증 등
       - intra_company (회사내): 부서 이동, 역할 전환, 내부 프로젝트 등

    ### 질문 패턴

    **회사간 갭 (inter_company):**
    ```
    [인사담당자] {이전회사} {이전프로젝트} 끝나고 {다음회사} {다음프로젝트} 시작하기 전에 {N}개월 정도 있었는데, 이 기간에 뭐 했어?
      1) 이직 준비하면서 사이드 프로젝트나 공부한 게 있음
      2) 프리랜서/컨설팅 같은 단기 일을 했음
      3) 이 기간은 건너뛰기
    ```

    **회사내 갭 (intra_company):**
    ```
    [인사담당자] {회사} 안에서 {이전프로젝트} 끝나고 {다음프로젝트} 들어가기까지 {N}개월 정도 걸렸는데, 그 사이에 뭐 했어?
      1) 부서 이동이나 역할 전환 과정이 있었음
      2) 다른 내부 프로젝트에 짧게 참여했음
      3) 이 기간은 건너뛰기
    ```

    ### 핵심 규칙

    - **"이 기간은 건너뛰기"는 반드시 마지막 선택지로 포함** — 예외 없음
    - 유저가 건너뛰기를 선택하면 추가 질문 없이 즉시 수용
    - 실질적 답변이 오면 에피소드 추출을 위한 후속 질문 1-2개 진행 가능
    - 갭 기간과 관련 회사/프로젝트 이름을 질문에 반드시 포함 (맥락 없는 "이 기간에 뭐 했어?"는 금지)
    ```

    Also add one new prohibition to the existing `## 금지사항` section:
    ```
    - 갭 프로빙 시 "왜 이 기간 동안 아무것도 안 했어?" 같은 심문조 금지 — 기회 프레이밍으로만 접근
    ```
  </action>
  <verify>
    <automated>grep -c "갭 프로빙 모드" plugins/resume/.claude/agents/hr.md</automated>
  </verify>
  <acceptance_criteria>
    - hr.md contains `## 갭 프로빙 모드` section header
    - hr.md contains `건너뛰기` at least 3 times (in rules + both question patterns)
    - hr.md contains `inter_company` and `intra_company` gap type handling
    - hr.md gap probing shows exactly 3 options per question (2 substantive + 건너뛰기)
    - hr.md contains `기회 프레이밍` as the framing rule
    - hr.md contains `[인사담당자]` prefix in both question pattern examples
    - hr.md 금지사항 section contains new prohibition about 심문조 금지
    - hr.md gap probing patterns include company/project names in question text (not generic)
  </acceptance_criteria>
  <done>hr.md has a complete gap probing mode section with inter/intra company patterns, mandatory skip option, and sensitivity guidelines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profiler prompt -> findings-inbox.jsonl | LLM-generated pattern findings written to structured JSON |
| hr agent -> user | Gap probing questions touch potentially sensitive career gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | I (Info Disclosure) | Pattern analysis | accept | Patterns derived from user's own resume data; no external data exposure |
| T-04-06 | S (Spoofing) | Pattern finding format | mitigate | Structured JSON schema with required fields prevents malformed findings from entering pipeline |
| T-04-07 | R (Repudiation) | Gap probing skip | mitigate | Mandatory "건너뛰기" option ensures user can opt out; intentional_gap marking creates audit trail |
</threat_model>

<verification>
1. profiler.md contains 4-category pattern analysis section between agent dispatch and findings recording
2. profiler.md pattern output format matches findings-inbox.jsonl schema
3. hr.md gap probing generates questions with 2 substantive + 1 skip option
4. hr.md uses opportunity framing, not interrogation framing
5. Both files maintain existing section structure (no deletions, only additions)
</verification>

<success_criteria>
Profiler has a structured cross-company pattern analysis framework. HR agent has gap probing mode with mandatory skip option. Both produce output compatible with SKILL.md orchestration rules (Plan 03). All existing prompt content preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profiler-analysis-timeline-pattern/04-02-SUMMARY.md`
</output>
