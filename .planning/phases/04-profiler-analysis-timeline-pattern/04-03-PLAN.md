---
phase: 04-profiler-analysis-timeline-pattern
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - plugins/resume/skills/resume-panel/SKILL.md
autonomous: true
requirements: [TIME-02, TIME-03, PTRN-02, PTRN-03]

must_haves:
  truths:
    - "SKILL.md handles timeline_gap_found findings by routing to HR agent in gap probing mode"
    - "SKILL.md handles pattern_detected findings by including them in Conversation Briefing"
    - "SKILL.md tracks intentional_gaps in meta.json and prevents re-probing"
    - "SKILL.md routes pattern findings to the agent specified in target_agent field"
    - "Skip selection on gap probing triggers intentional_gap marking in meta.json"
  artifacts:
    - path: "plugins/resume/skills/resume-panel/SKILL.md"
      provides: "timeline_gap_found + pattern_detected handlers + intentional_gap rules"
      contains: "timeline_gap_found"
  key_links:
    - from: "SKILL.md timeline_gap_found handler"
      to: "HR agent gap probing mode"
      via: "Agent tool call"
      pattern: "갭 프로빙 모드"
    - from: "SKILL.md pattern_detected handler"
      to: "Conversation Briefing"
      via: "MEDIUM urgency routing"
      pattern: "pattern_detected"
    - from: "SKILL.md skip handler"
      to: "meta.json intentional_gaps"
      via: "Bash write"
      pattern: "intentional_gaps"
---

<objective>
Add timeline_gap_found and pattern_detected message handling rules to SKILL.md orchestrator, plus intentional_gap tracking.

Purpose: Wire the timeline gap findings (from episode-watcher.mjs, Plan 01) to HR agent gap probing (Plan 02) and pattern findings (from profiler.md, Plan 02) to Conversation Briefing. This completes the full pipeline: hook detection -> findings -> orchestrator routing -> agent action -> user interaction.

Output: SKILL.md with complete orchestration rules for timeline gap probing, pattern-based episode discovery, and intentional gap tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-CONTEXT.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-RESEARCH.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-01-SUMMARY.md
@.planning/phases/04-profiler-analysis-timeline-pattern/04-02-SUMMARY.md

<interfaces>
<!-- Current SKILL.md message handling rules (lines 385-476): -->
<!-- 1. [resume-panel] profiler trigger → background Agent call -->
<!-- 2. [resume-panel:HIGH] → immediate AskUserQuestion -->
<!-- 3. [resume-panel:MEDIUM] → deliver on company/project change -->
<!-- 4. LOW → user-requested only -->
<!-- 5. [resume-panel:SO-WHAT] → So What chain -->

<!-- New additions needed: -->
<!-- 6. [resume-panel:MEDIUM] (timeline_gap_found) → gap probing via HR -->
<!-- 7. [resume-panel:MEDIUM] (pattern_detected) → Conversation Briefing + agent routing -->
<!-- + intentional_gap tracking rules in meta.json -->

<!-- Timeline gap finding format (from episode-watcher.mjs after Plan 01): -->
```jsonl
{"type":"timeline_gap_found","urgency":"MEDIUM","message":"{from_end} ~ {to_start} ({N}개월) 공백: {from_project}({from_company}) 종료 후 {to_project}({to_company}) 시작 전","context":{"from_company":"","from_project":"","to_company":"","to_project":"","gap_months":0,"gap_type":"inter_company|intra_company"}}
```

<!-- Pattern finding format (from profiler.md after Plan 02): -->
```jsonl
{"type":"pattern_detected","urgency":"MEDIUM","message":"패턴 발견: '{name}' -- ...","context":{"pattern_name":"","category":"","evidence_episodes":[...],"unexplored_company":"","suggested_question":"","target_agent":"시니어|C-Level|인사담당자|채용담당자"}}
```

<!-- HR agent gap probing output format (from hr.md after Plan 02): -->
```
[인사담당자] {gap probing question}
  1) {substantive option 1}
  2) {substantive option 2}
  3) 이 기간은 건너뛰기
```

<!-- meta.json intentional_gaps format (from RESEARCH.md): -->
```json
{
  "intentional_gaps": [
    { "from": "2022.03", "to": "2022.09", "marked_at": "2026-04-08T10:00:00Z" }
  ]
}
```

<!-- Existing SKILL.md AskUserQuestion conversion rules apply to HR gap probing output -->
<!-- Existing findings routing: hook delivers MEDIUM on company change -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeline_gap_found and pattern_detected handling rules to SKILL.md</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md (full 557-line file -- understand message handling section structure, especially items 1-5 and interview flow protection rules)
    - plugins/resume/.claude/agents/hr.md (gap probing mode section added in Plan 02 -- understand input format HR expects)
    - plugins/resume/.claude/agents/profiler.md (pattern analysis section added in Plan 02 -- understand output format)
    - .planning/phases/04-profiler-analysis-timeline-pattern/04-RESEARCH.md (SKILL.md handler pattern example, lines 333-351)
  </read_first>
  <action>
    Modify the "메시지 처리 규칙" section in SKILL.md to add two new message handling rules and intentional gap tracking.

    **1. Add item 6 after the existing item 5 (SO-WHAT handler).** This handles timeline_gap_found findings that arrive via the `[resume-panel:MEDIUM]` message when the finding type is timeline_gap_found:

    ```markdown
    6. **`[resume-panel:MEDIUM]` (timeline_gap_found)** → 갭 프로빙
       - MEDIUM 메시지 내용에 "공백" + "개월"이 포함되어 있으면 타임라인 갭 finding으로 판단
       - meta.json의 `intentional_gaps` 배열 확인 — 이미 스킵한 갭이면 무시하고 다음 메시지로
       - HR 에이전트를 갭 프로빙 모드로 호출:
         ```
         Agent(
           prompt: "갭 프로빙 모드. 공백: {from_company} {from_project} 종료({from_end}) ~ {to_company} {to_project} 시작({to_start}). {gap_months}개월 공백. 갭 유형: {gap_type}. 유저 프로파일: {프로파일 요약}. 리서처 조사: {관련 회사 조사 결과}."
         )
         ```
       - HR 리턴을 AskUserQuestion으로 변환 (기존 변환 규칙 동일 적용)
       - 유저 응답 처리:
         - **"건너뛰기" 선택 시**:
           - meta.json의 `intentional_gaps` 배열에 추가:
             ```bash
             # meta.json 읽기 → intentional_gaps에 추가 → 다시 쓰기
             # { "from": "{from_end}", "to": "{to_start}", "marked_at": "{ISO timestamp}" }
             ```
           - 해당 갭에 대해 다시 질문하지 않음 — hook에서도 intentional_gaps를 확인하여 finding 재생성 방지
           - 인터뷰 즉시 복귀
         - **실질적 답변 시**:
           - 답변에서 에피소드 추출하여 resume-source.json에 저장 (기존 저장 규칙 동일)
           - HR 에이전트가 후속 질문 1-2개 더 할 수 있음 (일반 HR 모드로 전환)
           - 후속 질문 완료 후 인터뷰 복귀
       - **갭 프로빙 제한**: 한 세션에 최대 3개 갭까지만 프로빙. meta.json에 `gap_probes_this_session` 카운터로 추적. 초과 시 나머지 갭 finding은 무시.
    ```

    **2. Add item 7 after the new item 6.** This handles pattern_detected findings:

    ```markdown
    7. **`[resume-panel:MEDIUM]` (pattern_detected)** → 패턴 기반 에피소드 발굴
       - MEDIUM 메시지 내용에 "패턴 발견"이 포함되어 있으면 패턴 finding으로 판단
       - 대화 브리핑의 별도 섹션으로 포함:
         ```
         ## 대화 브리핑
         - 유저가 지금까지 강조한 키워드/주제: [...]
         - 이미 다룬 영역: [...]
         - 아직 안 다룬 영역: [...]
         - 유저의 직전 답변 요약: [...]
         - **발견된 패턴**: {패턴 이름} — {근거 에피소드 요약}. {미탐색 회사 추정 또는 심화 방향}.
         ```
       - 패턴의 `target_agent` 필드에 지정된 에이전트를 다음 에이전트 선택 시 우선 고려
       - `suggested_question`이 있으면 해당 에이전트 호출 시 컨텍스트에 포함:
         ```
         Agent(
           prompt: "{기존 컨텍스트}. 패턴 분석에서 제안된 질문: '{suggested_question}'. 이 질문을 기반으로 에피소드를 발굴해줘."
         )
         ```
       - 패턴 finding은 즉시 전달하지 않고 다음 에이전트 호출 시 자연스럽게 결합 (MEDIUM urgency 규칙 따름)
    ```

    **3. Update the "에이전트 선택 기준" section** to add gap probing routing:

    Add after the existing "So What 체인 모드 활성화 → C-Level" line:
    ```markdown
    - 타임라인 갭 프로빙 필요 → 인사담당자 (갭 프로빙 모드)
    - 패턴에서 `target_agent` 지정됨 → 해당 에이전트 우선
    ```

    **4. Update the "인터뷰 흐름 보호" section** to add gap probing flow rules:

    Add after the existing SO-WHAT flow protection rules:
    ```markdown
    - 갭 프로빙은 single-turn이므로 인터뷰 흐름을 크게 방해하지 않는다. 유저가 건너뛰기를 선택하면 즉시 복귀한다
    - 갭 프로빙과 SO-WHAT이 동시 도착하면 SO-WHAT을 먼저 처리한다 (체인 완료 후 갭 프로빙)
    - 갭 프로빙 제한(3개/세션)에 도달하면 나머지 갭 finding은 조용히 무시한다
    ```
  </action>
  <verify>
    <automated>grep -c "timeline_gap_found\|pattern_detected\|intentional_gaps\|갭 프로빙 모드\|gap_probes_this_session" plugins/resume/skills/resume-panel/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains item `6.` with `timeline_gap_found` handling
    - SKILL.md contains item `7.` with `pattern_detected` handling
    - SKILL.md item 6 contains `Agent(` call with "갭 프로빙 모드" prompt to HR agent
    - SKILL.md item 6 contains `intentional_gaps` array update logic for skip handling
    - SKILL.md item 6 contains `gap_probes_this_session` session limit (3)
    - SKILL.md item 7 contains `대화 브리핑` section with `발견된 패턴` field
    - SKILL.md item 7 contains `target_agent` routing logic
    - SKILL.md item 7 contains `suggested_question` passthrough to agent context
    - SKILL.md 에이전트 선택 기준 contains `타임라인 갭 프로빙 필요 → 인사담당자`
    - SKILL.md 에이전트 선택 기준 contains `패턴에서 target_agent 지정됨`
    - SKILL.md 인터뷰 흐름 보호 contains gap probing flow rules (single-turn, SO-WHAT priority, session limit)
    - SKILL.md preserves all existing items 1-5 unchanged
    - SKILL.md preserves all existing AskUserQuestion conversion rules unchanged
  </acceptance_criteria>
  <done>SKILL.md orchestration rules complete for timeline gap probing (item 6) and pattern-based episode discovery (item 7). Intentional gap tracking, session limits, and flow protection rules all in place. Full pipeline connected: hook -> findings -> orchestrator -> agent -> user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding message -> SKILL.md handler | Pattern matching on message content ("공백" + "개월", "패턴 발견") to determine finding type |
| user skip response -> meta.json | Skip selection triggers permanent intentional_gap marking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | S (Spoofing) | Finding type detection | mitigate | Two-signal detection ("공백" + "개월" for gaps, "패턴 발견" for patterns) reduces false classification |
| T-04-09 | D (DoS) | Gap probing flood | mitigate | Session limit of 3 gap probes prevents overwhelming user with gap questions |
| T-04-10 | T (Tampering) | intentional_gap persistence | mitigate | from/to date pair matching ensures only exact gap match is skipped; timestamps provide audit trail |
| T-04-11 | E (Elevation) | target_agent routing | accept | target_agent routes to existing agents only; no new agent creation or privilege escalation possible |
</threat_model>

<verification>
1. SKILL.md contains items 6 and 7 in message handling section
2. timeline_gap_found routes to HR agent with gap probing mode context
3. pattern_detected routes via Conversation Briefing to target_agent
4. Skip selection creates intentional_gap entry in meta.json
5. Session limit (3 gaps) is documented and enforced
6. All existing SKILL.md rules (items 1-5, AskUserQuestion, flow protection) are preserved
</verification>

<success_criteria>
SKILL.md orchestrates the full timeline gap + pattern detection pipeline. Users experience: gap found -> HR asks about it -> user answers or skips -> gap resolved. Patterns found -> included in briefing -> next agent asks pattern-informed questions. All findings flow through existing MEDIUM urgency routing. Intentional gaps are tracked and never re-probed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profiler-analysis-timeline-pattern/04-03-SUMMARY.md`
</output>
