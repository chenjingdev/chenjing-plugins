---
phase: 01-askuserquestion-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/resume/skills/resume-panel/SKILL.md
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "오케스트레이터가 에이전트 리턴을 파싱하여 AskUserQuestion을 호출한다"
    - "라운드 0의 세션 시작/직군 확인/정보 확인 질문이 AskUserQuestion으로 렌더링된다"
    - "서술형 질문(번호 선택지 없음)은 셀렉트 박스 없이 평문으로 전달된다"
    - "AskUserQuestion 실패 시 재시도 후 텍스트 번호 방식으로 폴백한다"
    - "프로파일러 findings 전달 시 AskUserQuestion으로 감싸서 표시한다"
    - "유저가 Other를 선택하면 자유 텍스트를 에이전트에 그대로 전달한다"
  artifacts:
    - path: "plugins/resume/skills/resume-panel/SKILL.md"
      provides: "AskUserQuestion conversion logic, fallback, Round 0 conversion, findings wrapping"
      contains: "AskUserQuestion"
  key_links:
    - from: "SKILL.md 에이전트 호출 방법 section"
      to: "AskUserQuestion tool call"
      via: "parsing agent output [에이전트명] + N) options -> AskUserQuestion JSON"
      pattern: "AskUserQuestion"
    - from: "SKILL.md 자율 오케스트레이션 section"
      to: "AskUserQuestion tool call"
      via: "wrapping findings in AskUserQuestion with meta-question"
      pattern: "AskUserQuestion.*분석 결과"
---

<objective>
SKILL.md 오케스트레이터 프롬프트를 수정하여, 에이전트 질문을 AskUserQuestion 셀렉트 박스로 변환하는 전체 로직을 구현한다. 라운드 0 오케스트레이터 질문도 AskUserQuestion으로 변환하고, 폴백 및 findings 전달 로직도 추가한다.

Purpose: 유저가 텍스트 번호 입력 대신 클릭 가능한 셀렉트 박스로 인터뷰에 답변할 수 있게 한다.
Output: SKILL.md에 AskUserQuestion 변환/폴백/findings 래핑 로직이 포함된 오케스트레이터 프롬프트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-askuserquestion-ux/01-CONTEXT.md
@.planning/phases/01-askuserquestion-ux/01-RESEARCH.md

<interfaces>
<!-- AskUserQuestion JSON Schema (verified from research) -->
```json
{
  "questions": {
    "type": "array",
    "minItems": 1,
    "maxItems": 4,
    "items": {
      "required": ["question", "header", "options", "multiSelect"],
      "properties": {
        "question": { "type": "string" },
        "header": { "type": "string", "description": "max 12 chars" },
        "multiSelect": { "type": "boolean" },
        "options": {
          "type": "array",
          "minItems": 2,
          "maxItems": 4,
          "items": {
            "required": ["label", "description"],
            "properties": {
              "label": { "type": "string", "description": "1-5 words, concise" },
              "description": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
```

<!-- Agent output format (all 5 frontstage agents follow this) -->
```
[에이전트명] {질문 텍스트}
  1) {선택지1}
  2) {선택지2}
  3) {선택지3} (optional)
  4) 직접입력
```

<!-- Header mapping (all fit within 12 chars) -->
- [시니어] -> "시니어"
- [C-Level] -> "C-Level"
- [채용담당자] -> "채용담당자"
- [인사담당자] -> "인사담당자"
- [커피챗: {이름}] -> "{이름}" (truncate to 12 chars if needed)

<!-- CRITICAL: Do NOT add AskUserQuestion to SKILL.md frontmatter allowed-tools (bug #29547) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md 핵심 원칙 + 에이전트 호출 + 유저 응답 + 라운드 0 AskUserQuestion 변환</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md (FULL — 416 lines, primary modification target)
    - .planning/phases/01-askuserquestion-ux/01-RESEARCH.md (parsing rules, schema, examples)
  </read_first>
  <action>
SKILL.md를 다음 4개 섹션에서 수정한다. 각 수정은 기존 내용을 교체하는 형태다.

**수정 1: 핵심 원칙 #2 (line 14)**

현재:
```
2. **선택지 필수** — 질문 시 반드시 2~3개 선택지 + 마지막에 `직접입력` 항목 제시. 유저는 번호 또는 자유 텍스트로 응답
```

변경:
```
2. **선택지 필수** — 질문 시 반드시 최대 4개 선택지 제시. AskUserQuestion이 자동으로 "Other" 옵션을 추가하므로 에이전트가 `직접입력`을 따로 넣지 않는다
```

**수정 2: 허용 질문 예시 (line 40-44)**

현재:
```
✓ 모든 선택지 질문의 마지막 항목은 "직접입력":
  "조사해보니 ㅇㅇ회사에서 ㅁㅁ플랫폼 만들었던데,
    1) 네가 처음부터 설계
    2) 기존 구조를 개선
    3) 직접입력"
```

변경:
```
✓ 모든 선택지 질문은 AskUserQuestion 셀렉트 박스로 렌더링됨 (직접입력은 자동 "Other"로 제공):
  "조사해보니 ㅇㅇ회사에서 ㅁㅁ플랫폼 만들었던데,
    1) 네가 처음부터 설계
    2) 기존 구조를 개선"
```

**수정 3: "에이전트 호출 방법" 섹션 끝부분 (line 96) 교체**

현재의 line 96:
```
에이전트가 리턴한 질문을 유저에게 그대로 전달한다. `[에이전트명]` 태그를 붙여서 누가 묻는 건지 표시한다.
```

이 한 줄을 아래 전체 블록으로 교체:
```markdown
에이전트가 리턴한 질문을 AskUserQuestion 셀렉트 박스로 변환하여 유저에게 전달한다.

#### AskUserQuestion 변환 규칙

에이전트 리턴 텍스트를 다음 절차로 파싱하여 AskUserQuestion을 호출한다:

1. **질문 텍스트 추출**: `[에이전트명]`부터 첫 번째 `\n  1)` 또는 `\n1)` 패턴 직전까지가 question
2. **선택지 추출**: 각 `N) 텍스트` 라인을 순서대로 options로 변환
3. **직접입력 제거**: 마지막 선택지가 "직접입력"이면 드롭 (AskUserQuestion이 자동으로 "Other" 추가)
4. **header 매핑**: `[에이전트명]` 태그에서 이름 추출
   - `[시니어]` → "시니어"
   - `[C-Level]` → "C-Level"
   - `[채용담당자]` → "채용담당자"
   - `[인사담당자]` → "인사담당자"
   - `[커피챗: {이름}]` → "{이름}" (12자 이내로 자르기)
5. **label 생성**: 선택지 텍스트를 1~5단어로 축약하여 label, 원문 전체를 description에 넣음
6. **서술형 질문 감지**: `N)` 패턴이 없으면 AskUserQuestion을 호출하지 않고 에이전트 텍스트를 평문으로 그대로 전달
7. **multiSelect**: 항상 `false`

변환 예시:
```
# 에이전트 리턴:
[시니어] Kafka 도입한 거 봤는데, 이거 직접 밀었어 아니면 이미 있던 거야?
  1) 내가 제안해서 도입
  2) 기존에 있었고 활용만
  3) 마이그레이션 작업
  4) 직접입력

# AskUserQuestion 호출:
questions: [{
  question: "Kafka 도입한 거 봤는데, 이거 직접 밀었어 아니면 이미 있던 거야?",
  header: "시니어",
  options: [
    { label: "제안 도입", description: "내가 제안해서 도입" },
    { label: "기존 활용", description: "기존에 있었고 활용만" },
    { label: "마이그레이션", description: "마이그레이션 작업" }
  ],
  multiSelect: false
}]
// "직접입력" 드롭 — "Other"가 자동 추가됨
```

**주의: AskUserQuestion을 SKILL.md frontmatter의 `allowed-tools`에 절대 추가하지 않는다** (bug #29547 — allowed-tools에 넣으면 빈 응답이 자동 완성됨).
```

**수정 4: "유저 응답 처리" 섹션 (line 108-112) 교체**

현재:
```
### 유저 응답 처리

유저가 번호를 입력하면 해당 선택지로 처리한다. 그 외 텍스트는 자유 답변으로 처리한다.

유저 답변에서 에피소드를 추출할 수 있으면 바로 resume-source.json에 저장한다.
```

변경:
```markdown
### 유저 응답 처리

AskUserQuestion의 응답을 처리한다:
- 유저가 선택지를 클릭하면: 해당 옵션의 description 텍스트를 에이전트 답변으로 사용
- 유저가 "Other"를 선택하여 자유 텍스트를 입력하면: 입력된 텍스트를 그대로 에이전트 답변으로 사용
- 폴백 모드(텍스트 번호 방식)에서: 유저가 번호를 입력하면 해당 선택지로 처리, 그 외 텍스트는 자유 답변

유저 답변에서 에피소드를 추출할 수 있으면 바로 resume-source.json에 저장한다.
```

**수정 5: 라운드 0 세션 시작 (line 126-130) 교체**

현재:
```
있으면:
\```
이전에 작업하던 이력서 소스가 있네요.
  1) 이어하기
  2) 새로 시작
\```
```

변경:
```markdown
있으면 AskUserQuestion으로 묻는다:
\```
AskUserQuestion({
  questions: [{
    question: "이전에 작업하던 이력서 소스가 있네요. 이어할까요?",
    header: "세션",
    options: [
      { label: "이어하기", description: "이전에 작업하던 이력서 소스에서 이어서 진행" },
      { label: "새로 시작", description: "이전 데이터를 무시하고 처음부터 새로 시작" }
    ],
    multiSelect: false
  }]
})
\```
```

**수정 6: 라운드 0 직군 확인 (line 150-155) 교체**

현재:
```
직군은 타겟 JD에서 자동 추출하고 유저에게 확인한다:
\```
JD 보니까 {직군} 포지션인데, 본인 직군도 {직군}이 맞아?
1) 맞아
2) 아니, 다른 직군인데 이 포지션에 지원하려는 거
3) 직접입력
\```
```

변경:
```markdown
직군은 타겟 JD에서 자동 추출하고 AskUserQuestion으로 확인한다:
\```
AskUserQuestion({
  questions: [{
    question: "JD 보니까 {직군} 포지션인데, 본인 직군도 {직군}이 맞아?",
    header: "직군 확인",
    options: [
      { label: "맞아", description: "본인 직군과 JD 포지션이 같음" },
      { label: "다른 직군", description: "다른 직군인데 이 포지션에 지원하려는 것" }
    ],
    multiSelect: false
  }]
})
\```
```

**수정 7: 라운드 0 기존 자료 확인 (line 167-173 부근)**

현재의 정보 확인 블록:
```
맞아요? 수정할 거 있으면 알려주세요.
```

이 확인 질문도 AskUserQuestion으로 변환:
```markdown
확인 질문은 AskUserQuestion으로 묻는다:
\```
AskUserQuestion({
  questions: [{
    question: "이력서에서 가져왔어요.\n- 이름: {이름}\n- 경력: {N}년\n- 회사: {회사목록}\n\n맞아요?",
    header: "정보 확인",
    options: [
      { label: "맞아", description: "가져온 정보가 맞음" },
      { label: "수정 필요", description: "수정할 것이 있음" }
    ],
    multiSelect: false
  }]
})
\```
```

참고: "기존 이력서, 포트폴리오..." 자료 수집 프롬프트(line 138-140)는 파일/텍스트 수집이므로 셀렉트 박스가 불필요하다. 평문 그대로 유지한다.
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" plugins/resume/skills/resume-panel/SKILL.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep "AskUserQuestion" SKILL.md returns 5+ matches (conversion rules, Round 0 session, Round 0 job role, Round 0 info confirm, example)
    - grep "header.*시니어" SKILL.md returns at least 1 match (header mapping present)
    - grep "multiSelect.*false" SKILL.md returns at least 1 match
    - grep "서술형 질문 감지" SKILL.md or grep "패턴이 없으면" SKILL.md returns a match (narrative passthrough rule)
    - grep "직접입력 제거" SKILL.md or grep "직접입력.*드롭" SKILL.md returns a match (drop rule present)
    - grep "allowed-tools" SKILL.md frontmatter does NOT contain "AskUserQuestion" (bug #29547 prevention)
    - grep "최대 4개 선택지" SKILL.md returns a match (핵심 원칙 #2 updated)
    - grep "label.*description" SKILL.md returns at least 1 match (label condensation rule)
  </acceptance_criteria>
  <done>
    - 핵심 원칙 #2가 "최대 4개 선택지 + 자동 Other"로 업데이트됨
    - "에이전트 호출 방법" 섹션에 7단계 AskUserQuestion 변환 규칙이 포함됨
    - "유저 응답 처리" 섹션이 AskUserQuestion 응답 처리로 교체됨
    - 라운드 0의 세션 시작, 직군 확인, 정보 확인이 모두 AskUserQuestion 형식으로 변환됨
    - 서술형 질문 패스스루 규칙이 명시됨
    - AskUserQuestion이 frontmatter allowed-tools에 추가되지 않음
  </done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md 폴백 로직 + 프로파일러 findings AskUserQuestion 래핑</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md (Task 1 수정 후 상태를 반드시 먼저 읽기)
    - .planning/phases/01-askuserquestion-ux/01-RESEARCH.md (Example 4: findings delivery, Example 5: fallback)
  </read_first>
  <action>
Task 1 수정이 완료된 SKILL.md에 두 가지를 추가한다.

**추가 1: AskUserQuestion 폴백 섹션 (변환 규칙 직후에 새 섹션 삽입)**

"에이전트 호출 방법" 섹션 내 AskUserQuestion 변환 규칙 블록 바로 다음에 폴백 로직을 추가한다:

```markdown
#### AskUserQuestion 폴백

AskUserQuestion 호출이 실패하면 (빈 응답, 에러, 타임아웃) 다음 절차를 따른다:

1. "셀렉트 박스 로딩에 문제가 생겼어요. 다시 시도할게요." 메시지 표시
2. 동일 파라미터로 AskUserQuestion 재시도 (1회)
3. 두 번째 시도도 실패하면:
   - "텍스트로 답변해주세요." 메시지 표시
   - 에이전트 원문 텍스트를 번호 선택지 포함하여 그대로 표시:
     ```
     [시니어] 질문 텍스트
       1) 선택지1
       2) 선택지2
       3) 선택지3
     번호를 입력하거나 자유롭게 답변해주세요.
     ```
   - 유저의 텍스트 입력을 번호 또는 자유 답변으로 처리 (기존 방식)
```

**추가 2: 자율 오케스트레이션 섹션 HIGH/MEDIUM findings를 AskUserQuestion으로 래핑**

"자율 오케스트레이션 — Hook 메시지 처리" 섹션의 항목 2번과 3번을 수정한다.

현재 항목 2 (line 322-324):
```
2. **`[resume-panel:HIGH]`** → 현재 질문-답변 사이클 완료 후 유저에게 전달
   - "아 그리고 방금 분석 결과가 나왔는데 — {내용}"
   - 전달 후 바로 다음 인터뷰 질문으로 복귀
```

변경:
```markdown
2. **`[resume-panel:HIGH]`** → 현재 질문-답변 사이클 완료 후 AskUserQuestion으로 전달
   - findings 내용을 메타 질문으로 감싸서 AskUserQuestion 호출:
     ```
     AskUserQuestion({
       questions: [{
         question: "프로파일러 분석 결과: {findings 내용 요약}. 어떻게 할래요?",
         header: "분석 결과",
         options: [
           { label: "관련 경험 있음", description: "관련 경험이 있는데 아직 얘기 안 한 것" },
           { label: "진짜 없음", description: "해당 경험이 정말 없음, 갭으로 기록" },
           { label: "넘어가기", description: "이 피드백은 나중에 보기" }
         ],
         multiSelect: false
       }]
     })
     ```
   - findings의 구체적 내용에 맞게 options를 적절히 조정한다
   - 전달 후 바로 다음 인터뷰 질문으로 복귀
```

현재 항목 3 (line 326-327):
```
3. **`[resume-panel:MEDIUM]`** → 현재 프로젝트/회사 에피소드 수집이 끝나면 전달
   - "여기까지 정리하면서 리뷰 결과도 같이 볼게 — {내용}"
```

변경:
```markdown
3. **`[resume-panel:MEDIUM]`** → 현재 프로젝트/회사 에피소드 수집이 끝나면 AskUserQuestion으로 전달
   - findings 내용을 다음 질문에 자연스럽게 결합:
     ```
     AskUserQuestion({
       questions: [{
         question: "여기까지 정리하면서 리뷰 결과도 같이 볼게 — {findings 요약}. 어떻게 할까?",
         header: "리뷰 결과",
         options: [
           { label: "더 자세히", description: "이 부분에 대해 더 자세히 듣기" },
           { label: "다음으로", description: "다음 주제로 넘어가기" }
         ],
         multiSelect: false
       }]
     })
     ```
```
  </action>
  <verify>
    <automated>grep -c "폴백" plugins/resume/skills/resume-panel/SKILL.md | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep "AskUserQuestion 폴백" SKILL.md returns a match (fallback section exists)
    - grep "셀렉트 박스 로딩에 문제가" SKILL.md returns a match (retry message present)
    - grep "두 번째 시도도 실패" SKILL.md or grep "텍스트로 답변" SKILL.md returns a match (text fallback present)
    - grep "분석 결과" SKILL.md in 자율 오케스트레이션 section returns a match with AskUserQuestion (HIGH findings wrapped)
    - grep "리뷰 결과" SKILL.md in 자율 오케스트레이션 section returns a match with AskUserQuestion (MEDIUM findings wrapped)
    - The "인터뷰 흐름 보호" sub-section (line 331-335) remains unchanged
  </acceptance_criteria>
  <done>
    - AskUserQuestion 폴백 섹션이 변환 규칙 직후에 추가됨 (재시도 1회 + 텍스트 번호 폴백)
    - HIGH findings가 AskUserQuestion 메타 질문으로 감싸져서 전달됨
    - MEDIUM findings가 AskUserQuestion으로 전달됨
    - 인터뷰 흐름 보호 규칙은 그대로 유지됨
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Local CLI tool, no network boundaries. All changes are prompt instructions within local markdown files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | AskUserQuestion allowed-tools | mitigate | Explicit warning in SKILL.md: "AskUserQuestion을 allowed-tools에 절대 추가하지 않는다" (bug #29547) |
| T-01-02 | Denial of Service | AskUserQuestion timeout (60s) | mitigate | Fallback mechanism: retry once, then fall back to text number selection |
| T-01-03 | Information Disclosure | N/A | accept | No sensitive data flows through AskUserQuestion — only interview question options |
</threat_model>

<verification>
1. SKILL.md에 "AskUserQuestion" 문자열이 5회 이상 등장
2. frontmatter의 allowed-tools에 AskUserQuestion이 포함되지 않음
3. "AskUserQuestion 변환 규칙" 섹션이 7단계를 모두 포함
4. "AskUserQuestion 폴백" 섹션이 재시도 + 텍스트 폴백을 포함
5. 라운드 0의 세션 시작, 직군 확인, 정보 확인이 AskUserQuestion 형식
6. 자율 오케스트레이션 HIGH/MEDIUM이 AskUserQuestion으로 래핑
</verification>

<success_criteria>
SKILL.md가 에이전트 출력을 AskUserQuestion으로 변환하는 완전한 오케스트레이터 프롬프트를 포함하며, 라운드 0 질문/폴백/findings 래핑이 모두 구현된 상태.
</success_criteria>

<output>
After completion, create `.planning/phases/01-askuserquestion-ux/01-01-SUMMARY.md`
</output>
