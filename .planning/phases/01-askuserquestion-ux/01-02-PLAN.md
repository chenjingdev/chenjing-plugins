---
phase: 01-askuserquestion-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/resume/.claude/agents/senior.md
  - plugins/resume/.claude/agents/c-level.md
  - plugins/resume/.claude/agents/recruiter.md
  - plugins/resume/.claude/agents/hr.md
  - plugins/resume/.claude/agents/coffee-chat.md
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "모든 프론트스테이지 에이전트가 최대 4개 선택지를 리턴하도록 가이드됨"
    - "에이전트 예시에서 직접입력이 제거됨 (AskUserQuestion의 Other가 대체)"
    - "에이전트의 산출 형식과 기존 [에이전트명] 태그 구조는 유지됨"
  artifacts:
    - path: "plugins/resume/.claude/agents/senior.md"
      provides: "Updated option count guideline and examples"
      contains: "최대 4개"
    - path: "plugins/resume/.claude/agents/c-level.md"
      provides: "Updated option count guideline and examples"
      contains: "최대 4개"
    - path: "plugins/resume/.claude/agents/recruiter.md"
      provides: "Updated option count guideline and examples"
      contains: "최대 4개"
    - path: "plugins/resume/.claude/agents/hr.md"
      provides: "Updated option count guideline and examples"
      contains: "최대 4개"
    - path: "plugins/resume/.claude/agents/coffee-chat.md"
      provides: "Updated examples without 직접입력"
  key_links:
    - from: "Agent output (N) options format)"
      to: "SKILL.md AskUserQuestion conversion"
      via: "Agent outputs max 4 substantive options -> orchestrator drops any remaining 직접입력 -> AskUserQuestion adds Other"
      pattern: "최대 4개"
---

<objective>
5개 프론트스테이지 에이전트 프롬프트의 선택지 수 가이드라인을 "2~3개"에서 "최대 4개"로 업데이트하고, 예시에서 "직접입력" 항목을 제거한다. 에이전트 출력 포맷과 구조는 변경하지 않는다.

Purpose: AskUserQuestion의 options 슬롯(최대 4개)을 최대한 활용하면서, "직접입력"이 자동 "Other"와 중복되지 않게 한다.
Output: 5개 에이전트 프롬프트에 업데이트된 선택지 가이드라인과 예시.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-askuserquestion-ux/01-CONTEXT.md
@.planning/phases/01-askuserquestion-ux/01-RESEARCH.md

<interfaces>
<!-- Current agent option count patterns to find and replace -->

senior.md line 14: "오케스트레이터에게 질문 1개 + 선택지 2~3개를 리턴한다"
senior.md line 30: "2. **선택지 2~3개 제시** — 유저가 번호로 선택하거나 자유 텍스트로 답할 수 있도록"
c-level.md line 30: "2. **선택지 2~3개 제시**"
recruiter.md line 30: "2. **선택지 2~3개 제시**"
hr.md line 30: "2. **선택지 2~3개 제시**"

<!-- 직접입력 occurrences to remove from examples -->
senior.md: lines 55, 64, 73, 84, 93, 103, 111, 121
c-level.md: lines 55, 64, 75, 84, 93, 103, 113
recruiter.md: lines 50, 69, 85
hr.md: lines 51, 60, 69, 79
coffee-chat.md: line 23

<!-- AskUserQuestion schema reminder -->
options: 2-4 items per question + automatic "Other"
Agents should output 2-4 substantive options (no 직접입력)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 에이전트 선택지 가이드라인 + 예시 업데이트</name>
  <files>
    plugins/resume/.claude/agents/senior.md
    plugins/resume/.claude/agents/c-level.md
    plugins/resume/.claude/agents/recruiter.md
    plugins/resume/.claude/agents/hr.md
    plugins/resume/.claude/agents/coffee-chat.md
  </files>
  <read_first>
    - plugins/resume/.claude/agents/senior.md (full — 129 lines)
    - plugins/resume/.claude/agents/c-level.md (full — 121 lines)
    - plugins/resume/.claude/agents/recruiter.md (full — 110 lines)
    - plugins/resume/.claude/agents/hr.md (full — 87 lines)
    - plugins/resume/.claude/agents/coffee-chat.md (full — 34 lines)
    - .planning/phases/01-askuserquestion-ux/01-RESEARCH.md (lines 133-145 — files to modify section)
  </read_first>
  <action>
5개 에이전트 파일에 동일한 패턴의 수정을 적용한다. 에이전트의 산출 형식(구조, 태그)은 변경하지 않는다.

**A. senior.md (3 changes)**

1. Line 14 — 역할 설명의 선택지 수:
   현재: `오케스트레이터에게 질문 1개 + 선택지 2~3개를 리턴한다`
   변경: `오케스트레이터에게 질문 1개 + 선택지 최대 4개를 리턴한다 (직접입력은 넣지 않는다 — 오케스트레이터가 자동 추가)`

2. Line 30 — 질문 생성 규칙의 선택지 수:
   현재: `2. **선택지 2~3개 제시** — 유저가 번호로 선택하거나 자유 텍스트로 답할 수 있도록`
   변경: `2. **선택지 최대 4개 제시** — 오케스트레이터가 셀렉트 박스로 변환. 직접입력은 넣지 않는다`

3. 모든 예시 코드 블록에서 `N) 직접입력` 라인 삭제:
   - line 55: `4) 직접입력` 삭제
   - line 64: `4) 직접입력` 삭제
   - line 73: `4) 직접입력` 삭제
   - line 84: `4) 직접입력` 삭제
   - line 93: `4) 직접입력` 삭제
   - line 103: `3) 직접입력` 삭제
   - line 111: `3) 직접입력` 삭제

4. 산출 형식 예시 (line 117-122):
   현재:
   ```
   [시니어] {질문 텍스트}
     1) {선택지1}
     2) {선택지2}
     3) {선택지3 (선택)}
     4) 직접입력
   ```
   변경:
   ```
   [시니어] {질문 텍스트}
     1) {선택지1}
     2) {선택지2}
     3) {선택지3 (선택)}
     4) {선택지4 (선택)}
   ```
   (직접입력 대신 선택적 4번째 선택지로 변경. 직접입력은 오케스트레이터가 Other로 자동 추가.)

**B. c-level.md (2 changes)**

1. Line 30:
   현재: `2. **선택지 2~3개 제시**`
   변경: `2. **선택지 최대 4개 제시** — 직접입력은 넣지 않는다 (오케스트레이터가 자동 추가)`

2. 모든 예시에서 `N) 직접입력` 라인 삭제:
   - lines 55, 64, 75, 84, 93, 103: `4) 직접입력` 삭제

3. 산출 형식 예시 (line 109-114):
   `4) 직접입력` → `4) {선택지4 (선택)}`

**C. recruiter.md (2 changes)**

1. Line 30:
   현재: `2. **선택지 2~3개 제시**`
   변경: `2. **선택지 최대 4개 제시** — 직접입력은 넣지 않는다 (오케스트레이터가 자동 추가)`

2. 모든 예시에서 `N) 직접입력` 라인 삭제:
   - line 50: `3) 직접입력` 삭제
   - line 69: `3) 직접입력` 삭제

3. 산출 형식 예시 (line 81-86):
   `3) 직접입력` → `3) {선택지3 (선택)}`

**D. hr.md (2 changes)**

1. Line 30:
   현재: `2. **선택지 2~3개 제시**`
   변경: `2. **선택지 최대 4개 제시** — 직접입력은 넣지 않는다 (오케스트레이터가 자동 추가)`

2. 모든 예시에서 `N) 직접입력` 라인 삭제:
   - lines 51, 60, 69: `4) 직접입력` 삭제

3. 산출 형식 예시 (line 75-80):
   `4) 직접입력` → `4) {선택지4 (선택)}`

**E. coffee-chat.md (1 change)**

1. 예시에서 `4) 직접입력` (line 23) 삭제

coffee-chat.md에는 "선택지 2~3개" 가이드라인이 없으므로 (규칙 섹션에 "구체적 선택지 필수"만 있음) 가이드라인 수정은 불필요. 예시의 직접입력만 삭제.

**모든 파일 공통 주의사항:**
- 산출 형식의 `[에이전트명] {질문 텍스트}` 구조는 절대 변경하지 않는다
- `N) 선택지` 번호 포맷도 그대로 유지한다
- 오직 "2~3개" → "최대 4개" 변경과 "직접입력" 라인 삭제만 수행한다
  </action>
  <verify>
    <automated>grep -rl "직접입력" plugins/resume/.claude/agents/ | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - grep "직접입력" across all 5 agent files returns 0 matches (all removed)
    - grep "최대 4개" plugins/resume/.claude/agents/senior.md returns 2 matches (role + rule)
    - grep "최대 4개" plugins/resume/.claude/agents/c-level.md returns 1 match
    - grep "최대 4개" plugins/resume/.claude/agents/recruiter.md returns 1 match
    - grep "최대 4개" plugins/resume/.claude/agents/hr.md returns 1 match
    - grep "오케스트레이터가 자동 추가" senior.md returns at least 1 match
    - grep "\[시니어\]" senior.md still returns a match (output format preserved)
    - grep "\[C-Level\]" c-level.md still returns a match (output format preserved)
    - grep "\[채용담당자\]" recruiter.md still returns a match (output format preserved)
    - grep "\[인사담당자\]" hr.md still returns a match (output format preserved)
  </acceptance_criteria>
  <done>
    - 5개 에이전트 모두 "최대 4개" 선택지 가이드라인으로 업데이트됨
    - 모든 예시에서 "직접입력" 라인이 제거됨
    - 에이전트 산출 형식 ([에이전트명] 태그 + 번호 선택지) 구조가 그대로 유지됨
    - 직접입력은 오케스트레이터의 AskUserQuestion "Other"가 대체함을 명시
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Local agent prompt files. No network boundaries. No user input processing changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Agent prompt changes | accept | Agent prompts contain no secrets; only interview question patterns. Changes are additive (remove 직접입력, update count). |
</threat_model>

<verification>
1. `grep -rl "직접입력" plugins/resume/.claude/agents/` returns empty (all 직접입력 removed)
2. `grep -rl "최대 4개" plugins/resume/.claude/agents/` returns 4 files (senior, c-level, recruiter, hr)
3. `grep -rl "2~3개" plugins/resume/.claude/agents/` returns empty (all old counts replaced)
4. Each agent file still has its `[에이전트명]` output format section intact
</verification>

<success_criteria>
5개 프론트스테이지 에이전트의 선택지 가이드라인이 "최대 4개"로 통일되고, 모든 예시에서 "직접입력"이 제거되어, SKILL.md의 AskUserQuestion 변환 로직과 호환되는 상태.
</success_criteria>

<output>
After completion, create `.planning/phases/01-askuserquestion-ux/01-02-SUMMARY.md`
</output>
