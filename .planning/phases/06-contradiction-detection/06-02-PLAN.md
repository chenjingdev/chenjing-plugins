---
phase: 06-contradiction-detection
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - plugins/resume/skills/resume-panel/SKILL.md
autonomous: true
requirements: [CONTR-01, CONTR-02, CONTR-03]
must_haves:
  truths:
    - "contradiction_detected HIGH finding이 AskUserQuestion 연결 톤 복원 질문으로 유저에게 즉시 제시된다"
    - "복원 질문에 큰 역할 옵션, 작은 역할 옵션, 상황이 달랐음 옵션이 포함된다"
    - "유저 응답에 따라 resume-source.json의 해당 에피소드 STAR 필드가 업데이트된다"
    - "세션당 최대 2개 모순만 제시되어 유저 신뢰가 보호된다"
    - "MEDIUM urgency 모순은 Conversation Briefing에 포함된다"
    - "모순 제시가 인터뷰 흐름(SO-WHAT, gap probing 등)을 방해하지 않는다"
  artifacts:
    - path: "plugins/resume/skills/resume-panel/SKILL.md"
      provides: "contradiction_detected handler (item 9) + flow protection rules + agent selection update"
      contains: "contradiction_detected"
  key_links:
    - from: "findings-inbox.jsonl contradiction_detected"
      to: "SKILL.md item 9 handler"
      via: "hook routes HIGH/MEDIUM messages to orchestrator"
    - from: "SKILL.md AskUserQuestion"
      to: "resume-source.json"
      via: "user response triggers STAR field update via Bash tool"
---

<objective>
SKILL.md에 contradiction_detected 핸들러(item 9)를 추가하고 STAR 필드 업데이트 로직을 구현한다.

Purpose: 프로파일러가 탐지한 모순을 유저에게 연결 톤으로 제시하고, 유저 응답에 따라 에피소드 데이터를 정정하여 이력서 품질을 높인다.
Output: SKILL.md에 contradiction_detected 메시지 핸들러(item 9), 인터뷰 흐름 보호 규칙, meta.json contradictions_presented_this_session 추적
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contradiction-detection/06-CONTEXT.md
@.planning/phases/06-contradiction-detection/06-RESEARCH.md
@.planning/phases/06-contradiction-detection/06-01-SUMMARY.md

<interfaces>
<!-- SKILL.md message handling structure (lines 393-546) -->
<!-- Current items: 1=profiler call, 2=HIGH, 3=MEDIUM, 4=LOW, 5=SO-WHAT, 6=timeline_gap_found, 7=pattern_detected, 8=perspective_shift -->
<!-- New item 9 = contradiction_detected -->

From SKILL.md existing handler pattern (item 8 perspective_shift, for reference):
```markdown
8. **`[resume-panel:MEDIUM]` (perspective_shift)** -> perspective shift question
   - MEDIUM message content contains "관점 전환" -> perspective_shift finding
   - meta.json perspective_shifts_this_session counter check -- 2 or more -> ignore
   - meta.json perspective_shifted_episodes array check -- already shifted -> ignore
   - context target_agent -> invoke in perspective shifting mode
   - Agent return -> AskUserQuestion conversion
   - User response: humble option -> meta tracking + interview resume; upgraded role -> follow-up + meta tracking
   - meta.json perspective_shifts_this_session counter increment
```

From SKILL.md flow protection pattern (lines 533-546):
```markdown
- HIGH feedback: complete current Q&A cycle first, then insert
- MEDIUM/LOW: don't interrupt interview
- SO-WHAT chain: multi-turn, suspend normal flow until chain complete
- SO-WHAT priority over gap probing and perspective shifting
- Session limits: gap probing 3/session, perspective shifting 2/session
```

contradiction_detected finding format (from Plan 01):
```json
{
  "type": "contradiction_detected",
  "urgency": "HIGH" or "MEDIUM",
  "context": {
    "claim_a": { "claim_id", "text", "episode_ref", "company", "project", "period", "star_field" },
    "claim_b": { "claim_id", "text", "episode_ref", "company", "project", "period", "star_field" },
    "contradiction_type": "role_scope | time | scale | contribution",
    "likely_cause": "겸손에 의한 축소 | 기억 오류 | 맥락 차이",
    "restoration_question": "아까 {에피소드A}에서 ... 실제로는 어디까지 한 거야?"
  }
}
```

From SKILL.md existing STAR field update pattern (line 624):
```bash
cat <<'EOF' > ./resume-source.json
{ ... full JSON ... }
EOF
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contradiction_detected handler, session tracking, STAR update, and flow protection to SKILL.md</name>
  <files>plugins/resume/skills/resume-panel/SKILL.md</files>
  <read_first>
    - plugins/resume/skills/resume-panel/SKILL.md (full file -- understand current message handling items 1-8, flow protection rules, agent selection criteria, and STAR update pattern)
    - .planning/phases/06-contradiction-detection/06-CONTEXT.md (locked decisions: connecting tone, session limit 2, AskUserQuestion presentation, STAR update)
    - .planning/phases/06-contradiction-detection/06-RESEARCH.md (Pattern 5: SKILL.md handler with AskUserQuestion + STAR Update)
    - .planning/phases/06-contradiction-detection/06-01-SUMMARY.md (finding format from Plan 01)
  </read_first>
  <action>
Add three sections to SKILL.md:

**A. Message handler item 9 (after item 8 perspective_shift):**

```markdown
9. **`[resume-panel:HIGH]` (contradiction_detected)** -> 모순 복원 질문
   - HIGH 메시지 내용에 "모순 발견" 또는 "역할 모순"이 포함되어 있으면 contradiction_detected finding으로 판단
   - meta.json의 `contradictions_presented_this_session` 카운터 확인 -- 2 이상이면 무시
   - context에서 claim_a, claim_b, contradiction_type, likely_cause, restoration_question 추출
   - AskUserQuestion으로 복원 질문 제시:
     ```
     AskUserQuestion({
       questions: [{
         question: "아까 이야기랑 연결해보면, {에피소드A}에서는 {claim_a.text 요약}라고 했는데 {에피소드B}에서는 {claim_b.text 요약}라고 했거든. 실제로는 어디까지 한 거야?",
         header: "연결 확인",
         options: [
           { label: "{큰 역할 요약}", description: "{claim with bigger role/scale}" },
           { label: "{작은 역할 요약}", description: "{claim with smaller role/scale}" },
           { label: "상황이 달랐음", description: "두 에피소드의 맥락이 달라서 역할이 다른 것" }
         ],
         multiSelect: false
       }]
     })
     ```
   - 유저 응답 처리:
     - **큰 역할 선택**: 작은 claim이 있는 에피소드의 해당 STAR 필드(claim_b.star_field)를 큰 역할 내용으로 업데이트. resume-source.json 전체를 Bash tool로 재저장 (기존 저장 패턴 동일)
     - **작은 역할 선택**: 큰 claim이 있는 에피소드의 해당 STAR 필드(claim_a.star_field)를 작은 역할 내용으로 업데이트
     - **"상황이 달랐음" 선택**: 양쪽 모두 유효, STAR 업데이트 없음. 인터뷰 즉시 복귀
   - STAR 필드 업데이트 시: 해당 필드만 수정하고 나머지 에피소드 내용은 보존. 기존 내용을 정정/보완하며 전체 교체하지 않음
   - meta.json `contradictions_presented_this_session` 카운터 증가
```

For MEDIUM urgency contradictions (time/scale/contribution), add a sub-rule:

```markdown
   - MEDIUM urgency contradiction (메시지에 "모순 발견" 포함 + urgency가 MEDIUM):
     - 대화 브리핑의 별도 섹션으로 포함:
       ```
       ## 대화 브리핑
       - ...기존 항목...
       - **확인 필요**: {contradiction_type} 관련 -- {에피소드A}에서 {claim_a 요약}, {에피소드B}에서 {claim_b 요약}. 다음 관련 질문 시 자연스럽게 확인.
       ```
     - 즉시 AskUserQuestion으로 제시하지 않고, 다음 에이전트 호출 시 브리핑에 포함하여 자연스럽게 확인
```

**B. Flow protection rules addition (in the existing "인터뷰 흐름 보호" section):**

Add these 3 lines to the existing flow protection rules:

```markdown
- 모순 복원은 single-turn이므로 인터뷰 흐름을 크게 방해하지 않는다. 유저가 "상황이 달랐음"을 선택하면 즉시 복귀한다
- 모순 복원과 SO-WHAT이 동시 도착하면 SO-WHAT을 먼저 처리한다 (체인 완료 후 모순 복원)
- 모순 복원 제한(2개/세션)에 도달하면 나머지 모순 finding은 조용히 무시한다
```

**C. Two-signal classification note:**

The handler uses two-signal classification following the established pattern:
- Item 6 (gap): "공백" + "개월"
- Item 7 (pattern): "패턴 발견"
- Item 8 (perspective): "관점 전환"
- Item 9 (contradiction): "모순 발견" 또는 "역할 모순"

Note that contradiction_detected HIGH findings are classified differently from generic HIGH findings (item 2). The two-signal ("모순 발견" OR "역할 모순" in message content) distinguishes them. Generic HIGH findings without these keywords are handled by item 2 as before.

Ensure the contradiction handler is checked BEFORE the generic HIGH handler (item 2) in evaluation order, or add a note that item 9's keyword check takes priority when the message matches.
  </action>
  <verify>
    <automated>grep -c "contradiction_detected" plugins/resume/skills/resume-panel/SKILL.md | grep -q "[1-9]" && grep -c "contradictions_presented_this_session" plugins/resume/skills/resume-panel/SKILL.md | grep -q "[1-9]" && grep -c "연결 확인" plugins/resume/skills/resume-panel/SKILL.md | grep -q "[1-9]" && grep -c "상황이 달랐음" plugins/resume/skills/resume-panel/SKILL.md | grep -q "[1-9]" && grep -c "모순 복원" plugins/resume/skills/resume-panel/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "contradiction_detected" SKILL.md returns match (handler exists)
    - grep "contradictions_presented_this_session" SKILL.md returns match (session counter)
    - grep "연결 확인" SKILL.md returns match (AskUserQuestion header)
    - grep "상황이 달랐음" SKILL.md returns match (third option)
    - grep "모순 복원" SKILL.md returns match in flow protection section
    - grep "모순 발견" SKILL.md returns match (two-signal keyword)
    - grep "star_field" SKILL.md returns match in contradiction context (STAR update reference)
    - grep "resume-source.json" SKILL.md returns match in contradiction handler context (STAR update)
    - grep "SO-WHAT.*모순" OR "모순.*SO-WHAT" SKILL.md returns match (priority rule)
    - grep "2개/세션" OR "2 이상이면 무시" SKILL.md returns match in contradiction context (session limit)
    - grep "MEDIUM.*모순" OR "확인 필요" SKILL.md returns match (MEDIUM urgency handling)
    - Item 9 appears after item 8 in the message handling rules section
  </acceptance_criteria>
  <done>SKILL.md has a contradiction_detected handler (item 9) that: (1) classifies contradiction findings via two-signal detection, (2) enforces session limit of 2 via meta.json counter, (3) presents HIGH urgency contradictions via AskUserQuestion with connecting tone and 3 options (big role, small role, context different), (4) routes MEDIUM urgency contradictions to Conversation Briefing, (5) updates resume-source.json STAR fields based on user response, (6) has flow protection rules for SO-WHAT priority and session limit enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profiler finding -> Orchestrator | Finding data crosses from sub-agent to orchestrator for user presentation |
| User response -> STAR update | User selection determines which episode data gets modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | STAR field update | mitigate | Update only the specific STAR field referenced by claim's star_field metadata. Preserve all other episode content. Amend/correct rather than replace. |
| T-06-05 | D (Denial of Service) | excessive contradiction presentation | mitigate | Session limit of 2 contradictions via meta.json counter. Counter checked before presentation. |
| T-06-06 | R (Repudiation) | user trust erosion | mitigate | Connecting tone ("아까 이야기랑 연결해보면...") enforced. "상황이 달랐음" escape hatch provided. No accusatory framing. |
</threat_model>

<verification>
1. SKILL.md item 9 handles contradiction_detected findings with two-signal classification
2. AskUserQuestion uses connecting tone with "연결 확인" header
3. Three options provided: big role, small role, "상황이 달랐음"
4. Session limit of 2 enforced via contradictions_presented_this_session counter
5. STAR field update only modifies the referenced field, preserves other content
6. MEDIUM urgency contradictions routed to Conversation Briefing, not immediate AskUserQuestion
7. Flow protection: SO-WHAT priority, single-turn behavior, session limit enforcement
8. Two-signal keywords ("모순 발견" / "역할 모순") distinguish from generic HIGH findings
</verification>

<success_criteria>
- contradiction_detected findings are properly classified and handled by SKILL.md
- HIGH urgency (role_scope) contradictions presented via AskUserQuestion with connecting tone
- MEDIUM urgency contradictions included in Conversation Briefing
- User responses trigger appropriate STAR field updates in resume-source.json
- Session limit of 2 contradictions enforced
- Interview flow protected (SO-WHAT priority, single-turn, session cap)
- Full contradiction pipeline wired: profiler detection -> finding generation -> hook routing -> orchestrator handling -> user interaction -> STAR update
</success_criteria>

<output>
After completion, create `.planning/phases/06-contradiction-detection/06-02-SUMMARY.md`
</output>
